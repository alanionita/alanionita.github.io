<?xml version="1.0" encoding="UTF-8"?>
        <rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/">
            <channel>
                <title>Alan Ionita - Fullstack developer porfolio, React, AWS, Typescript, bun</title>
                <link>https://alanionita.github.io/portfolio--svelte</link>
                <description>Latest blog posts</description>
                <language>en-GB</language>
                <lastBuildDate>Wed, 15 Apr 2026 20:49:39 GMT</lastBuildDate>
                <item>
            <title>Observability Stack, 2026</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2026-04-15-observability-stack</link>
            <pubDate>Wed, 15 Apr 2026 00:00:00 +0100</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Observability Stack, 2026</h1>
<p>Observability, also know as how we know that a system is performing as expected, is a lot easier in 2026.</p>
<p>That being said the fragmentation of solutions, providers, ways of doing things, makes it hard to pick the right tools for job.</p>
<p>For my use case, I wanted a simple and reliable container observability stack, but I also wanted to be able to extend that system for IoT (high cardinality).</p>
<p>The Grafana stack was an obvious choice due popularity and and large pool of resources. The stack is showcased beautifully in this repo, and it comes with a great video explanation, <a href="https://github.com/JamesTurland/JimsGarage/tree/main/Grafana-Monitoring">Jim's Garage, Grafana Monitoring</a>.</p>
<p>The trouble with good quality resources is that they are not always up to date. A side-effect of a fast moving sector, in a fast moving industry.</p>
<p>Below is a fork of the stack, with some crucial updates for 2026. I'll continue to refer back to Jim's stack for comparison.</p>
<p>For reference the forked repo is here, <a href="https://github.com/alanionita/observability-stack">Alan Ionita, Repo, Observability Stack</a>.</p>
<h1>Monitoring, Jim's example</h1>
<p>The core goal is to monitor the Docker containers on the host. understand performance, resource use, and be alerted of failure or health concerns.</p>
<p>Jim uses Telegraf and InfluxDB for metrics collection and storage. Telegraf, acting as the middleware connector service that scrapes metrics and syncs the data with the time-series database InfluxDB.</p>
<p>Visualizing the data is done with Grafana.</p>
<p>Graphite is also included as an alternative time-series storage and graphing option (using the Cairo graphics library).</p>
<p>Prometheus, is deployed here to scrape and display Crowdsec IP data. Not to monitor Docker containers.</p>
<p>Grafana Promtail and Grafana Loki are used together for log files. Loki handles the scraping of log files. Promtail takes log files, parses them, and stores them in a database.</p>
<h1>Grafana Promtail deprecated</h1>
<p>Promtail has been deprecated in March 2026.</p>
<p>The recommended migration path is to use Grafana Alloy, another collector from Grafana.</p>
<h1>Grafana Alloy</h1>
<p>Promtail had a symbiotic relationship with Loki, or log collector.</p>
<p>Alloy is self-contained and acts like an orchestrator for tasks like scraping, logging, relabeling, and storing.</p>
<p>We've gone from having:</p>
<pre><code>----------------------------------------------------------

Metrics:

[ Telegraf ] -> [ InfluxDB ] &#x3C;-> [ Grafana ]

----------------------------------------------------------

Logs:

[ Loki ] -> [ Promtail ] &#x3C;-> [ Grafana ]
 
----------------------------------------------------------

Alerts:

[ Prometheus ]

----------------------------------------------------------
</code></pre>
<p>To this:</p>
<pre><code>----------------------------------------------------------

Metrics:

[ Alloy ] -> [ cAdvisor] -> [ Prometheus ] &#x3C;-> [ Grafana ]
[ Alloy ] -> [ cAdvisor] -> [ Mimir ]      &#x3C;-> [ Grafana ]

----------------------------------------------------------

Logs:

[Alloy ] -> [ Loki ] &#x3C;-> [ Grafana ]
 
----------------------------------------------------------

Alerts:

[ Prometheus ]

----------------------------------------------------------
</code></pre>
<p>Alloy is far more powerful than Promtail and runs at a system level. This runtime also impacts how we deal with environment variables for Alloy.</p>
<p>In this modern stack Alloy is where we define our observability rules. Alloy instructs Loki to collect logs. Alloy instructs cAdvisor to collect Docker metrics and push them to Prometheus or Mimir.</p>
<h1>Monitoring, Fork</h1>
<h2>Alloy, Config</h2>
<p>The 'config.alloy' file is the core of the new stack and where we find all of our business logic.</p>
<p>The syntax for Alloy is an implementation of HCL syntax from Terraform, with each construct being a block - <a href="https://grafana.com/docs/alloy/latest/get-started/syntax/">Alloy, Syntax</a>.</p>
<p>For each rule we can have:</p>
<ul>
<li>A collection block or 'scrape' / 'exporter' block</li>
<li>A parsing / transforming / enriching block or 'relabel' block</li>
<li>A storage block or 'write' block</li>
</ul>
<p>The structure is similar to functional programming:</p>
<pre><code>Inputs -> Function -> Outputs

Function
- Inputs transformation or enrichment
</code></pre>
<p>The top of the 'config.alloy' contains all the write or output blocks, followed by the actual logic.</p>
<p>Finally we define the scrape block which takes our raw / enriched data and sends it to the output destination.</p>
<h2>Alloy, Metrics</h2>
<p>In the fork we use Prometheus and Grafana Mimir for Docker container metrics collection and storage.</p>
<p>The ease and Prometheus and PromQL, is why I replaced InfluxDB (and Telegraf) for container metrics.</p>
<p>InfluxDB doesn't support or follow the implementation of the helpter utility functions found in PromQL, and this leads to scenarios where a Prometheus dashboard can drift away from an InfluxDB dashboard. Latency can be an issue in this design too, Prometheus itself being faster to ingest vs InfluxDB + Telegraf.</p>
<p>Also from local results the InfluxDB and Telegraf services consumed more resources than Prometheus and Mimir combined.</p>
<p>Prometheus is already in the stack for Crowdsec data and alerts, why not use it for short-term Docker container data collection and storage. Mimir serving as long-term storage for data audits of 30 days or more.</p>
<p>In Alloy we first define the outputs, or write blocks:</p>
<p>[config.alloy]</p>
<pre><code>prometheus.remote_write "main" {
  endpoint {
    url = "http://prometheus:9090/api/v1/write"
  }
}

prometheus.remote_write "mimir" {
  endpoint {
    url = "http://mimir:9009/api/v1/push"
  }
}
</code></pre>
<p>We will need a matching Docker container for these write block destinations, Prometheus and Mimir.</p>
<p>Then we define the rules for collection or scrape blocks:</p>
<pre><code>prometheus.exporter.cadvisor "main" {
  docker_only = true
  storage_duration = "10m"
}

discovery.relabel "cadvisor_rename" {
    targets = prometheus.exporter.cadvisor.main.targets

    rule {
        target_label = "job"
        replacement  = "integrations/docker"
    }

    rule {
        target_label = "instance"
        replacement  = constants.hostname
    }
}

prometheus.scrape "scraper" {
  targets    = discovery.relabel.cadvisor_rename.output
  forward_to = [ prometheus.remote_write.main.receiver, prometheus.remote_write.mimir.receiver ]


  scrape_interval = "15s"
  scrape_timeout = "15s"
}
</code></pre>
<p>Internally the Alloy 'prometheus.exporter.cadvisor' uses  cAdvisor, a popular Docker container monitoring tool -<a href="https://github.com/google/cadvisor">Google, cAdvisor, repo</a>.</p>
<p>We won't need a container for cAdvisor in the stack, but Alloy runs cAdvisor at a system level on the host via the 'prometheus.exporter.cadvisor' block.</p>
<p>The large collection of blocks built into Alloy and the low-level runtime, makes Alloy a powerful data collector and orchestrator.</p>
<p>The cAdvisor config for 'storage_duration' is more than the default (2m) to guarantee that we have enough data stored in memory in case of downstream latency. Without it we might loose some metric batches.</p>
<p>The 'discovery.relabel' is a basic example and should be greatly expanded for production use. The ability to relabel metrics, at the Alloy level, means that we can transform metrics before they are ingested by Prometheus.</p>
<p>In the example we also define a Prometheus recording rule <code>configs/prometheus/rules/container_heartbeat.yml</code>, where we produce a computed metric from a raw metric, sampled at a rate of 1m.</p>
<p>Don't confuse relabels with recording rules, since it's generally expected to relabel before Prometheus, and never using the recording rules. Recording rules are mainly used for data aggregation.</p>
<p>The next Alloy block, the scraper block takes the relabeled metrics and pushes them to the outputs, Prometheus and Mimir.</p>
<p>The structure simplified:</p>
<ul>
<li>targets: what data are we pushing, eg. 'discovery.relabel'</li>
<li>forward_to: where are pushing the data to eg. 'prometheus.remote_write.main.receiver'</li>
<li>scrape_interval: how often we retrieve a batch and push, here mapped to Prometheus default of 15s</li>
<li>scrape_timeout: how often do we discard data in case of lag or errors; here mapped to 15s seconds so that we loose at most 1 batch, which shouldn't introduce data problems unless we get repeated discards over a prolonged period.</li>
</ul>
<p>Alloy is flexible and can forward data to multiple targets, and can also receive data from multiple sources.</p>
<p>This ability can reduce lag when pushing metrics to Prometheus and more sources, Alloy doing this in parallel. Whilst with Telegraf and InfluxDB we would have to deal with a queue, adding lag to the process. When benchmarking Telegraf + InfluxDB as long-term storage for Prometheus we also noticed a large number of out of sync batches.</p>
<p>Alloy contains 1 scraping interval rule for two targets, making it less likely to introduce data inconsistencies via independent service configs.</p>
<p>All the "rules" of our observability system, are now in Alloy. With the storage services (Prometheus, Mimir, Loki) containing self-management, runtime, or networking rules.</p>
<p>With Alloy there's less need to chain services, eg. Prometheus which pushes to Telegraf which pushes to InfluxDB all via independent service apis, prone for data format type errors.</p>
<p>Next we need to configure the Alloy Docker container.</p>
<h2>Alloy, Docker</h2>
<p>Key goals for the Alloy container config:</p>
<ul>
<li>Bind mounts for service config, outgoing to container</li>
<li>Bind mount for incoming Alloy data with local folder sync</li>
<li>Bind mounts for accessing Docker metrics and system logs from host</li>
<li>Port opening for '12345' and the CLI flag '--server.http.listen-addr=0.0.0.0:12345'; enables access for the Alloy Web UI</li>
</ul>
<pre><code class="language-yml">  alloy: 
    container_name: alloy
    image: grafana/alloy:latest
    restart: unless-stopped
    networks:
      - grafana-monitoring
    ports:
      - "12345:12345"
    volumes:
      - ./configs/config.alloy:/etc/alloy/config.alloy:ro
      - ./data/alloy:/var/lib/alloy/data
      - /var/run/docker.sock:/var/run/docker.sock
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    command: 
      - run 
      - --server.http.listen-addr=0.0.0.0:12345    # Required to access Alloy UI on http://localhost:12345/
      - --storage.path=/var/lib/alloy/data 
      - /etc/alloy/config.alloy
</code></pre>
<p>The image version should be pinned in production to avoid drift.</p>
<h2>General, Configs</h2>
<p>The fork project introduces a folder for configs with nested folders per service where multiple files are required. Alloy, Loki, and Mimir are single file configs, but Prometheus and Grafana are folders.</p>
<p>Now that we have configured Alloy, lets configure the write blocks that Alloy will push data to: Prometheus and Mimir</p>
<h2>Prometheus, Config</h2>
<p>Simple Prometheus config without any business logic, apart from 'storage.tsdb.retention' set to 12h.</p>
<pre><code class="language-yml">global:
  scrape_interval: 15s
  evaluation_interval: 30s
  body_size_limit: 15MB
  sample_limit: 1500
  target_limit: 30
  label_limit: 30
  label_name_length_limit: 200
  label_value_length_limit: 200
storage:
  tsdb:
    retention: 
      time: 12h
rule_files:
  - "rules/container_heartbeat.yml"
</code></pre>
<p>Once again that is the intent: all the business logic staying in the Alloy configuration.</p>
<p>Prometheus data is to be held for a maximum of 12 hours. The data is NOT deleted automatically, but moved to a tombstone location after that time window.</p>
<p>The script at 'test-scripts/prometheus-clear-tombstones.sh' allows you to clear the tombstones manually, but the Prometheus container must be running with the 'admin' API enabled via the CLI flag.</p>
<p>The 'scrape_interval' dictates to Prometheus how often we collect metrics, and with 'evaluation_interval' we are waiting for two metric batches to evaluate the data.</p>
<p>'body_size_limit' to 'label_value_length_limit' represent metrics performance optimizations, see <a href="https://prometheus.io/docs/prometheus/latest/configuration/configuration/">Prometheus, Configuration</a>.</p>
<p>The 'rule_files' configures both recording and alerting rules but here, only recording rules are shown.</p>
<p>[rules/container_heartbeat.yml]</p>
<pre><code class="language-yml">groups:
  - name: container_heartbeat
    interval: 1m
    rules:
      - record: container_heartbeat:bpm
        expr: (changes(container_last_seen{name=~".+"}[1m])>0)
</code></pre>
<p>The rule checks the liveness of a container. 'container_last_seen' is a timestamp gauge produced by cAdvisor, <a href="https://github.com/google/cadvisor/blob/master/docs/storage/prometheus.md">Monitoring cAdvisor with Prometheus</a>. The timestamp should change 3 times per minute, matching the number of gaps between the 15 second Prometheus scrape intervals. The result of the aggregation becomes an indicator for perfect health with anything below highlighting health issues. The indicator can display minor 'flickers' in liveness, lasting between 15s-60s, as well as larger downtime events.</p>
<p>The recording rule is then down-sampled to 5min intervals,</p>
<p>We use the aggregate 'container_heartbeat:bpm' metric to visualize the container heartbeat using a heatmap chart.</p>
<h2>Prometheus, Docker</h2>
<p>Key goals for the Prometheus container config:</p>
<ul>
<li>Enabled remote write receiver CLI flag</li>
<li>Bind mounts for config outgoing to container</li>
<li>Bind mount for incoming data from Prometheus to filesystem</li>
<li>Docker user permissions, needed for incoming data write</li>
<li>Service healthcheck, required by Grafana container</li>
</ul>
<pre><code class="language-yml">  prometheus:
    container_name: prometheus
    user: ${GRAFANA_DOCKER_USER}
    image: prom/prometheus
    restart: unless-stopped
    networks:
      - grafana-monitoring
    ports:
      - 9090:9090
    volumes:
      - ./configs/prometheus/global.yml:/etc/prometheus/prometheus.yml:ro
      - ./configs/prometheus/rules/:/etc/prometheus/rules/:ro
      - ./data/prometheus:/prometheus/data
    command:
      - --web.enable-remote-write-receiver
      - --config.file=/etc/prometheus/prometheus.yml
      # - --web.enable-admin-api    # Required to clear tombstone files
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 5s
      timeout: 3s
      retries: 5
</code></pre>
<p>If we want Alloy to be succesful in pushing data to Prometheus, we need the 'web.enable-remote-write-receiver' CLI flag.</p>
<p>Bind mounts are used here to sync the configs. Note that root config name 'global.yml' is not the same as the Prometheus default 'prometheus.yml'. The mount is rewriting from 'global' to 'prometheus' files.</p>
<p>Data from the container is synced within the repo folder '/data/prometheus'. And because of this we need to ensure that the container makes use of a user with read/write access to the local files.</p>
<p>Since the Prometheus storage is a dependency for visualization, we introduced a healthcheck which will produce the 'grafana' container dependency rule.</p>
<p>The rest is a standard Prometheus config: port exposing to for the Prometheus UI host access, network config for inter-container requests, etc.</p>
<h2>Mimir, Config</h2>
<p>Key goals for the Mimir service config:</p>
<ul>
<li>Single node configuration</li>
<li>Storage set to filesystem</li>
<li>Data retention set to 60days</li>
<li>Data discard rule for out of time data</li>
</ul>
<pre><code class="language-yml"># Do not use this configuration in production.
# It is for demonstration purposes only.
multitenancy_enabled: false

blocks_storage:
  backend: filesystem
  bucket_store:
    sync_dir: /data/tsdb-sync 
  filesystem:
    dir: /data/blocks
  tsdb:
    dir: /data/tsdb

compactor:
  data_dir: /data/compactor
  sharding_ring:
    kvstore:
      store: memberlist

limits:
  # Delete from storage metrics data older than 2 months.
  compactor_blocks_retention_period: 60d
  # Process metric with timestamps of a wider time delay
  out_of_order_time_window: 5m

distributor:
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: memberlist

ingester:
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: memberlist
    replication_factor: 1

ruler_storage:
  backend: filesystem
  filesystem:
    dir: /data/rules

server:
  http_listen_port: 9009
  grpc_listen_port: 9080
  log_level: error

store_gateway:
  sharding_ring:
    replication_factor: 1
</code></pre>
<p>By design Mimir is a distributed system that uses cloud providers for storage. Here we bypass those recommendations, in favour of a single node runtime with local file-system storage. This config is achieved via "storage.gateway.sharding_ring.replication_factor", "multitenancy_enabled", and "blocks_storage.backend". It is recommended that in production you migrate from file-system and single-node, to a distributed setup with a cloud-provider for storage.</p>
<p>Data retention is set to 60days, since Mimir is our Prometheus long-term storage. This grants us a data buffer when fulfilling the 30day data audit requirement. This config is achieved via "limits.compactor_blocks_retention_period".</p>
<p>During development some "out of time" errors were discovered. An anomaly in this use case, because "Prometheus TSDB only accepts in-order samples that are less than one hour old, discarding everything else" - <a href="https://grafana.com/blog/new-in-grafana-mimir-introducing-out-of-order-sample-ingestion/">Mimir, Introducing out-of-order sample ingestion</a>.</p>
<p>"out of time" errors might be a development-only error caused by frequent restarts interfering with metrics processing pipeline, creating a "write-ahead log" (WAL) backlog.</p>
<p>Equally the processing of 'old metrics' at 1h intervals would introduce lag to the system if high cardinality is expected. The overall health and performance of the system is greater than the value of such a long metrics queue.</p>
<p>To guarantee the system performance and to clearly specify expected expiry, we set the 'limits.out_of_order_time_window' to 5min. Wider time delay metrics will be discarded, saving on host resources with minimal data loss.</p>
<p>The rest follows stock configuration for Mimir, <a href="https://grafana.com/docs/mimir/latest/configure/configuration-parameters/">Mimir, Configuration Parameters</a>.</p>
<h2>Mimir, Docker</h2>
<p>Key goals for the Mimir container config:</p>
<ul>
<li>Bind volume for service config outgoing to container</li>
<li>Bind volume for incoming Mimir data writen to filesystem</li>
<li>Healthcheck required for Grafana container</li>
</ul>
<pre><code class="language-yml">  mimir:
    container_name: mimir
    image: grafana/mimir-alpine
    networks:
      - grafana-monitoring
    ports:
      - "9009:9009"
    volumes:
      - ./configs/mimir-config.yml:/etc/mimir/mimir.yaml:ro
      - ./data/mimir:/data:rw
    command:
      - "-config.file=/etc/mimir/mimir.yaml"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9009/ready"]
      interval: 10s
      timeout: 30s
      retries: 5
</code></pre>
<h2>Monitoring, Conclusion</h2>
<p>Three containers (Alloy, Prometheus, Mimir), collection logic stored in Alloy, orchestraction logic stored in Alloy, data stored in Prometheus and Mimir, and an abstracted implementation of 'cAdvisor' via Alloy components.</p>
<p>We no longer need Telegraf and InfluxDB.</p>
<p>We can also replace Promtail with Alloy, but it was never really used in the monitoring context. Promtail was used in the logging context, which we will cover next.</p>
<h1>Logging, Fork</h1>
<p>In the source, Promtail was quoted as a storage options for logs. The actual purpose for Promtail was to scraping and push log data into Loki. Loki handled the storage and indexing layer for logs.</p>
<p>Now that Promtail is deprecated, in the fork we will replace it with Alloy.</p>
<h2>Alloy, Logs</h2>
<p>Similar to the metrics out config contains:</p>
<ul>
<li>A write definition, mapped to Loki contain push api</li>
<li>A scraper definition, via the 'discovery.docker' component</li>
<li>A simple relabeling rule</li>
<li>A forwarding block from the scraper to the writer</li>
</ul>
<pre><code>// Targets
// > Groups all of the *write components together

loki.write "main" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}

...


// Logging

discovery.docker "linux" {
  host = "unix:///var/run/docker.sock"
}

discovery.relabel "docker_logs" {
    targets = []

    rule {
        source_labels = ["__meta_docker_container_name"]
        regex = "/(.*)"
        target_label = "container_name"
    }

    rule {
      target_label = "instance"
      replacement  = constants.hostname
    }
  }

loki.source.docker "main" {
  host       = "unix:///var/run/docker.sock"
  targets    = discovery.docker.linux.targets
  relabel_rules = discovery.relabel.docker_logs.rules
  forward_to = [loki.write.main.receiver]
}
</code></pre>
<p>'discovery.docker' finds the containers and exposes them as targets, hence the 'docker.sock' host definition, <a href="https://grafana.com/docs/grafana-cloud/send-data/alloy/reference/components/discovery/discovery.docker/">Grafana, Alloy, Components, discover.docker</a>.</p>
<p>Previous Promtail logs scraping, is now achieved in Alloy with this 'discovery.docker' component.</p>
<p>Alloy has another component for the same purpose - 'loki.source.docker'. Currently unclear what are the differences between the two, <a href="https://grafana.com/docs/grafana-cloud/send-data/alloy/reference/components/loki/loki.source.docker/">Grafana Alloy, Components, loki.source.docker</a>. Perhaps 'discovery.docker' represents a more generalized implementation.</p>
<p>Lets move on to define the Loki Docker container.</p>
<h2>Loki, Config</h2>
<p>Key goals for the Loki service config:</p>
<ul>
<li>Loki is a distributed system and once again we are configuring it as a singular node service via 'common.replication_factor'</li>
<li>Specify data to be held for 24h via 'schema_config.configs' entry, set with 'index.period'</li>
</ul>
<pre><code class="language-yml"># DOCS: https://grafana.com/docs/loki/latest/configure/examples/configuration-examples/
# This is a complete configuration to deploy Loki backed by the filesystem.
# The index will be shipped to the storage via tsdb-shipper.

auth_enabled: false

limits_config:
  allow_structured_metadata: true
  volume_enabled: true

distributor:
  otlp_config:
    # List of default otlp resource attributes to be picked as index labels
    # CLI flag: -distributor.otlp.default_resource_attributes_as_index_labels
    default_resource_attributes_as_index_labels: [service.name service.namespace service.instance.id deployment.environment deployment.environment.name cloud.region cloud.availability_zone k8s.cluster.name k8s.namespace.name k8s.container.name container.name k8s.replicaset.name k8s.deployment.name k8s.statefulset.name k8s.daemonset.name k8s.cronjob.name k8s.job.name]

server:
  http_listen_port: 3100

common:
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory
  replication_factor: 1
  path_prefix: /tmp/loki

schema_config:
  configs:
  - from: 2020-05-15
    store: tsdb
    object_store: filesystem
    schema: v13
    index:
      prefix: index_
      period: 24h

storage_config:
  tsdb_shipper:
    active_index_directory: /tmp/loki/index
    cache_location: /tmp/loki/index_cache

pattern_ingester:
  enabled: true
</code></pre>
<p>Rest is a stock Loki config, <a href="https://grafana.com/docs/loki/latest/configure/examples/configuration-examples/#9-s3-with-sse-kms-snippetyaml">Grafana Loki, Configure examples</a>.</p>
<h2>Docker, Loki</h2>
<p>Key goals for the Loki container config:</p>
<ul>
<li>Binding the config outgoing to the container</li>
<li>Binding container data to local data folder</li>
<li>Add user with correct read/write permissions for the incoming container data sync with file-system</li>
</ul>
<pre><code class="language-yml">loki:
    container_name: loki
    user: ${GRAFANA_DOCKER_USER}
    image: grafana/loki:main
    restart: unless-stopped
    networks:
      - grafana-monitoring
    ports:
      - "3100:3100"
    volumes:
      - ./data/loki:/etc/loki
      - ./configs/loki.yml:/etc/loki/local-config.yaml:ro
    command: -config.file=/etc/loki/local-config.yaml
</code></pre>
<h1>Visuals</h1>
<p>For visualization we leverage Grafana. In the source example the user, data source, and dashboard configs were done manually via the Grafana UI. This cause a big of friction, we needed an InfluxDB account, to create a token, to import InfluxDB as a data source, in order the create a dashboard that visualizes that data source.</p>
<p>In the fork provisioning is used or infrastructure-as-code to configure the data sources and dashboards.</p>
<p>Environment variables and container variables are also implemented. Some are used by Grafana as internal feature flags for the initial account creation, whilst others are used for variable expansion within provisioning rules.</p>
<h2>Docker, Grafana</h2>
<p>Key goals for the Grafana container config:</p>
<ul>
<li>Container depends on the data sources being alive, and healthy: Prometheus, Mimir; Loki should probably be added to the list</li>
<li>Bind volume sync for provisioning; usually containing the 'datasources/' and 'dashboard/' configs; the 'dashboard' configs don't contain any actual dashboard panel logic, just linking to the ultimate dashboard json file</li>
<li>Bind volume sync for provisioned dashboards: where we store the actual json config for the dashboards</li>
<li>Environment config for admin user, password, provisioning wait timeout; acting as internal CLI flags for Grafana</li>
<li>Extra environment variables used within provisioning configs</li>
</ul>
<pre><code class="language-yml">  grafana:
    container_name: grafana
    user: ${GRAFANA_DOCKER_USER}
    depends_on: 
      prometheus:
        condition: service_healthy
      mimir:
        condition: service_healthy
    image: grafana/grafana-oss:latest
    restart: unless-stopped
    networks:
      - grafana-monitoring
    ports:
     - 3000:3000/tcp
    volumes:
      - ./configs/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./configs/grafana/dashboards-json:/var/lib/grafana/dashboards:ro
      - ./data/grafana:/var/lib/grafana
    environment: 
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASS}
      GF_PROVISIONING_WAIT_TIMEOUT: 60
      LOKI_URL: ${LOKI_URL}
</code></pre>
<p>Environment:</p>
<ul>
<li>Everything with 'GF_*' an internal Grafana flag</li>
<li>The presence of "GF_SECURITY_ADMING_USER" and "*_PASSWORD" automatically triggers the initial signup flow with the entries provided; manual login with default credentials to create an admin account is no longer required</li>
<li>'PROVISIONING_WAIT_TIMEOUT' grants a time buffer to the initial sign-up flow</li>
<li>'LOKI_URL' is a custom variable that is expanded within the 'config/grafana/provisioning/*' files</li>
</ul>
<p>Grafana will automatically configure the data sources and dashboards at startup if the 'provisioning/*' files are present.</p>
<p>With this, our observability stack only requires 1 command to be fully operational: 'docker compose up' or the alias 'make up'.</p>
<h2>Data source, Loki</h2>
<p>Basic config for Loki data source with environment variable expansion for 'url:', and some minor optimisations under 'jsonData:'.</p>
<p>[configs/provisioning/datasources/loki.yml]</p>
<pre><code class="language-yml">apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    orgId: 1
    url: ${LOKI_URL}
    basicAuth: false
    isDefault: false
    version: 1
    editable: false
    jsonData:
      timeout: 60
      maxLines: 1000
</code></pre>
<h2>Data source, Prometheus</h2>
<p>Basic config for Prometheus with optimisations under 'jsonData'. We could also expand the 'url:' value from the environment.</p>
<p>[configs/provisioning/datasources/prometheus.yml]</p>
<pre><code class="language-yml">apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    orgId: 1
    url: http://prometheus:9090
    jsonData:
      httpMethod: POST
      prometheusType: Prometheus
      prometheusVersion: 3.3.0
      cacheLevel: 'High'
      timeInterval: 15s  # Prometheus scrape interval
      manageAlerts: true
      disableRecordingRules: false
      seriesEndpoint: false
      incrementalQuery: true
      incrementalQueryOverlapWindow: 10m
</code></pre>
<p>'jsonData' config details:</p>
<ul>
<li>'timeInterval': matched the scraping of Prometheus (15s); same as default value, but specified here for clarity.</li>
<li>'manageAlerts': allows Grafana to view and show the Prometheus alerts and recording rules configured</li>
<li>'prometheusType': Prometheus for native and Mimir or etc for derivatives</li>
<li>'prometheusVersion': 3.3.0; based major.minor version</li>
<li>'cacheLevel': high query caching in the Grafana UI, leads to faster loading dashboards and less strain on the data source</li>
<li>'disableRecordingRules': used to disable recording rules, improving data source availability; here used to say that we are 'definitely are using recording rules', one recoding rule being the basis for a shared panel across two dashboards</li>
<li>'seriesEndpoint': default, but defined as if to say 'not using the series endpoint'</li>
<li>'incrementalQuery': by default Grafana will run the query each dashboard interval, putting strain on the data source; this flag allows for the fetching of new results, appending to the old data cache; beta feature</li>
<li>'incrementalQueryOverlapWindow': set to the default of 10m, specified here for clarity; dependent on the above flag.</li>
</ul>
<h2>Data source, Mimir</h2>
<p>Basic config for Mimir with optimisations under 'jsonData'. We could also expand the 'url:' value from the environment.</p>
<p>[configs/provisioning/datasources/mimir.yml]</p>
<pre><code class="language-yml">apiVersion: 1

datasources:
  - name: Mimir
    type: prometheus
    orgId: 1
    url: http://mimir:9009/prometheus
    jsonData:
      httpMethod: POST
      prometheusType: Mimir
      cacheLevel: 'High'
      timeInterval: 15s     # Prometheus scrape interval
      manageAlerts: false
      disableRecordingRules: false
      seriesEndpoint: false
      incrementalQuery: true
      incrementalQueryOverlapWindow: 10m
</code></pre>
<p>Basic config for Mimir with optimisations under 'jsonData', similar to above Prometheus config. We could also expand the 'url:' value from the environment.</p>
<p>Alerts usage is planned on the short-term storage (Prometheus), so Mimir has 'manageAlerts' disabled.</p>
<p>An interesting config quirk to outline is that the top level 'type' is still 'prometheus', whilst 'jsonData.prometheusType' is 'Mimir'.</p>
<h2>Dashboards, Provisioning</h2>
<p>[configs/provisioning/dashboards/default.yml]</p>
<pre><code class="language-yml">apiVersion: 1

providers:
  - name: dashboards
    type: file
    updateIntervalSeconds: 30
    editable: true          # Allows UI edits
    disableDeletion: false  # Blocks UI deletion
    allowUiUpdates: false   # Blocks UI updates
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true
</code></pre>
<p>Details of the config:</p>
<ul>
<li>'name' set to generic 'dashboards'.</li>
<li>'folder': not specified here since we're using a parent folder with enabled file structure detection.</li>
<li>'updateIntervalSeconds': dashboards refresh value.</li>
<li>'editable', 'disableDeletion', and 'allowUiUpdates': allow edits to dashboards via the UI, but disable deletion and automating saving of UI changes; forces the dashboard updates to only happen via code, but allows UI use for edits; code changes to be implemented via JSON export from the UI.</li>
<li>'options.path': where to find the dashboard JSON files; specifically where the files from 'configs/grafana/dashboards-json/' are synced with the container after the bind mount config</li>
<li>'options.foldersFromFilesStructure': allows for folders within the core '../dashboards/'</li>
</ul>
<p>For our this system I wanted to nest dashboards under their datasource, eg. '../dashboards/prometheus/cAdvisor.json'.</p>
<p>In general it provides a template for organizing the dashboards using nested folders, bypassing the need for further .yml rules when expansion is needed.</p>
<h2>Dashboards, JSON</h2>
<p>The dashboards themselves, the rows, the panels, the ranges, the visualizations, the queries are all configured via JSON.</p>
<p>You can use local files or link to a public dashboard.</p>
<p>Here, I chose to fork a public dashboard for Prometheus cAdvisor metrics and tweak it, <a href="https://grafana.com/grafana/dashboards/19908-docker-container-monitoring-with-prometheus-and-cadvisor/">Grafana Labs, Dashboards, cAdvisor Docker Insights</a>.</p>
<p>The tweaks are mostly on the queries themselves, with the largest set of changes to the 'Container Restarts' panel query and visualization. I wrote in detail about the queries in this previous post, <a href="https://alanionita.github.io/blog/2026-03-31-prometheus-mimir/">Alan Ionita, Prometheus Long-term Storage</a>.</p>
<h1>Conclusion</h1>
<p>The replacement of Grafana Promtail with Grafana Alloy allowed us to simplify the stack by removing 3 containers: Grafana Promtail, Telegraf (InfluxData), InfluxDB. Overall CPU and RAM usage improvements were observed as a result.</p>
<p>Core data storage for metrics is now done via Prometheus instead of InfluxDB.</p>
<p>Grafana Mimir was added in order to provide long-term support for monitoring data at 60day intervals, supporting a 30day auditing goal.</p>
<p>InfluxDB could have been used for long-term Prometheus storage, but visualizations were easier to implement with PromQL queries. The queries were tricky to implement in InfluxDB and produced mismatched trends and summaries.</p>
<p>Grafana is pre-configured with an admin user and password, the 3 data sources (Loki, Prometheus, Mimir), and 2 dashboards (Prometheus, Mimir). Dashboards are forked from a public dashboard. Showcasing an example of infrastructure-as-code.</p>
<p>The system rules are now contained in Alloy, making the rest of the service configs smaller and reducing hidden dependencies. For example, Prometheus, Mimir, Loki all contain minimal variations on default configs.</p>
<p>I hope you enjoyed this explanation, feel free to see the repo for the code and further documentation, <a href="https://github.com/alanionita/observability-stack">Alan Ionita, Repo, Observability Stack</a>.</p>
<h1>Open questions</h1>
<p>List of unanswered questions:</p>
<ul>
<li>What about Traces, the final ingredient to an Observability stack?</li>
<li>Where are the alerting examples? What is the best way to configure them?</li>
<li>We are observing the containers, what about collecting metrics about the host?</li>
<li>Example is tested in isolation, what happens if we have multiple stacks of containers on the same host?</li>
</ul>
<p>Feel free to use these as the basis for forks, updates, or derivative projects.</p>

                ]]>
            </description>
        </item><item>
            <title>InfluxDB, infrastructure-as-code</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2026-04-02-influxdb-iac</link>
            <pubDate>Thu, 02 Apr 2026 00:00:00 +0100</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>InfluxDB, infrastructure-as-code</h1>
<p>There is a wealth of resources online covering the topic of setting up InfluxDB as a Docker Compose micro-service.</p>
<p>Few of those discuss how to provision the initial setup as infrastructure-as-code, with built it authentication, local volumes (for debugging), and health checks.</p>
<pre><code class="language-yml">  influxdb:
    container_name: influxdb
    image: influxdb:2.8.0   # Required: pined to prevent config drift v2.8
    restart: unless-stopped
    ports:
      - 8086:8086
    networks:
      - grafana-monitoring
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: ${INFLUXDB_ADMIN_USER}
      DOCKER_INFLUXDB_INIT_PASSWORD: ${INFLUXDB_ADMIN_PASS}
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: ${INFLUXDB_ADMIN_TOKEN}
      # DOCKER_INFLUXDB_INIT_AUTH_ENABLED: true   # Required: fix auth auto-enforce
    volumes:
      - ./data/influxdb:/var/lib/influxdb2
    healthcheck:
      test: ["CMD", "influx", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
</code></pre>
<h1>Breakdown</h1>
<p>The Docker image is pinned to v2.8. In April 2026, the 'influxdb:latest' image will switch over to v3, and that comes with some significant changes. Here we make sure to stay on v2.8.</p>
<p>On the environment we use '.env' file which Docker Compose will consume to load the variables into your container.</p>
<p>This allows for a complex configuration without leaking any sensitive information with the infrastructure code.</p>
<p>Docker and InfluxDB recommend the use of Docker secrets, but secrets and environment variables are equally bad for security in lieu of encryption at rest or in transit. <a href="https://docs.influxdata.com/influxdb/v2/install/use-docker-compose/">InfluxDB, Install with Docker Compose</a>.</p>
<p>There is one situation where secrets are better: build step that require credentials. Without using secrets it's very easy to read the 'docker history IMAGE_NAME' and read the secrets. Especially important if you plan to deploy an image to DockerHub</p>
<p>In the face of two systems with equal consequences and outside of the edge case, I prefer to stick to the more popular system or environment variables.</p>
<p>When using environment variables it's good to add an '.env-example' file for documenting the required variables.</p>
<pre><code>INFLUXDB_ADMIN_USER=
INFLUXDB_ADMIN_PASS=
INFLUXDB_ADMIN_TOKEN=
</code></pre>
<p>Also remember to add '.env' and '.env.*' files to '.gitignore' and '.dockerignore'.</p>
<h1>InfluxDB, Configuration</h1>
<p>Most walkthroughs show how easy it is to spin up an InfluxDB container and manually create the user and token, usually in the context of a stack with Grafana and InfluxDB. In these, you have to first run the stack to configure InfluxDB authentication, then re-run the stack to create a 'Datasource' entry for InfluxDB in Grafana.</p>
<p>You can use 'DOCKER_INFLUXDB_INIT_MODE' set to 'setup' alongside 'DOCKER_INFLUXDB_INIT_USERNAME', and 'DOCKER_INFLUXDB_INIT_PASSWORD' to configure the InfluxDB user via iac.</p>
<p>The 'INFLUXDB_ADMIN_TOKEN' configures a token from an existing value.</p>
<p>With this setup you can now configure Grafana Datasource or Telegraf pipelines to talk to InfluxDB with authentication, and work from the first 'docker compose up'.</p>
<p>The bind mounts seen here are for development-only, and should be migrated to named volumes in production. During development I like the convenience of see and interrogate the data produced by a stack within the same folder as the infrastructure code.</p>
<p>A health check is configured here because InfluxDB is usually a required service for downstream use, so it's nice to start those downstream services once all the dependencies are healthy.</p>
<h1>InfluxDB, Debugging</h1>
<p>In some cases the initial authentication flow doesn't execute and it can be manually triggered using the 'DOCKER_INFLUXDB_INIT_AUTH_ENABLED' flag.</p>
<p>This issue will manifest by getting an authentication error when trying to login on InfluxDB UI with the provisioned user and pass. If everything seems correct on the config and data entry, try using the above flag to force the auth initialization flow.</p>

                ]]>
            </description>
        </item><item>
            <title>Prometheus + Mimir</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2026-03-31-prometheus-mimir</link>
            <pubDate>Tue, 31 Mar 2026 00:00:00 +0100</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Prometheus Long-term Storage</h1>
<p>In the last post I discussed what it's like to use InfluxDB as a long-term storage option for Prometheus, <a href="https://alanionita.github.io/blog/2026-03-09-prometheus-influxdb/">Prometheus Long-term Storage, this same blog</a>.</p>
<p>Spoiler: I didn't think it was worth it.</p>
<p>To use InfluxDB you had to implement your own versions of PromQL functions and, of course, they produced different results. The CPU Usage panel was as an example of such query, where a minor time range variance (acceptable at that scale), caused big long-term deviations in totals.</p>
<p>Trends were also impacted by this variance.</p>
<p>Then there was the labor, you have to craft these queries.</p>
<p>Then there were the theoretical issues to do with query performance. At the time I bypassed this talking point because I didn't have benchmarks.</p>
<p>I still don't have any new benchmarks on this, but I'm using the observed rates for the InfluxDB and Telegraf. I've recently tried to produce a 7days view using the InfluxDB data and the spikes in resource use are there. I also hit a memory issue in Grafana, the InfluxDB panel needs to render too many points and as such the query is blocked or timed out.</p>
<p>Sure, we can fix it with down-sampling, but will we need to do this for every visualization? We can also use a Prometheus rule to compute and record new down-sampled metrics on the fly.</p>
<p>We don't have these issues with Prometheus visualizations, which are presumably downsampled by the included functions.</p>
<h1>Mimir</h1>
<p>Grafana Mimir is designed to be long-term storage option for Prometheus. It's another open source solution, and features integrations with cloud providers.</p>
<p>In terms of performance, as observed coarsely on my system, it uses up 4x more CPU than Telegraf and 10x less CPU than InfluxDB.</p>
<p>It also supports PromQL out of the box, making the Grafana setup super easy.</p>
<h2>Setup</h2>
<p>Starting at the metric collector, aka Grafana Alloy.</p>
<pre><code>prometheus.remote_write "mimir" {
  endpoint {
    url = "http://mimir:9009/api/v1/push"
  }
}

...

prometheus.scrape "scraper" {
  targets    = discovery.relabel.cadvisor_rename.output
  forward_to = [ prometheus.remote_write.main.receiver, prometheus.remote_write.telegraf.receiver, prometheus.remote_write.mimir.receiver ]

  ...
}

...
</code></pre>
<p>Very simple change to introduce Mimir as another target for the Prometheus scraper.</p>
<p>The Mimir config itself.</p>
<p>[mimir-config.yml]</p>
<pre><code class="language-yml">multitenancy_enabled: false

blocks_storage:
  backend: filesystem
  bucket_store:
    sync_dir: /data/tsdb-sync 
  filesystem:
    dir: /data/blocks
  tsdb:
    dir: /data/tsdb

compactor:
  data_dir: /data/compactor
  sharding_ring:
    kvstore:
      store: memberlist

limits:
  # Delete from storage metrics data older than 2 months.
  compactor_blocks_retention_period: 60d
  # Process metric with timestamps of a wider time delay
  out_of_order_time_window: 5m

distributor:
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: memberlist

ingester:
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: memberlist
    replication_factor: 1

ruler_storage:
  backend: filesystem
  filesystem:
    dir: /data/rules

server:
  http_listen_port: 9009
  grpc_listen_port: 9080
  log_level: error

store_gateway:
  sharding_ring:
    replication_factor: 1
</code></pre>
<p>'block_storage', 'compactor', 'ruler_storage' the paths were updated to a standardised path 'data/*'.</p>
<p>Mimir uses a distributed network to allow nodes to gossip, but here we specified a single node via the 'store_gateway.sharding_ring.replication_factor', and 'multitenancy_enabled'.</p>
<p>The 'ingested' and 'distributer' still required this network config, and they along with the 'compactor' use a key store for encrypted comms.</p>
<p>'block_storage.backend' set to 'filesystem', where you can also configure Grafana Cloud, AWS S3, or GCP storage.</p>
<p>The 'limits.*' set the retention to 60days, adjust the 'out of time' window in case processing of samples is delayed past the default.</p>
<p>We originally wanted to be able to report from long-term storage at 30d intervals so 60d grants us a bit of buffer.</p>
<p>Crucial is the 'http_listen_port', which is where Alloy posts the metrics.</p>
<p>Next up is the container setup itself.</p>
<p>[docker-compose.yml]</p>
<pre><code class="language-yml">  mimir:
    image: grafana/mimir-alpine
    container_name: mimir
    ports:
      - "9009:9009"
    command:
      - "-config.file=/etc/mimir/mimir.yaml"
    volumes:
      - ./configs/mimir-config.yml:/etc/mimir/mimir.yaml:ro
      - ./data/mimir:/data:rw
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9009/ready"]
      interval: 10s
      timeout: 30s
      retries: 5
    networks:
      - grafana-monitoring

   grafana:
    container_name: grafana
    depends_on: 
      mimir:
        condition: service_healthy
</code></pre>
<p>Simple config exposing the post, bind mounting the config and the output data, and adding a healthcheck via the '/ready' api. The Grafana container depends on this health check.</p>
<p>And for the Grafana config I'll share the data source</p>
<p>[grafana/provisioning/datasources/mimir.yml]</p>
<pre><code class="language-yml">apiVersion: 1

datasources:
  - name: Mimir
    type: prometheus
    orgId: 1
    url: http://mimir:9009/prometheus
    jsonData:
      httpMethod: POST
      prometheusType: Mimir
      cacheLevel: 'High'
      timeInterval: 15s # Prometheus scrape interval
      incrementalQueryOverlapWindow: 10m
</code></pre>
<p>Very similar to the recommended Prometheus config. I've not seen an official reference config, but this does the trick.</p>
<p>The core part is the 'url' suffix of '/prometheus', which is just a proxy for '/'. The Mimir API contains other paths and to avoid any endpoint clashes they with other non-Prometheus endpoints they by default prefix the query API, the main show, with the default '/prometheus'. You can set your own suffix with a CLI flag.</p>
<h2>Results</h2>
<p>Grafana Mimir is a lot more comfortable to use as a long-term Prometheus store, because of the native PromQL support.</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-31-prometheus-mimir/mimir-dashboard.webp" alt="" title="dashboard with Mimir data"></p>
<p>The InfluxDB dashboard for reference.</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-31-prometheus-mimir/influx-dashboard.webp" alt="" title="dashboard with InfluxDB data"></p>
<p>You can see here the differences in totals across the two solutions at a wider time frame of 6h. The InfluxDB query is wildly off.</p>
<p>Also clear to see the trend differences, with more noticeable spikes from the Mimir query.</p>
<p>And the native sampling produces as better heatmap resolution.</p>
<h1>Conclusion</h1>
<p>There's a lot to love about Mimir, and for the home lab I'll continue to use it. The PromQL support just fixes everything, and I don't understand why InfluxDB doesn't add it also.</p>
<p>Mimir suggests that the 'file storage' option is experimental and for development use only. The docs also point out how non-POSIX filesystems are not supported aka AWS Elastic File Store (EFS). Curious if this will extend to AWS wrapper services.</p>
<p>In production I'd configure the AWS S3 option or Grafana Cloud, but that will be another post for another day.</p>
<p>Hope you learned something new about Prometheus long-term storage, from reading this.</p>
<h2>Next up</h2>
<p>Planning to talk about a modern observability setup in 2026.</p>
<p>I've seen a few videos, posts, and resources where the authors skip over provisioned authentication, bind mounting, and in general favour UI config vs infrastructure-as-code.</p>
<p>Should be able to show and tell how to provision an entire modern monitoring stack with code, along with all the mandatory "dos and donts" ramblings and links to docs.</p>
<p>If you're interested, subscribe to the RSS feed.</p>

                ]]>
            </description>
        </item><item>
            <title>Prometheus + InfluxDB</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2026-03-09-prometheus-influxdb</link>
            <pubDate>Mon, 09 Mar 2026 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Prometheus Long-term Storage</h1>
<p>Can you use InfluxDB for Prometheus long-term storage?</p>
<p>Lets assume you're using Prometheus to monitor those Docker containers on your homelab, a pretty common use case these days.</p>
<p>Prometheus by default gives you a data retention config of 15days. This can be extended, but it is recommended to use another solution for long-term storage.</p>
<p>For example, lets say we want to implement for our homelab an compliant backup retention policy of 30 days. The use of Prometheus is both not recommended and would require a larger storage hardware.</p>
<p>This example situation can be further worsened depending on the total and throughput of data going to be observed. As a general rule of thumb, the industry expects an observability system to be 50x the size of your systems data profile.</p>
<p>Case study - Signal messages:</p>
<ul>
<li>Take a standard Signal 240 character message, some emojis since they cost more bytes, add the excess encryption overhead, and we arrive at an estimated 1.5KB per message</li>
<li>Assuming 70million active users per month, and assuming each send 20 messages per day, would leave us with 1.4 billion messages / day</li>
<li>The data volume per the 2h window would be 166.7 GB</li>
</ul>
<p>And that is without considering that Prometheus splits up the chunks into 512MB chunks, or in our case study, ~300 chunks.</p>
<p>The throughput storage impact is also a scaling issue since the more "real-time" a system is, the more storage chunks / blocks created and stored by Prometheus.</p>
<p>Case study - Grafana Docker stack:</p>
<ul>
<li>Take a standard Grafana stack of Docker containers, eg. 6 containers</li>
<li>Assume 15sec scraping rule for Prometheus, thats 4 data points / minute</li>
<li>Assume each container collects ~40 metrics, 6 containers leave us with 240 metrics</li>
<li>Assume each data point is sized as above, at 1.5KB, gives us 1.4 MB / minute</li>
<li>Or 84 MB / hour, 168 MB / 2h, ~2 GB / day, ~60 GB / month</li>
</ul>
<p>Or 120 Prometheus storage chunks. It quickly mounts up or we have a maths error somewhere!</p>
<p>Note that for the rest of the article I'll never again return to the topic of storage benchmarking for something other than Prometheus. I really should measure it and compare it, and I might in another post. If you fancy reading that, make sure you are following the RSS for future updates.</p>
<h1>Storage options</h1>
<p>Grafana Mimir, Thanos, Victoria Metrics and more. You can read a nice comparisson in this handy resource - <a href="https://kubernetesinmyhead.net/observability/prometheus/prometheus-long-term-storage/">Kubernetes in my head - Prometheus Long-term Storage</a>.</p>
<p>The issue is not finding the solutions, but picking one.</p>
<p>If you followed any homelab video setups from past few years, you will know the Grafana stack. A good stack for home observability. Most of these older videos use and recommend InfluxDB, usually in the same stacks as Prometheus (used for metrics and alerts).</p>
<p>InfluxDB as a long-term storage for Prometheus becomes more desirable in this context. If you choose it you're not adding a new service and at face value the two options look viable.</p>
<pre><code class="language-md">| InfluxDB                           | Prometheus                |
| -----------------------------------| ------------------------- |
| Time-series db                     | Time-series metrics       |
| Designed for analytics             | Designed for metrics      | 
| Time-series focused query language | Functional query language |
| Broad integrations                 | Broad integration         |
| Compression and downsampling       |                           |
</code></pre>
<p>InfluxDB docs show native api's for Prometheus, tooling for it via Telegraf, and guides on the ingestion pipeline.</p>
<p>There's even a CNCF talk from InfluxDB's Paul Dix, discussing native support for Prometheus data ingestion - <a href="https://www.youtube.com/watch?v=6UjVX-RTFmo"> Integrating Prometheus and InfluxDB - Paul Dix, InfluxData (Intermediate Skill Level)</a></p>
<h1>Data Ingestion, Telegraf</h1>
<p>Grafana Telegraf is the default runner recommended for getting data not meant for InfluxDB, into InfluxDB. It's main job is to  handle the data formatting.</p>
<p>A common flow is to push data to Prometheus, Prometheus.remote_write to Telegraf, and Telegraf push to InfluxDB.</p>
<p>[telegraf.conf]</p>
<pre><code>[[inputs.http_listener_v2]]
  service_address = ":1234" # Telegraf port
  paths = ["/receive"]
  methods = ["POST"]
  data_format = "prometheusremotewrite"
  read_timeout = "10s"
  write_timeout = "10s"
  max_body_size = "64MB"
  http_headers = {"Connection" = "keep-alive"}
</code></pre>
<p>[prometheus.yml]</p>
<pre><code class="language-yml">remote_write:
  - url: "http://telegraf:1234/receive"
</code></pre>
<p>Results:</p>
<ul>
<li>Data gets to InfluxDB fine.</li>
<li>Not benchmarked, but may feature some lag due to the extra steps: pushing from Prometheus, writing to disk, pushing to Telegraf, and pushing to InfluxDB.</li>
<li>Telegraf is a queue / buffer, misconfiguration of data flow will create data mismatches; eg. you flush your buffer too often or not often enough and Telegraf can't write as quickly as you post data; misconfigurations will produce incompatible rates of data across the data sets which will break dashboards, and ultimately an untrustworthy auditing data lake eg. where Prometheus has 4 measurements / second, InfluxDB might feature less result in rate() calculations diverging.</li>
<li>Introduces the Telegraf service, and adds extra system requirements; on avgerage (for my system) Telegraf eats up 2-3x the amount of memory that Prometheus does.</li>
</ul>
<h1>Data Ingestion, Alloy</h1>
<p>Grafana Alloy is a runner / collector, flexible and powerful, and extendable. Designed to replace Promtail, which used to be the defacto collector of hardware or container metrics.</p>
<p>In 2026, Alloy is the recommended way to collect Docker container metrics, via a built-in cAdvisor component and pushing the metrics to Prometheus.</p>
<p>[config.alloy]</p>
<pre><code>livedebugging {
  enabled = true // TODO: remove when not in development
}

// Targets
// > Groups all of the *write components together

prometheus.remote_write "main" {
  endpoint {
    url = "http://prometheus:9090/api/v1/write"
  }
}

prometheus.remote_write "telegraf" {
  endpoint {
    url = "http://telegraf:9273/metrics"
  }
}

// Metrics

prometheus.exporter.cadvisor "main" {
  docker_only = true
  storage_duration = "10m"
}

discovery.relabel "cadvisor_rename" {
    targets = prometheus.exporter.cadvisor.main.targets

    rule {
        target_label = "job"
        replacement  = "integrations/docker"
    }

    rule {
        target_label = "instance"
        replacement  = constants.hostname
    }
}

prometheus.scrape "scraper" {
  targets    = discovery.relabel.cadvisor_rename.output
  forward_to = [ prometheus.remote_write.main.receiver, prometheus.remote_write.telegraf.receiver ]


  scrape_interval = "15s"
}

</code></pre>
<p>Config uses Alloy to define a Prometheus cAdvisor  scraper, relabel some of the defaults from cAdvisor (minor relabelling, kept for reference), and push the data to two receivers: Prometheus, Telegraf. Telegraf being our data queue to InfluxDB.</p>
<p>Important to use "docker_only" on the cAdvisor definition, otherwise you won't be able to access "machine_cpu" metric, crucial for one of the dashboards.</p>
<p>Also "docker_host = "unix:///var/run/docker.sock"" only, will also stop "machine_cpu" from being scraped.</p>
<p>Speaking of scraping, the the default scrape "storage_duration" is 2m. The increase is required for container restarts. We don't want to flush the data from the previous container too early. Should be increased to 1h minimum, in production.</p>
<p>Results:</p>
<ul>
<li>Achieves similar outputs to the Telegraf example.</li>
<li>Benefit: keeps Prometheus and Telegraf configs clean.</li>
<li>Benefit: more robust data integrity across the 2 storage options; data is more likely to arrive in sync.</li>
<li>Benefit: theoretically better latency, but not benchmarked.</li>
<li>Benefit: centralises configuration around Alloy, as opposed to multiple changes across other services; less service chaining.</li>
</ul>
<h1>Data Ingestion, Alloy + InfluxDB</h1>
<p>This relies on InfluxDB documentation that reports "native" api support for Prometheus via "/api/v*/write".</p>
<p>We continue to use Alloy as the main runner, but we remove the need for Telegraf. We instead define an Influx</p>
<p>[config.alloy]</p>
<pre><code>...

prometheus.remote_write "influx" {
  endpoint {
    url = "http://influxdb:8086/api/v2/write?bucket=mybucket&#x26;org=myorganisation"

    bearer_token = "BearerTokenGoesHere=="
  }
}

...

prometheus.scrape "scraper" {
  targets    = discovery.relabel.cadvisor_rename.output
  forward_to = [ prometheus.remote_write.main.receiver, prometheus.remote_write.influx.receiver ]
  scrape_interval = "15s"
}

</code></pre>
<p>This uses the <code>/api/v2/write</code> api from InfluxDB which should have native support for Prometheus.</p>
<p>Results:</p>
<ul>
<li>We are using InfluxDB v2.8, not the latest version v3.</li>
<li>Critical error: InfluxDB throws ingestion error due to the data schema difference; InfluxDB documentation does mention that Telegraf handles data differently, but I didn't expect a full stop data compatibility error.</li>
<li>Fully blocked on this path.</li>
</ul>
<h1>Visualization, Grafana</h1>
<p>Using Grafana to visualize our observability data via a generic dashboard for cAdvisor from the open catalog.</p>
<p>Why cAdvisor?</p>
<p>If you didn't notice from the Alloy configs, we use the <code>prometheus.exporter.cadvisor</code> which uses <a href="https://github.com/google/cadvisor/tree/master">cAdvisor (repo)</a> under the hood to measure usage and performance of Docker containers. A standard library for this process, but unclear how the abstraction affects performance or configures cAdvisor.</p>
<p>The dashboard used as a base measures CPU, memory, I/O, network traffic, container restarts, <a href="https://grafana.com/grafana/dashboards/19908-docker-container-monitoring-with-prometheus-and-cadvisor/">Grafana dashboards / cAdvisor Docker Insights</a>. We will however edit this dashboard queries later due to some fundamental data representation bugs.</p>
<h1>Dashboards, Prometheus</h1>
<p>With the above dashboard and Prometheus as a data source, we have no issues and the system is performing nominally.</p>
<p>The dashboard is designed with Prometheus in mind so all the panels use PromQL queries.</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/promql-dashboard.webp" alt="" title="promql dashboard"></p>
<h1>Dashboards, InfluxDB</h1>
<p>Prometheus will contain our 12-24h data, and be used for alerting.</p>
<p>InfluxDB will contain all our historical data, and we are using the older v2.8 since v3 has significant api and license changes.</p>
<p>To make the same dashboard work with InfluxDB, we need to make the InfluxDB data queries mimick PromQL queries.</p>
<p>Our ultimate test is whether we can achieve like for like dashboard parity.</p>
<p>First blocker, InfluxDB doesn't use PromQL. It uses InfluxQL or Flux as query languages, where:</p>
<ul>
<li>Flux is a functional style time-series query language</li>
<li>InfluxQL is a SQL-like query language</li>
</ul>
<p>Given PromQL is also a functional query language, we should use Flux to recreate the dashboard queries.</p>
<p>More details in the full parity chart between the two languages, <a href="https://docs.influxdata.com/influxdb/v2/reference/syntax/flux/flux-vs-influxql/#influxql-and-flux-parity">InfluxQL and Flux parity chart</a>.</p>
<p>Note, this is only true for InfluxDB v2.8. InfluxDB v3 adopted SQL in full, and renamed Flux to InfluxQL.
See the InfluxDB product chart for more info see this docs page <a href="https://docs.influxdata.com/platform/identify-version/#understanding-influxdb-products">Understanding InfluxDB products</a></p>
<pre><code>InfluxQL -> SQL
Flux     -> InfluxQL
</code></pre>
<h1>Panel, Running Containers, Prometheus</h1>
<pre><code>Dashboard: cAdvisor 
Panel: Running container
Data source: Prometheus
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/running-containers.png" alt="" title="running containers panel"></p>
<p>PromQL query.</p>
<pre><code>count(count(container_last_seen) by (name))
</code></pre>
<p>A better, semantic query variation.</p>
<pre><code>sum by (name) (count(container_last_seen))
</code></pre>
<p>The metric "container_last_seen" is a Gauge as defined by cAdvisor, in their docs <a href="https://github.com/google/cadvisor/blob/master/docs/storage/prometheus.md">Monitoring cAdvisor with Prometheus</a>.</p>
<p>First we "count()" all the "container_last_seen` returning the total for all containers. For example, if we have three stacks running, and each contained a grafana service, we would receive the count value of 3</p>
<p>Then "sum by (name)()" or recount by name, in order to get the distinct number of services. Returning to the above example, this sum will return 1.</p>
<p>With "sum by" it's easier to reason what we expect from the output: a sum of container_last_seen counts.</p>
<h1>InfluxDB, working with data</h1>
<p>InfluxDB stores Prometheus metrics with the combination of _measurement and _field. The measurement will stay constant across all the queries.</p>
<p>From the docs, <a href="https://docs.influxdata.com/influxdb/v1/concepts/key_concepts/#measurement">InfluxDB, key concepts, measurement</a>:</p>
<blockquote>
<p>The measurement acts as a container for tags, fields, and the time column, and the measurement name is the description of the data that are stored in the associated fields. Measurement names are strings, and, for any SQL users out there, a measurement is conceptually similar to a table. The only measurement in the sample data is census. The name census tells us that the field values record the number of butterflies and honeybees - not their size, direction, or some sort of happiness index.</p>
</blockquote>
<p>The docs also raise some interesting questions:</p>
<ul>
<li>"v1.5 and earlier, all Prometheus data goes into a single measurement named _ and the Prometheus measurement name is stored in the <strong>name</strong> label"; Odd statement, since in the data we see a common <code>_measurement</code> value for all "prometheus_remote_write" metrics; <code>_field</code> and other _* values are used; the measurement name is actually stored under <code>_field</code>, and we also have a name property which in our case is the name of the observed container.</li>
<li>"In InfluxDB v1.6 or later, every Prometheus measurement gets its own InfluxDB measurement."; We are using InfluxDB v2.8 and  this is not what we see; every measurement is stored under "prometheus_remote_write" and each measurement gets it's own field</li>
<li>"This format is different than the format used by the Telegraf Prometheus input plugin." - Since we are using Telegraf, could it be why we see inconsystencies?; no follow-on links or comments are present in the docs, so a bit more sign-posting would be ace.</li>
</ul>
<p>Documentation source for the above quotes, <a href="https://docs.influxdata.com/influxdb/v1/supported_protocols/prometheus/#example-parse-prometheus-to-influxdb">InfluxDB, example parse Prometheus to InfluxDB</a>.</p>
<p>Some answers to those questions:</p>
<ul>
<li>The "_measurement" and "_field" combo usage is referenced by docs to the InfluxDB Prometheus API v2. Where v1 format actually matches the above statements. A reference of this can be found, <a href="https://docs.influxdata.com/flux/v0/prometheus/metric-types/histogram/?t=Metric+version+1">Calculate quantile values from Prometheus histograms</a>. This suggests that v1 queries only require 1 filter by "_measurement" where it matches "container_last_seen" from cAdvisor. Whilst v2 requires 2 filters, one "_measurement" and one "_field". First one matching "prometheus" or "prometheus_remote_write", second one matching cAdvisor "container_last_seen"</li>
<li>The two fromats are also discussed in <a href="https://docs.influxdata.com/influxdb/v2/reference/prometheus-metrics/">Prometheus metric parsing formats</a>. Here the explanation is more vague because both formats feature the same "_measurement" and "_field" combo, expect where it comes to usage, v1 using "_measurement" for the Prometheus metric name, and v2 using "_field" for the Prometheus metric name.</li>
</ul>
<p>The system here uses v2 measurement api, so "_field" gets us to the point of selecting the cAdvisor metric we're targeting.</p>
<blockquote>
<p>Docs: "The sample recording from Prometheus is stored in value column"</p>
</blockquote>
<p>In our case the sample recording is actually stored in the "_value" column.</p>
<p>Lets take the a simplified query and build up from there.</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_last_seen")
</code></pre>
<p>"range(start: v.timeRangeStart, stop: v.timeRangeStop)" returns the range defined by the Grafana UI, as opposed to a hard-coded value eg. '5m', '6h'</p>
<p>Let's review the query output as JSON, since it's easier to read.</p>
<pre><code class="language-json">{
  "table": 0,
  "_start": "2026-03-12T12:14:13.928991563Z",
  "_stop": "2026-03-12T...",
  "_time": "2026-03-12T...",
  "_value": 1773317667,
  "_field": "container_last_seen",
  "_measurement": "prometheus_remote_write",
  "container_label_com_docker_compose_config_hash": "271ce3",
  "container_label_com_docker_compose_container_number": "1",
  "container_label_com_docker_compose_image": "sha256:f820",
  "container_label_com_docker_compose_oneoff": "False",
  "container_label_com_docker_compose_project": "base-monitoring-stack",
  "container_label_com_docker_compose_project_working_dir": "~/base-monitoring-stack",
  "container_label_com_docker_compose_replace": "e05564",
  "container_label_com_docker_compose_service": "alloy",
  "container_label_com_docker_compose_version": "2.35.1",
  "container_label_org_opencontainers_image_ref_name": "ubuntu",
  "container_label_org_opencontainers_image_source": "https://github.com/grafana/alloy",
  "container_label_org_opencontainers_image_version": "24.04",
  "host": "e35f",
  "id": "/system.slice/docker-76c4d.scope",
  "image": "grafana/alloy:latest",
  "instance_job": "75c9e",
  "name": "alloy"
}
</code></pre>
<p>All the <code>container_label_*</code> entries are from Docker itself, and not related to the architecture described so far, not cAdvisor, not Alloy, not Prometheus, not InfluxDB. Let's filter these out, for now.</p>
<p>Sidenote: the size of the above entry is 1KB, close to the estimated values at the top of the article.</p>
<pre><code class="language-json">{
  "table": 0,
  "_start": "2026-03-12T12:14:13.928991563Z",
  "_stop": "2026-03-12T...",
  "_time": "2026-03-12T...",
  "_value": 1773317667,
  "_field": "container_last_seen",
  "_measurement": "prometheus_remote_write",
  "host": "e35f",
  "id": "/system.slice/docker-76c4d.scope",
  "image": "grafana/alloy:latest",
  "instance_job": "75c9e",
  "name": "alloy"
}
</code></pre>
<p>This is now the expected schema for a <code>container_last_seen</code> metric entry.</p>
<blockquote>
<p>How do I develop a feel for InfluxDB data and queries?</p>
</blockquote>
<p>The best way to query and develop an feel for the data in InfluxDB is via the InfluxDB query UI.</p>
<p>Navigate to <code>InfluxDB UI / Data Explorer</code> and:</p>
<ul>
<li>From top left dropdown toggle the visualization to 'Simple Table' or 'Table' view.</li>
<li>From right side of the page toggle the "Script writer"</li>
</ul>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-script-writer.webp" alt="" title="influxdb data explorer script writer"></p>
<p>You might be tempted to use <code>Grafana UI / Connections / Data sources / InfluxDB / your_query</code>, but the table UI here is lacking. You'll miss key debug information that is present in InfluxDB Query UI.</p>
<p>The Influx / Data Explorer view shows us the types stored in each column, eg. 'Group string'. And these types will impact the Flux queries. It also shows you the tables produced by a query, or "series".</p>
<h2>Metric schema</h2>
<p>What if I wanted to see a single Prometheus metrics schema in InfluxDB?</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop:v.timeRangeStop)
  |> keys()
  |> keep(columns: ["_value"])
  |> group()
  |> distinct()
</code></pre>
<p>Show all the item attributes available in InfluxDB. All the "container_label_*" fields, were removed as mentioned above.</p>
<p>The default schema for all metrics entries:</p>
<pre><code>_start
_stop
_field
_measurement
branch
goarch
goos
goversion
host
instance
job
revision
tags
version
kernelVersion
osVersion
id
major
minor
operation
device
image
name
cpu
failure_type
scope
interface
state
boot_id
machine_id
system_uuid
mode
</code></pre>
<h2>Metric types</h2>
<p>What if I wanted to see all the metric types collected by cAdvisor?</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop) 
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> group(columns: ["_field"])
  |> distinct(column: "_field")
</code></pre>
<p>Produces a list comparable to the ones from cAdvisor, <a href="https://github.com/google/cadvisor/blob/master/docs/storage/prometheus.md">Monitoring cAdvisor with Prometheus</a></p>
<pre><code>cadvisor_build_info
cadvisor_version_info
container_blkio_device_usage_total
container_cpu_load_average_10s
container_cpu_system_seconds_total
container_cpu_usage_seconds_total
container_cpu_user_seconds_total
container_fs_inodes_free
container_fs_inodes_total
container_fs_io_current
container_fs_io_time_seconds_total
container_fs_io_time_weighted_seconds_total
container_fs_limit_bytes
container_fs_read_seconds_total
container_fs_reads_bytes_total
container_fs_reads_merged_total
container_fs_reads_total
container_fs_sector_reads_total
container_fs_sector_writes_total
container_fs_usage_bytes
container_fs_write_seconds_total
container_fs_writes_bytes_total
container_fs_writes_merged_total
container_fs_writes_total
container_last_seen
container_memory_cache
container_memory_failcnt
container_memory_failures_total
container_memory_kernel_usage
container_memory_mapped_file
container_memory_max_usage_bytes
container_memory_rss
container_memory_swap
container_memory_usage_bytes
container_memory_working_set_bytes
container_network_receive_bytes_total
container_network_receive_errors_total
container_network_receive_packets_dropped_total
container_network_receive_packets_total
container_network_transmit_bytes_total
container_network_transmit_errors_total
container_network_transmit_packets_dropped_total
container_network_transmit_packets_total
container_oom_events_total
container_scrape_error
container_spec_cpu_period
container_spec_cpu_shares
container_spec_memory_limit_bytes
container_spec_memory_reservation_limit_bytes
container_spec_memory_swap_limit_bytes
container_start_time_seconds
container_tasks_state
machine_cpu_cores
machine_cpu_physical_cores
machine_cpu_sockets
machine_memory_bytes
machine_nvm_avg_power_budget_watts
machine_nvm_capacity
machine_scrape_error
machine_swap_bytes
scrape_duration_seconds
scrape_samples_post_metric_relabeling
scrape_samples_scraped
scrape_series_added
up
</code></pre>
<p>The values with "container_*" prefix represent cAdvisor metrics.</p>
<p>The values with "machine_*" represent Prometheus hardware metrics.</p>
<p>The values with "scrape_<em>" and "up" are from the Grafana Alloy, from the "prometheus.scrape" block. "up" is "1 if the instance is healthy and reachable, or 0 if the scrape failed.", and "scrape_</em>" represent Prometheus scraping metadata, <a href="https://grafana.com/docs/grafana-cloud/send-data/alloy/reference/components/prometheus/prometheus.scrape/">prometheus.scrape - Scraping Behaviour (Grafana Alloy docs)</a>.</p>
<p>All of these represent the "type" of each metric, which will share the above metric schema.</p>
<h1>Panel, Running Containers, InfluxDB</h1>
<pre><code>Dashboard: cAdvisor [historic]
Panel: Running container
Data source: InfluxDB
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/running-containers.webp" alt="" title="running containers panel"></p>
<p>The panel is powered by the following Flux query.</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_last_seen")
  |> group(columns:["name"])
  |> distinct(column: "name")
  |> keep(columns: ["_time", "_value"])
  |> count()
</code></pre>
<p>The beginning of the query is similar to our simple starter query, and it just narrows down the data to "container_last_seen".</p>
<p>"group()" is interesting, InfluxDB groups data by: taking all the _* columns without _time, _value, and attaching all the remaining columns.</p>
<p>For reference, the PromQL grouping done via "by (name)".</p>
<pre><code>count(count by (name) (container_last_seen))
</code></pre>
<p>In InfluxDB we have 2 options:</p>
<ul>
<li>We can remove all grouping via an empty call to "group()". This creates an aggregate base, and columns store raw values not groups, making them useful for sum totals.</li>
<li>Or we organise groups by name, akin to PromQL. Remember from above that "name" is one of the attributes on the metric type, so we will need that as a column. eg. "group(columns: ["name"])"</li>
</ul>
<p>For this example you should switch the "InfluxDB / Data Explorer" view to "Table", not "Simple table".</p>
<p>Here is a query without "group()"".</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_last_seen")
  |> distinct(column: "name")
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-no-group.png" alt="" title="influxdb-query-result-without-group"></p>
<p>"distinct()"", will make sure that we're counting services not instances of those services. Here not an issue, but important if you have multiple stacks running, with the same services.</p>
<p>The output, as seen above, is a list of tables matching the amount of containers, where each table contains 1 entry "{ _value, name }". Again this returns a collection of TABLES not values, and we need the values for aggregation.</p>
<p>"group()" will merge the data into one table, and set up for correct aggregation. One table, many metrics.</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_last_seen")
  |> group()
  |> distinct(column: "name")
  |> count()
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-with-group.png" alt="" title="influxdb-query-result-with-group"></p>
<p>Notice how we have have an extra blank entry?!</p>
<p>We need to filter out empty "name" samples. Unclear why the data is reporting entries with a blank "name".</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_last_seen" and r.name != "")
  |> group()
  |> distinct(column: "name")
  |> count()
</code></pre>
<p>"keep()" just reduces the data to just what we need, discarding the other columns. In the examples above we've only got "_value" which is actually the original "name" column,</p>
<p>And we need to do the same filtering on the Prometheus dashboard query, with "{name!=""}".</p>
<pre><code>sum by (name) (count(container_last_seen{name!=""}))
</code></pre>
<h1>Panel, Running Containers, Reflection</h1>
<pre><code>Dashboard: cAdvisor [historic]
Panel: Running container
Data source: InfluxDB
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/running-containers.webp" alt="" title="running containers panel"></p>
<p>InfluxDB Flux queries here are verbose. Most of it, caused by the double filtering of measurement and field.</p>
<p>PromQL is a lot nicer, and I would favor a long-term storage solution with native PromQL support for parity and max comfort.</p>
<p>Overall, this panel port from PromQL to InfluxDB data source is a "PASS".</p>
<h1>Panel, CPU Usage, Prometheus</h1>
<pre><code>Dashboard: cAdvisor
Panel: CPU Usage
Data source: Prometheus
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/promql-cpu-usage.webp" alt="" title="cpu usage panel"></p>
<p>The PromQL query:</p>
<pre><code>sum by (name) (rate(container_cpu_usage_seconds_total{name=~".+"}[1m])) * 100
</code></pre>
<p>Starting from the outer level, we are producing a "sum by (name) () " and multiplying by 100 for % rate.</p>
<p>"container_cpu_usage_seconds_total" is reported by cAdvisor as a Counter type of total CPU seconds used averaged across CPU cores.</p>
<p>The chart displays the usage of each service as a % of the total, so the syntax seems odd for now.</p>
<p>"rate()[1m]" will produce an averaged value across the time-range given [1m].</p>
<p>In the "rate()" the outliers are typically removed since Counters are considered generally monotonic, or constantly increasing or decreasing values, eg. when a service is offline you get an outlier. Those metrics are smoothed from the output of "rate()".</p>
<p>The rate here should stay at 1m in order to represent accurate per second values.</p>
<p>A sibling of "rate()" is "irate()", which is recommended for more volatile loads and can be used here to chart more interesting spikes of the CPU use. It should not be used for alerting because of increased noise from said spikes.</p>
<p>The "{name=~".+"}" is a label matcher, here used as a filter. It translates to "filter any name tables with 1 + characters". Here it's used to remove a "sum by (name)()" output entry, which would otherwise alter the figures.</p>
<p>Notice how we simply data the sum or the rates and multiply by 100, will this actually represent CPU usage as a percentage?</p>
<p>No.</p>
<p>In PromQL any "* 100" can be interpreted as a percent value. Here, the "container_cpu_usage_seconds" are actually nano seconds eg "alloy" using 0.0238s, ~2.38ns.</p>
<p>Taking these values and multiplying by 100 just turns these values into seconds, from 2.3ns to 2.3s. Which is not the measurement we got from the service.</p>
<p>Since chart expects percentages, it will report the value as 2.3%.</p>
<p>To get better reading, we need to solve the "usage / number CPUs" formula.</p>
<p>From documentation, "container_cpu_usage_seconds_total" represents sum usage across all CPU cores. We would need to divide the cores to get the change rate. The change rate being a more interesting rate to chart vs aggregate change across all cores.</p>
<pre><code>sum by (name)(
  rate(container_cpu_usage_seconds_total{name=~".+"}[1m])
) / on() group_left() machine_cpu_cores * 100
</code></pre>
<p>From cAdvisor metrics we use "machine_cpu_cores" to get the CPUs available. "on() group_left()" changes the right-side data to match the format on the left of the divisor. Without it the "machine_cpu_cores" returns a single table for all "name" columns vs the left query which returns 1 table per "name".</p>
<p>"on() group_left()" is a many-to-one matcher, or a LEFT JOIN in SQL. More info on these methods in the PromQL cheatsheet, https://promlabs.com/promql-cheat-sheet/</p>
<h1>Panel, CPU Usage, InfluxDB</h1>
<pre><code>Dashboard: cAdvisor [historic]
Panel: CPU Usage
Data source: InfluxDB 
</code></pre>
<p>Reviewing the steps from Prometheus PromQL we need to:</p>
<ul>
<li>Get the values for "container_cpu_usage_seconds_total"</li>
<li>Get the number of "machine_cpu_cores"</li>
<li>Calculate the division of the two as a percentage value</li>
</ul>
<p>For simplicity, we will use variables for formula part.</p>
<pre><code>cpuUsageRate = ...

totalCPUCores = ...

cpuUsageRate
  |> map(fn: (r) => ({
      _time: r._time,
      name: r.name,
      _value: (r._value / totalCPUCores) * 100.0
    }))
  |> yield(name: "cpu_usage_percentage")

</code></pre>
<p>The final output should be covering each entry in "cpuUsageRate" and divide the "_value" by the "totalCPUCores", and represent it as a percentage,</p>
<p>For "totalCPUCores" we use:</p>
<pre><code>totalCPUCoresRecord = from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "machine_cpu_cores")
  |> last()
  |> findRecord(fn: (key) => true, idx: 0)

totalCPUCores = totalCPUCoresRecord._value
</code></pre>
<p>After narrowing / filtering down to "machine_cpu_cores", we fetch the last value of "machine_cpu_cores" i.e. most accurate / recent value. "last()" returns a table and we get the first record in that table via "findRecord()", with idx 0.</p>
<p>"findRecord()" will return the record not the value, so we use another variable to save the "_value" data.</p>
<p>For "cpuUsageRate" query:</p>
<pre><code>cpuUsageRate = from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_cpu_usage_seconds_total")
  |> filter(fn: (r) => exists r.name)
  |> increase()
  |> aggregateWindow(every: 1m, fn: mean)
  |> derivative()
</code></pre>
<p>Continuing the pattern, we narrow the query to "container_cpu_usage_seconds_total" and we filter out any values with empty "name". Unclear why cAdvisor is reporting these value, or what they represent; they are however polluting the data and have to go.</p>
<p>Next is the crucial part, attempting to replicate the "rate()" behavior from PromQL.</p>
<p>"increase()" to normalise any counter resets.</p>
<p>"aggregateWindow(every: 1m, fn: mean)" produces a window averaging similar to "rate()"</p>
<p>"derivative()" helps us convert monotonic counter values to a rate, here defaulting to 1s.</p>
<p>This should translate to "1s average rate across a 1m window".</p>
<p>Final query, variation A:</p>
<pre><code>cpuUsageRate = from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_cpu_usage_seconds_total")
  |> filter(fn: (r) => exists r.name)
  |> increase()
  |> aggregateWindow(every: 1m, fn: mean)
  |> derivative()

totalCPUCoresRecord = from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "machine_cpu_cores")
  |> last()
  |> findRecord(fn: (key) => true, idx: 0)

totalCPUCores = totalCPUCoresRecord._value

cpuUsageRate
  |> map(fn: (r) => ({
      _time: r._time,
      name: r.name,
      _value: (r._value / totalCPUCores) * 100.0
    }))
  |> yield(name: "cpu_usage_percentage")
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-cpu-usage-a.webp" alt="" title="cpu usage panel (influxDB a)"></p>
<p>For reference, the PromQL panel for the same time range</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/promql-cpu-usage-a.webp" alt="" title="cpu usage panel (influxDB a)"></p>
<p>And alternate query, as referenced by <a href="https://docs.influxdata.com/flux/v0/prometheus/metric-types/counter/#calculate-the-average-rate-of-change-in-specified-time-windows">InfluxDB, Work with Prometheus Counters</a>.</p>
<p>Final query, variation B:</p>
<pre><code>import "experimental/aggregate"

cpuUsageRate = from(bucket: "telegraf")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_cpu_usage_seconds_total")
  |> filter(fn: (r) => exists r.name)
  |> increase()
  |> aggregate.rate(every: 15s, unit: 1s, groupColumns: ["name"])

... 
</code></pre>
<p>We use the "experimental/aggregate" module to replicate the "rate()". "groupColumns" applies the aggregation after combining / grouping by that column, here creating an "aggregate.rate" on the "name" column.</p>
<p>This method adds both windowing and extrapolation in comparison to the manual "aggregateWindow()"" example</p>
<p>This is the most accurate proxy to PromQL "rate()" available in InfluxDB, <a href="https://docs.influxdata.com/flux/v0/prometheus/metric-types/counter/#calculate-the-average-rate-of-change-in-specified-time-windows">InfluxDB, Work with Prometheus Counters</a>.</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-cpu-usage-b.webp" alt="" title="cpu usage panel (influxDB b)"></p>
<h1>Panel, CPU Usage, Reflection</h1>
<pre><code>Dashboard: cAdvisor [historic]
Panel: CPU usage
Data source: InfluxDB
</code></pre>
<p>Far more verbose query requirements in InfluxDB Flux.</p>
<p>Final solution uses an experimental module, which can be changed or removed at any moment.</p>
<p>Even with this, we can't seem to replicate the same numbers for the visualisation.</p>
<pre><code class="language-md">| Solution  | Name       | Mean    | Max      | Max Difference |
| --------- | ---------- | ------- | -------- | -------------- |
| PromQL    | alloy      | 0.111%  | 0.107%   | 0 (control)    |
| Influx, A | alloy      | 0.177%  | 0.108%   | 0.067%         |
| Influx, B | alloy      | 0.126%  | 0.107%   | 0.015%         |
| --------- | ---------- | ------- | -------- | -------------- |
| PromQL    | influx     | 0.0442% | 0.0319%  | 0 (control)    |
| Influx, A | influx     | 0.112%  | 0.0336%  | 0.0678%        |
| Influx, B | influx     | 0.263%  | 0.0370%  | 0.2188%        |
| --------- | ---------- | ------- | -------- | -------------- |
| PromQL    | prometheus | 0.0286% | 0.0270%  | 0 (control)    |
| Influx, A | prometheus | 0.0510% | 0.0274%  | 0.0224%        |
| Influx, B | prometheus | 0.0346% | 0.0274%  | 0.0060%        |
| --------- | ---------- | ------- | -------- | -------------- |
| PromQL    | loki       | 0.0223% | 0.0214%  | 0 (control)    |
| Influx, A | loki       | 0.0384% | 0.0216%  | 0.0161%        |
| Influx, B | loki       | 0.0247% | 0.0214%  | 0.0024%        |
| --------- | ---------- | ------- | -------- | -------------- |
| PromQL    | mimir      | 0.0215% | 0.0211%  | 0 (control)    |
| Influx, A | mimir      | 0.0338% | 0.0213%  | 0.0123%        |
| Influx, B | mimir      | 0.0271% | 0.0211%  | 0.0056%        |
| --------- | ---------- | ------- | -------- | -------------- |
| PromQL    | grafana    | 0.0199% | 0.0142%  | 0 (control)    |
| Influx, A | grafana    | 0.0676% | 0.0156%  | 0.0477%        |
| Influx, B | grafana    | 0.0526% | 0.0155%  | 0.0427%        |
| --------- | ---------- | ------- | -------- | -------------- |
| PromQL    | telegraf   | 0.0104% | 0.00995% | 0 (control)    |
| Influx, A | telegraf   | 0.0144% | 0.0100%  | 0.0040%        |
| Influx, B | telegraf   | 0.0117% | 0.00993% | 0.0013%        |
| --------- | ---------- | ------- | -------- | -------------- |
</code></pre>
<p>InfluxDB variation B is statistically better performing, with the only outlier being the "influx" service showing a 5x variation.</p>
<p>The trends displayed also vary across the solutions, and across the two data sources.</p>
<p>PromQL</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/promql-cpu-usage-a.webp" alt="" title="cpu usage panel"></p>
<p>InfluxDB / A</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-cpu-usage-a.webp" alt="" title="cpu usage panel (influxDB a)"></p>
<p>InfluxDB / B</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-cpu-usage-b.webp" alt="" title="cpu usage panel (influxDB b)"></p>
<p>The calculation inconsistency, the trends variation, and the difficulty of the implementation, makes this port a "FAIL".</p>
<p>Whilst assessing our ability to port PromQL dashboards to InfluxDB data source, if one more "FAIL" occurs we will have to declare the port as a fail also.</p>
<h1>Panel, Container Restarts, Prometheus</h1>
<pre><code>Dashboard: cAdvisor
Panel: Container Restarts
Data source: Prometheus
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/promql-container-restarts.webp" alt="" title="container restarts panel"></p>
<p>The PromQL query:</p>
<pre><code>sum by (name) (count_over_time(
  (changes(container_last_seen{name=~".+"}[1m])>0)
  [5m:1m]
))
</code></pre>
<p>The easiest way to think about this is as a heartbeat. Every minute your Prometheus scraping job will record 4 sets of metrics, since my scrape interval is 15s. That gives us a heartbeat per minute of 4.</p>
<p>Next step is to use the cAdvisor "container_last_seen", a timestamp.</p>
<pre><code>time() - container_last_seen{name=~".+"}
</code></pre>
<p>Subtracting that last seen timestamp from current time, and calling that query will return a value between 0.00 to 15.00, because 15s is our scraping interval.</p>
<p>The next step is to track changes of that value. Since we want heartbeats / minute we set the range to [1m]. Counting the "changes()" of container_last_seen across a 1m time window will usually return 3; we are counting the changes or gaps between 4 points, not the points themselves.</p>
<p>This gives us an effective bpm. For the visualization, we actually want to see issues with a container, and restarts. To do that we need to add another window of time analysis to our bpm, essentially answering "how's our container heartbeat over 5m?". If it dips, it signifies a health issue.</p>
<p>To achieve this we count the change over a 5m window at a 1m resolution [5m:1m] using "count_over_time". This should nominally return 5, as in the "container was last seen 5 times in 5 minutes" aka container is alive. Any value lower than 5 is symptomatic of issues, and the closer we get to 0 the more likely the container is restarted, throttled, stopped, or crashed.</p>
<p>The final "sum by (name)" tidies the data to group by name.</p>
<p>One final tweak is "changes()>0", which increases our resolution. Changes will be 0 if the container has not been seen for 1m. You might be thinking "but we're discarding values, how are we increasing the resolution?". Well, the "changes()" output is used within a "count_over_time()", when changes is 0 we still count it so we actually will get a 5 count over 5m window.</p>
<p>When we add the ">0" to changes we now get a 4 from "count_over_time", which will show up in the visualization as symptomatic. For example, in the above chart there's the grafana service, which reported 4 bpm for a period, the container was stopped, but reporting was laggy.</p>
<blockquote>
<p>Why use a heatmap for discrete events?</p>
</blockquote>
<p>Normally restart events are discrete events, but sadly I haven't found a native way to ingest the Docker events with this system. I know you can add an exporter service or write your own, but I wanted to make use of what I had.</p>
<p>I think if I had access to restart events, I would not use a "Heatmap", I'd use a "Time series" visualization, a better way to show discrete events.</p>
<p>In this case, we are tracking the volume of BPM, and want to see decreases since they are linked to health symptoms. A heatmap is adequate here.</p>
<h1>Prometheus custom metrics</h1>
<p>In Prometheus you can define 2 types of rules, recording and alerting rules. Because the above heartbeat might be a useful reusable metric I'd like to define it as a custom metric via a recording rule.</p>
<p>First define the rule file location in the global Prometheus config.</p>
<pre><code class="language-yml">rule_files:
  - "rules/container_heartbeat.yml"
</code></pre>
<p>Then define the container_heartbeat.yml recording rule, here using the same expression detailed above.</p>
<pre><code class="language-yml">groups:
  - name: container_heartbeat
    interval: 1m
    rules:
      - record: container_heartbeat:bpm
        expr: (changes(container_last_seen{name=~".+"}[1m])>0)
</code></pre>
<p>The interval here is just an extra optimisation point, we're only running / recording the new metric every minute. We don't need finer granularity.</p>
<blockquote>
<p>Recording rules allow you to precompute frequently needed or computationally expensive expressions and save their result as a new set of time series. Querying the precomputed result will then often be much faster than executing the original expression every time it is needed. This is especially useful for dashboards, which need to query the same  expression repeatedly every time they refresh.</p>
</blockquote>
<p>Not a big concern here, but nice to try it for something low effort, from <a href="https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/">Prometheus, Defining Rules</a></p>
<p>With this we can now change the above PromQL query to:</p>
<pre><code>sum by (name) (count_over_time(
  container_heartbeat:bpm
  [5m:1m]
))
</code></pre>
<h1>Panel, Container Restarts, InfluxDB</h1>
<pre><code>Dashboard: cAdvisor [historic]
Panel: Container Restarts
Data source: InfluxDB
</code></pre>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-container-restarts-3h.webp" alt="" title="container restarts panel (influxdb 3h)"></p>
<p>For reference the Prometheus version for the same time range</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/promql-container-restarts-A-3h.webp" alt="" title="container restarts panel (promql 3h)"></p>
<p>The visual differences you see are just a rendering issue cause by the amount of points visualized, and you'll notice it on the InfluxDB panel in the next section.</p>
<pre><code>from(bucket: "metrics")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r._measurement == "prometheus_remote_write")
  |> filter(fn: (r) => r._field == "container_last_seen")
  |> filter(fn: (r) => exists r.name)
  |> difference()
  |> aggregateWindow(every: 1m, fn: count, createEmpty: false)
  |> aggregateWindow(every: 1m, period: 5m, fn: count)
  |> rename(columns: {_value: "bpm"})
  |> group(columns: ["name"])
</code></pre>
<p>First steps are of course the filters: by metric, by field, only valid name data.</p>
<p>"difference()" here calculates the change between points, similar to "change()" from PromQL.</p>
<p>We expect from "difference()" 3 items, given we have narrowed our range to 1m. To test this for InfluxDB we'd actually have the set the Data Explorer time dropdown to 1m vs Prometheus [1m] range setting.</p>
<p>"difference()" correctly returns a table / name, as expected, and the spaces between each points, as [15, 15, 15] across the rows under "_value".</p>
<p>PromQL "change()" returns the amount of changes, 3, the size of the array of changes, not the values themselves.</p>
<p>Lets aggregate the count for a 1m time window:</p>
<pre><code>|> difference()
|> aggregateWindow(every: 1m, fn: count, createEmpty: false)
</code></pre>
<p>This correctly returns the tables / name, but within each table we get 2 rows: one with 0 and one with the count we expect, 3.</p>
<p>To drop these empty rows, we use the "createEmpty" param.</p>
<p>Next we need to count up all 1m window changes.</p>
<pre><code>|> aggregateWindow(every: 1m, fn: count, column: "has_changed", createEmpty: false)

|> aggregateWindow(every: 1m, period: 5m, fn: count)
</code></pre>
<p>We use "period" to define the sliding window for aggregation, 5m, and every to define how often we're counting, every minute.</p>
<p>Finally we rename the "_value" column to get a tidier response.</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-container-restarts-3h.webp" alt="" title="container restarts panel (influxdb 3h)"></p>
<h1>Panel, Container Restarts, Reflection</h1>
<blockquote>
<p>InfluxDB does not currently support visualizing Prometheus histogram metrics as a traditional histogram.</p>
</blockquote>
<p>I was initially worried that the "Container Restarts" visualization would not be achievable with InfluxDB queries, but I think they mean "natively" not supported in the InfluxDB Data Explorer since they cover examples of how to query histograms.</p>
<p><a href="https://docs.influxdata.com/flux/v0/prometheus/metric-types/histogram/#visualize-prometheus-histograms-in-influxdb">InfluxDB, Work with Prometheus Histograms</a></p>
<p>Worth saying that the Prometheus panel, is not actually using a histogram dataset, rather a Counter.</p>
<p>Possibly there's an extra lesson here about the value of a histogram, and it's "dos and donts". In theory, the use for container restarts is daft, since restarts are discrete events. A time series is better for this. Yet the way the data was diced up or downsampled made it possible and not horrible to use a heatmap, showing us a clear picture of health issues across our system.</p>
<p>What will it be like to deal with REAL histogram dataset, remains to be seen. The docs cover one such example using <code>prometheus.histogramQuantile</code> from "experimental/prometheus" and looks promising once downsampling has happened. Yet it is an experimental package, and that comes with a stern warning.</p>
<p>More visualization concerns with InfluxDB where the beginning of the chart is darker. This doesn't happen in the Prometheus panel, and is likely to do with how the queries run or return differently across the datasources.</p>
<p>Further it seems like finer changes in the trends are lost at longer timeframes: watch careful how some flakyness in the grafana service doesn't display in InfluxDB, whilst showing in Prometheus.</p>
<p>Prometheus:</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/promql-container-restarts-A-6h.webp" alt="" title="container restarts panel (promql 6h)"></p>
<p>InfluxDB:</p>
<p><img src="https://alanionita.github.io/images/posts/2026-03-09-prometheus-influxdb/influxdb-container-restarts-A-6m.webp" alt="" title="container restarts panel (influxdb 6h)"></p>
<p>Also notice the colour rendering issue we saw above.</p>
<p>The InfluxDB Flux query itself is almost 3x larger than PromQL, and not as easy to reason about. It's also using 3x filter() and 2x aggregateWindows() which are likely to be slower than PromQL. Benchmarking evidence is required to validate this claim.</p>
<p>The output of the query, is indeed closer to PromQL and for that I consider this step a "PASS"</p>
<h1>Conclusion</h1>
<blockquote>
<p>Should you use InfluxDB for long-term Prometheus storage?</p>
</blockquote>
<p>InfluxDB would want you to, and offer enough info to make your life easier. It's also possible that v3 is better in some respects, although the query process seen above will be similar.</p>
<p>There is labor involved in crafting the queries such that they align across the two systems. They are not carbon copies!</p>
<p>That labor requires knowledge of both Prometheus and InfluxDB. Enough knowledge that I don't think someone can complete a port without a high amount of effort and focus.</p>
<p>I'd rather use a long-term storage system with native PromQL support.</p>
<p>Dealing with data and trend mismatch is painful, even once you've created the queries across the two systems. Although I'll add the caveat that it could also be a skill issue on my part.</p>
<p>I stopped porting 6 panel visualizations because they all used a combination of rate() and sum(). The findings from CPU Usage panel will likely repeat across all of these.</p>
<p>In CPU Usage the reported data variations between Prometheus and InfluxDB were between 2-6x.</p>
<p>How can I justify to management that our audit data is 2-6x different than our daily data. It would certainly be a difficult thing to defend in a fictional court situation.</p>
<p>And because of that... the labor, the query data inconsistencies, and the possible performance differences... InfluxDB is not my choice of long-term storage for Prometheus.</p>
<p>"Horses for courses" as they say.</p>
<p>I hope you do consider it for your use case, and may this analysis be enough to get you up and running.</p>

                ]]>
            </description>
        </item><item>
            <title>Docker for Node</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2026-02-06-docker-for-node</link>
            <pubDate>Fri, 06 Feb 2026 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Docker for Node</h1>
<p>Any non-Alpine Linux images have a significant amount of vulnerabilities. However Alpine is bare-bones and requires more work to get setup.</p>
<blockquote>
<p>Worth saying that vulnerabilities, what are vulnerabilities etc is tricky to define here. I'm increasingly feeling like the ranking of vulnerabilities is biased towards 'high', but I think it's important to zoom in on the volume of vulnerabilities here</p>
</blockquote>
<p>Scenario: host a micro-service with Typescript modules and some features to post out to another Docker service. Think about it as a simple lambda.</p>
<p>This doesn't need a full-fat Linux distro for the container, so why would you use it?</p>
<p>Well... I think you'd use it because it's there.</p>
<p>Going to the <a href="https://hub.docker.com/_/node/tags">Node images repository</a> and you can see huge amount of image options.</p>
<p>Let's take the <a href="https://hub.docker.com/layers/library/node/24.13-trixie/images/sha256-542d79268854032f4580af3af391fc3246d0b6dc4b45accb9617ea939bbbc709">node:24.13-trixie</a> for example:</p>
<ul>
<li>It has a total for 6 high, 9 medium, 136 low vulnerabilities</li>
<li>Node v24.13 introducing 6 high, 1 medium, 1 low vulnerabilities</li>
</ul>
<p>If we filter the vulnerabilities we notice that the entire image has just 1 'unpatchable' vulnerability, which at the time of writing is 14days old.</p>
<h1>Not plug and play</h1>
<p>A few years ago I experienced a hard bug with downstream dependency due to Debian security repo changing locations, and it bricked a lot of images - including the Node image I was using.</p>
<p>The <a href="https://stackoverflow.com/questions/68802802/repository-http-security-debian-org-debian-security-buster-updates-inrelease/76153964#76153964">Stack Overflow question</a> for that issue has 118k views.</p>
<p>That experience made me audit the images more before using them.</p>
<p>The community vibe around images is more 'just plug and play', a naive view that is very far from the truth.</p>
<p>Images are very sensitive and can't be relied upon fully even when published by Node themselves.</p>
<h1>Suggestions</h1>
<p>If you are using hosted images, learn to review the images you use, what dependencies they have, what fixes they require.</p>
<p>In general, choosing an Alpine base gives you most stable base.</p>
<p>If you really want certainty, build your own image from an Alpine base, and reuse it.</p>
<p>Review the CVEs that can't be fixed for your own judgment on the issue:</p>
<ul>
<li>What is it about?</li>
<li>How can it be exploited?</li>
<li>How does it affect your code?</li>
<li>How doesn't it stack on the MITRE framework</li>
</ul>
<h2>How to audit, quickly</h2>
<p>This is just the quick process, there's more to it that this but you can start with this.</p>
<ul>
<li>Go to <a href="https://hub.docker.com/">Docker hub</a> and search for your image</li>
<li>Look for 'official' badge</li>
</ul>
<blockquote>
<p>Ignore DHI images for now, even though these will be top of the list; will cover DHI in another post</p>
</blockquote>
<p>On /image page</p>
<ul>
<li>Tag Summary (right column)</li>
<li>Review and make note of recently updated tag</li>
<li>Check pulls</li>
</ul>
<blockquote>
<p>For non-official/obscure images you want to see how many people are using it; not to say that use is a direct proxy for a quality image, but it's a data point</p>
</blockquote>
<ul>
<li>Click on "Tags"</li>
</ul>
<p>On /image/tags page</p>
<ul>
<li>Look for the 'recent' tags from above</li>
<li>Review vulnerabilities, size, and commands</li>
</ul>
<blockquote>
<p>As part of the command you can see the name of the tag, can be used in Docker file definitions</p>
</blockquote>
<ul>
<li>Open the tag page</li>
</ul>
<p>On /images/tags/tag page</p>
<ul>
<li>Review where the vulnerabilities are coming from</li>
</ul>
<blockquote>
<p>Up to this point all the vulnerability totals are sums for the whole image; however, the base image in some case will have 0 or more vulnerabilities, in addition to the module added to the base image</p>
</blockquote>
<ul>
<li>Review the CVEs themselves</li>
<li>You can expand a vulnerability to see further details, review the dates</li>
<li>Then I navigate to the Docker.Scout page for the vulnerability</li>
</ul>
<blockquote>
<p>This gives you more insight into the mechanics of the CVE, and based on this decide how to deal with it + impact of it on your app</p>
</blockquote>
<ul>
<li>Review the scripts with alert badges</li>
</ul>
<blockquote>
<p>Important to understand the scripts with issues, especially if you might have to rebuild the image itself</p>
</blockquote>

                ]]>
            </description>
        </item><item>
            <title>Complex Amplify VTL resolver</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-09-02-complex-amplify-vtl</link>
            <pubDate>Tue, 02 Sep 2025 00:00:00 +0100</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Complex VTL resolvers, for Amplify</h1>
<p>Let's start with a problem: you want to implement a profile editing feature, your app is a VueJS frontend and an Node AppSync GraphQL backend, and you database layer is a set of DynamoDB tables.
As for the implementation, the AppSync resolvers are a mix of Javascript Lambdas and Apache VTL resolvers.</p>
<p>The profile editing requires dynamic data inputs: when editing a profile you can edit one feature, none of them, or all of them.</p>
<p>Here's a simple DynamoDB.UpdateItem command example.</p>
<pre><code class="language-json">{
    "version" : "2018-05-29",
    "operation" : "UpdateItem",
    "key": {
        "id" : $util.dynamodb.toDynamoDBJson($context.identity.username)
    },

    "update" : {
        "expression" : "set #name = :name, imgUrl = :imgUrl, bgImgUrl = :bgImgUrl, bio = :bio, #location = :location, website = :website, birthdate = :birthdate",
        "expressionNames" : {
           "#name" : "name",
           "#location": "location",
       },

       "expressionValues" : {
            ":name" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.name),
            ":imgUrl" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.imgUrl),
            ":bgImgUrl" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.bgImgUrl),
            ":bio" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.bio),
            ":location" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.location),
            ":website" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.website),
            ":birthdate" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.birthdate),
        }

    },

    "condition" : {
        "expression" : "attribute_exists(id)"
    },
}

</code></pre>
<p>The AppSync GraphQL query looks a bit like this.</p>
<pre><code class="language-gql">mutation editMyProfile($input: ProfileInput!) {
    editMyProfile(newProfile: $input) {
        ... myProfileFields

        tweets {
        nextToken
        tweets {
            ... on Tweet {
                ... tweetFields
            }
        }
    }
}
</code></pre>
<p>And the ProfileInput type has this shape.</p>
<pre><code class="language-gql">input ProfileInput {
    name: String!
    imgUrl: AWSURL
    bgImgUrl: AWSURL
    bio: String
    location: String
    website: String
    birthdate: AWSDate
}
</code></pre>
<p>Within ProfileInput, only <code>name</code> is required, with the remaining values being optional.</p>
<p>In the VTL template we wrote the command as if that all the attributes exist at execution time. When they are missing DynamoDB will override those values with <code>'null'</code>, a significant issue that leads to data loss.</p>
<h2>DynamoDB.UpdateItem</h2>
<p>Updates is DynamoDB tend to require complex queries because they have a large amount of requirements, and come with the added risk of data loss.</p>
<p>Officially AWS recommends that an Update query handles these situations:</p>
<ul>
<li>handles each argument to produce a list of required updates</li>
<li>skips over any guaranteed arguments</li>
<li>if an argument is <code>null</code> or empty (<code>""</code>), that argument is removed from the item</li>
<li>if an argument is missing from the item, that argument is added</li>
<li>if an argument exists on the item, that argument is set to the new value</li>
</ul>
<p>Even though this is true for most DynamoDB update commands, the example below is an AppSync specific one.</p>
<p>These scenarios produce 3 sets of instruction lists around the directive verbs: remove, set, add.</p>
<p>The expression itself is empty and we then use the lists to populate the expression, expressionNames, and expressionValues accordingly.</p>
<p><a href="https://docs.aws.amazon.com/appsync/latest/devguide/aws-appsync-resolver-mapping-template-reference-dynamodb-updateitem.html">AWS AppSync, Mapping Template Resource / DynamoDB.UpdateItem example</a></p>
<pre><code class="language-vtl">{
    "version" : "2017-02-28",

    "operation" : "UpdateItem",

    "key" : {
        "id" : $util.dynamodb.toDynamoDBJson($ctx.args.id)
    },

    ## Set up some space to keep track of things we're updating **
    #set( $expNames  = {} )
    #set( $expValues = {} )
    #set( $expSet = {} )
    #set( $expAdd = {} )
    #set( $expRemove = [] )

    #foreach( $entry in $context.arguments.entrySet() )
        #if( $entry.key != "name" )
            #if( (!$entry.value) &#x26;&#x26; ("$!{entry.value}" == "") )
                ## If the argument is set to "null", then remove that attribute from the item in DynamoDB **

                #set( $discard = ${expRemove.add("#${entry.key}")} )
                $!{expNames.put("#${entry.key}", "$entry.key")}
            #else
                ## Otherwise set (or update) the attribute on the item in DynamoDB **

                $!{expSet.put("#${entry.key}", ":${entry.key}")}
                $!{expNames.put("#${entry.key}", "$entry.key")}

                #if( $entry.key == "ups" || $entry.key == "downs" )
                    $!{expValues.put(":${entry.key}", { "N" : $entry.value })}
                #else
                    $!{expValues.put(":${entry.key}", { "S" : "${entry.value}" })}
                #end
            #end
        #end
    #end

    ## Start building the update expression, starting with attributes we're going to SET **
    #set( $expression = "" )
    #if( !${expSet.isEmpty()} )
        #set( $expression = "SET" )
        #foreach( $entry in $expSet.entrySet() )
            #set( $expression = "${expression} ${entry.key} = ${entry.value}" )
            #if ( $foreach.hasNext )
                #set( $expression = "${expression}," )
            #end
        #end
    #end

    ## Continue building the update expression, adding attributes we're going to ADD **
    #if( !${expAdd.isEmpty()} )
        #set( $expression = "${expression} ADD" )
        #foreach( $entry in $expAdd.entrySet() )
            #set( $expression = "${expression} ${entry.key} ${entry.value}" )
            #if ( $foreach.hasNext )
                #set( $expression = "${expression}," )
            #end
        #end
    #end

    ## Continue building the update expression, adding attributes we're going to REMOVE **
    #if( !${expRemove.isEmpty()} )
        #set( $expression = "${expression} REMOVE" )

        #foreach( $entry in $expRemove )
            #set( $expression = "${expression} ${entry}" )
            #if ( $foreach.hasNext )
                #set( $expression = "${expression}," )
            #end
        #end
    #end

    ## Finally, write the update expression into the document, along with any expressionNames and expressionValues **
    "update" : {
        "expression" : "${expression}"
        #if( !${expNames.isEmpty()} )
            ,"expressionNames" : $utils.toJson($expNames)
        #end
        #if( !${expValues.isEmpty()} )
            ,"expressionValues" : $utils.toJson($expValues)
        #end
    },

    "condition" : {
        "expression"       : "version = :expectedVersion",
        "expressionValues" : {
            ":expectedVersion" : $util.dynamodb.toDynamoDBJson($ctx.args.expectedVersion)
        }
    }
}

</code></pre>
<h2>Solution</h2>
<p>For our app this means that whenever we pass a profile with an <code>undefined</code> value we will remove the existing values from the item.</p>
<p>Again we hit the data loss problem, however we can stop building the 'remove' instructions lists for undefined values.</p>
<pre><code class="language-vtl">#set( $expNames  = {} )
#set( $expValues = {} )
#set( $expSet = {} )
#set( $expRemove = [] )

#foreach( $entry in $context.arguments.newProfile.entrySet() )
    #if( $entry.key != "name" )
        #if( (!$entry.value) &#x26;&#x26; ("$!{entry.value}" == "") )
            ## If the argument is set to null, then remove that attribute from the item in DynamoDB **

            #set( $discard = ${expRemove.add("#${entry.key}")} )
            $!{expNames.put("#${entry.key}", "$entry.key")}
        #else
            ## Otherwise set (or update) the attribute on the item in DynamoDB **

            $!{expSet.put("#${entry.key}", ":${entry.key}")}
            $!{expNames.put("#${entry.key}", "$entry.key")}
            $!{expValues.put(":${entry.key}", { 
                "S": "${entry.value}"
            })}
            
        #end
    #else
        $!{expSet.put("#${entry.key}", ":${entry.key}")}
        $!{expNames.put("#${entry.key}", "$entry.key")}
        $!{expValues.put(":${entry.key}", { 
            "S": "${entry.value}"
        })}
    #end
#end

## Start building the update expression, starting with attributes we are going to SET **

#set( $expression = "" )
#if( !${expSet.isEmpty()} )
    #set( $expression = "SET" )
    #foreach( $entry in $expSet.entrySet() )
        #set( $expression = "${expression} ${entry.key} = ${entry.value}" )
        #if ( $foreach.hasNext )
            #set( $expression = "${expression}," )
        #end
    #end
#end

## Continue building the update expression, adding attributes we are going to REMOVE **
#if( !${expRemove.isEmpty()} )
    #set( $expression = "${expression} REMOVE" )

    #foreach( $entry in $expRemove )
        #set( $expression = "${expression} ${entry}" )
        #if ( $foreach.hasNext )
            #set( $expression = "${expression}," )
        #end
    #end
#end

{
    "version": "2018-05-29",
    "operation": "UpdateItem",
    "key": {
        "id" : $util.dynamodb.toDynamoDBJson($context.identity.username)
    },
    "update": {
        "expression" : "${expression}"
        #if( !${expNames.isEmpty()} )
            ,"expressionNames" : $utils.toJson($expNames)
        #end
        #if( !${expValues.isEmpty()} )
            ,"expressionValues" : $utils.toJson($expValues)
        #end
    },
    "condition" : {
        "expression" : "attribute_exists(id)"
    }
}
</code></pre>
<p>We continue to use the 'remove' lists for incoming values set to <code>null</code> or <code>""</code> eg. user wants to change their website to an empty string.</p>
<p>We no longer handle any 'add' instructions because can handle new properties with 'set'.</p>
<p>With these changes we can now support the dynamic ProfileInput object and correctly make the database changes.</p>
<h2>Testing</h2>
<p>The testing of AWS AppSync VTL templates generically involves:</p>
<ul>
<li>creating a mock context object with relevant identity and arguments (payload)</li>
<li>loading the VTL template given the mock context</li>
<li>verifying the VTL template expansion</li>
</ul>
<p>When it comes to the data verification we are looking to check:</p>
<ul>
<li>expression shape</li>
<li>expressionNames presence and values</li>
<li>expressionValues shape, presence, and values</li>
</ul>
<p>If payload newProfile contains name, bio, website</p>
<ul>
<li>Then the template expression should feature name, bio, website</li>
<li>Where all params in the expression are under the SET verb</li>
<li>The expressionNames should contain #name, #bio, #website</li>
<li>The expressionNames should have corresponding values</li>
<li>The expressionValues should contain :name, :bio, :website</li>
<li>The expressionValues should have the corresponding values</li>
<li>The expressionValues are correctly mapped as DynamoDB string ("S")</li>
</ul>
<p>If payload newProfile contains name, bio, and bio is <code>null</code> or <code>""</code></p>
<ul>
<li>Then  the template expression should feature name, bio</li>
<li>Where *name is under the SET verb</li>
<li>Where *bio is under the REMOVE verb</li>
</ul>
<h2>Notes</h2>
<h3>ExpressionValues</h3>
<p>ExpressionValues are strings.</p>
<p>One crucial thing to note is the discrepancy between the ProfileInput types and the DynamoDB expressionValues.</p>
<p>Even though some values are of type AWSUrl or AWSDate, when given to the expressionValue they must be of type string ("S"), otherwise the query will fail.</p>
<p>Please note that this issue will trigger a false positive in testing because template itself is still value regardless of the correct value of expressionValues</p>
<h3>Template evaluation</h3>
<p>AppSync testing can be done by combining the "@aws-amplify/amplify-appsync-simulator" and "amplify-velocity-template" packages.</p>
<p>This method is prone to errors, and requires some output parsing of the templates after they've been loaded by the simulator eg. trimming training commas from the output.</p>
<p>A more reliable option is the <code>EvaluateMappingTemplateCommand</code> from "@aws-sdk/client-appsync". This uses an AppSync Client to evaluate any template string for validity, and it's a locally executing command.</p>
<p><a href="https://docs.aws.amazon.com/appsync/latest/APIReference/API_EvaluateMappingTemplate.html">AWS Amplify EvaluateMappingTemplateCommand documentation</a></p>
<h3>VTL variations</h3>
<p>You can also try a simpler loop for building the expression.</p>
<pre><code class="language-vtl">#set($exp = "SET #name = :name")
#set($expNames = {"#name":"name"})
#set($expValues = {":name": {"S": $ctx.args.newProfile.name}})

#set($fields = ["imgUrl", "bgImgUrl", "bio", "location", "website", "birthdate"])

#foreach($field in $fields)
  #if($ctx.args.newProfile[$field] &#x26;&#x26; "$!ctx.args.newProfile[$field]" != "")
    #set($exp = "$exp, #${field} = :${field}")
    $util.qr($expNames.put("#${field}", "$field"))
    $util.qr($expValues.put(":${field}", {"S": "$ctx.args.newProfile[$field]"}))
  #end
#end

{
    "version": "2018-05-29",
    "operation": "UpdateItem",
    "key": {
        "id": $util.dynamodb.toDynamoDBJson($ctx.identity.username)
    },
    "update": {
        "expression": "$exp",
        "expressionNames": $util.toJson($expNames),
        "expressionValues": $util.toJson($expValues)
    },
    "condition": {
        "expression": "attribute_exists(id)"
    }
}

</code></pre>
<p>This is shorter, but will not handle scenarios where users require the removal of values eg. when a user wants to set the their existing profile website to <code>""</code>.</p>
<h2>Documentation</h2>
<p>Besides the AWS resources and the VTL project docs, I found the JEdit plugin writing guide to be excellent. The JEdit plugins are written in Java, but make use of VTL and the explanations of the various VTL directives are very useful and concise.</p>
<p>On testing the <code>amplify-velocity-template</code> module test suite is a great resource for ideas on how to test more complex VTL templates.</p>
<p><a href="https://docs.aws.amazon.com/appsync/latest/devguide/resolver-mapping-template-reference-dynamodb.html">AWS AppSync resolver mapping template reference for DynamoDB</a></p>
<p><a href="https://velocity.apache.org/engine/devel/vtl-reference.html#vtl-reference-contents">The Apache Velocity Project - Developer reference</a></p>
<p><a href="https://plugins.jedit.org/plugindoc/Templates/ch04.html">JEdit - Plug-in writing developer guide, VTL Directives</a></p>
<p><a href="https://github.com/aws-amplify/amplify-cli/tree/dev/packages/amplify-velocity-template/tests">Amplify Velocity Template - test suite</a></p>

                ]]>
            </description>
        </item><item>
            <title>Amplify Vite config correction</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-06-11-amplify-vite-config</link>
            <pubDate>Wed, 11 Jun 2025 00:00:00 +0100</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Amplify Vite config correction</h1>
<p>Under the Vue docs for Amplify UI, we see a the following config specification.</p>
<p>https://ui.docs.amplify.aws/vue/connected-components/authenticator</p>
<pre><code class="language-js">// vite.config.js

export default defineConfig({
  plugins: [vue()],
  resolve: {
      alias: [
      {
        find: './runtimeConfig',
        replacement: './runtimeConfig.browser', // ensures browser compatible version of AWS JS SDK is used
      },
    ]
  }
})
</code></pre>
<p>The config is required to ensure a browser compatible version of <code>AWS JS SDK</code>.</p>
<blockquote>
<p>Why do we need the AWS JS SDK if we're using Amplify? None of the Amplify docs mention the <code>aws-sdk</code> as a dependency.</p>
</blockquote>
<h2>Problem</h2>
<p>Logic above expects to config Vite aliases with find / replacement pattern.</p>
<p>However the default starter config recommends a simple object structure with key / value pairs.</p>
<p>The Vue v3 wizard produces this Vite configuration</p>
<pre><code class="language-js">// vite.config.js

export default defineConfig({
  plugins: [
    vue(),
    vueDevTools(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
})
</code></pre>
<h2>Solution</h2>
<p>Far more sensible to bypass the documentation suggestion, and match the key/value format.</p>
<pre><code class="language-js">// vite.config.js

export default defineConfig({
  plugins: [
    vue(),
    vueDevTools(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
      './runtimeConfig': path.resolve(__dirname, './runtimeConfig.browser')
    },
  },
})
</code></pre>
<h2>Why does it matter?</h2>
<p>Yes, because it meets users where ever they are by matching closely the default starter config vs. introducing a new pattern.</p>
<p>Depending on config you could end up with some odd issues.</p>
<blockquote>
<p>A mis-configured alias would trigger errors stemming from <code>EnvironmentPluginContainer.resolveId</code> and <code>EnvironmentModuleGraph._resolveUrl</code>. A false negative pointing to mis-configured environment variables as opposed Vite alias resolution.</p>
</blockquote>

                ]]>
            </description>
        </item><item>
            <title>Amplify Documentation Safari</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-06-11-amplify-docs-safari.md</link>
            <pubDate>Wed, 11 Jun 2025 00:00:00 +0100</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Amplify Documentation Safari</h1>
<p>Join me for a pinball ride through the Amplify docs, where you have to bob and weave in order to reach our goal of connecting an AppSync backend to a Vue frontend via the Amplify service and modules.</p>
<h2>Background</h2>
<h3>Amplify</h3>
<p>Amplify is an ecosystem that contains cli, scaffolding, and UI tools.</p>
<p>The combination of these tools means that we can:</p>
<ul>
<li>Configure a client API to hit our AppSync backend</li>
<li>Configure the authentication flow as Cognito</li>
<li>Use pre-build UI components to handle the authentication stories via Cognito: sign in, sign up, login, forgot password</li>
<li>Manage tokens from Cognito</li>
<li>Send queries down to this API</li>
</ul>
<p>Of note to us, is the integration with Cognito for auth, and calls to the Appsync backend.</p>
<h3>Cognito</h3>
<p>Cognito is an OAuth flow implementation and as such would require some legwork (boilerplate code) to manage keys and orchestrate the flows correctly.</p>
<p>With Amplify we get UI components, and API configurations. We no longer manage tokens and the standard OAuth flows, vastly simplifying the service logic.</p>
<h3>Service architecture</h3>
<p>Backend:</p>
<ul>
<li>AppSync with data sources as Lambda, DynamoDB, etc</li>
<li>Cognito for auth</li>
</ul>
<p>Frontend:</p>
<ul>
<li>Framework use: Vue, React etc</li>
<li>Cognitor for auth</li>
</ul>
<h2>Plans</h2>
<p>Use Amplify for:</p>
<ul>
<li>Connecting to a pre-deployed AppSync API</li>
<li>Authenticating via Cognito, managing tokens behind the scenes</li>
<li>Running GraphQL queries and getting data back in the UI</li>
</ul>
<p>The goals of this article:</p>
<ul>
<li>Enumerate and explain documentation locations</li>
<li>Sign-post and add commentary about documentation</li>
</ul>
<h2>Amplify, code generation</h2>
<p>A big chore for GraphQL processes is the mirroring of queries across backend and frontend.</p>
<p>With Amplify you can with one command generate code for all Queries and Mutations from your AppSync API:</p>
<pre><code>npx @aws-amplify/cli codegen add --apiId YOUR_APPSYNC_API_ID --region YOUR_REGION
</code></pre>
<p>This generates files inside <code>graphql/</code> for <code>queries.js</code> and <code>mutations.js</code> with following examples</p>
<pre><code class="language-js">export const getProfile = /* GraphQL */ `
  query GetProfile($screenName: String!) {
    getProfile(screenName: $screenName) {
      id
      name
      screenName
      imgUrl
      bgImgUrl
      bio
      location
      website
      birthdate
      createdAt
      tweets {
        nextToken
        __typename
      }
      followersCount
      followingCount
      tweetsCount
      likesCount
      following
      followedBy
      __typename
    }
  }
`;
</code></pre>
<h2>Amplify Docs safari</h2>
<h3>1. Amplify Vue Docs</h3>
<p>Best starting point is the official Amplify Docs, scoped by framework of choice.</p>
<p><a href="https://docs.amplify.aws/vue/">Amplify UI / Vue </a></p>
<p>From here, follow the <a href="https://docs.amplify.aws/vue/build-a-backend/data/set-up-data/">Set up Amplify Data</a></p>
<p>Once on the page you notice that the setup involves the scaffolding of both backend and frontend in parallel using Amplify.</p>
<p>The audience for Amplify seems to be folks that use it for both.</p>
<blockquote>
<p>I'd wager a bet that most developers build AppSync with Serverless Framework, CDK, or Terraform, bypassing Amplify on the backend, whilst using Amplify libraries on the UI.</p>
</blockquote>
<p>As someone with a pre-deployed AppSync API, that means hunting and pecking through the docs for relevant code and instructions.</p>
<p>What is useful here, is how to create the API on the client using <code>generateClient</code> from <code>aws-amplify/data</code></p>
<blockquote>
<p>Important because of the many ways to achieve this. Amplify modules tend to look the same, but contain key api variations that cause setup trouble.</p>
</blockquote>
<p>As a result, the docs above are not a good starting point!</p>
<h3>2. AppSync Console</h3>
<p>Most sensible spot to start, is the Appsync Console (web).</p>
<ul>
<li>From the AWS Console</li>
<li>Navigate to AppSync</li>
<li>Open your specific AP</li>
</ul>
<p>The instructions should appear my default on the main page, but they are are incomplete.</p>
<p>These instructions make use of <code>./aws-exports.js</code> which is not defined or explained.</p>
<blockquote>
<p>After a bit of experimentation, the aws-exports.js file is generated by one of the aws-amplify methods for creating an API.</p>
</blockquote>
<p>We can guess that it involves configuration details, but in the following steps the configuration variables are hard-coded in the file.</p>
<p>Also there are no:</p>
<ul>
<li>mentions about how to hide this sensitive information or how to use .env variables</li>
<li>configuration explanations for Cognito, even though most Appsync solutions will use it</li>
</ul>
<h3>3. Amplify UI Vue docs</h3>
<p>Essentially the same as point 1. but more focused on the frontend. Thus more relevant for your needs.</p>
<p>Requires framework scoping selection, below focused on Vue.</p>
<p><a href="https://ui.docs.amplify.aws/vue/connected-components/authenticator">Amplify UI / Vue / connected-components / Authenticator</a></p>
<p>At this point in our journey with Amplify and Appsync, we identiy as a developer who wants to connect a UI to an existing AppSync backend via Amplify.</p>
<p>Even though the docs here should be for us, the instructions are too simple:</p>
<ul>
<li>Doesn't cover any Sign In, Sign Up flows</li>
<li>Doesn't explain how to render different auth components depending on the authentication states: authenticated, unauthenticated, or loading.</li>
</ul>
<p>There is more information within the 'Advanced Usage' section of the docs, but all of the initial instructions are replaced my them.</p>
<blockquote>
<p>To me, these 'advanced' use cases, are just base requirements in 2025</p>
</blockquote>
<h2>Sandboxing</h2>
<p>Throughout the docs sand-boxing is mentioned, but never fully detailed.</p>
<p><a href="https://docs.amplify.aws/vue/deploy-and-host/sandbox-environments/features/">Amplify docs / sanbox-environments / features</a></p>
<p>Unclear how to use sand-boxing with an existing API but the secrets explanation is very helpful and interesting.</p>
<p>Via sand-boxing you attach secrets to a sandbox, and then reference it within your backend. The 'backend' definition here is murky, is it a local backend generated with Amplify, is it a Vue SSR backend, or is it an AWS backend?</p>
<p>Here we can find the 'Generate client config' docs, which outlines how to create <code>amplify_outputs.json</code> mentioned above.</p>
<p>No details on how to integrate a sandbox with a pre-deployed API or how to manage a sandbox in that instance. Seems like 'sand-boxing' is purely reserved for Amplify deployed resources, which tend to be sand-boxed within Amplify environments by default.</p>
<h2>Conclusion</h2>
<p>We have found a solution for configuring the auth with Amplify, and doing the Cognito auth via it's libraries.</p>
<p>We have also found a way to <code>defineClient()</code> via Amplify and make calls to the GraphQL backend.</p>
<p>Unresolved is the topic of, how to best establish the GraphQL backend integration.</p>
<p>The docs are inconsistent in details, and they rarely cover the setup from the perspective of different users. For the Amplify team, the only user of their API is a developer which is deploying with Amplify.</p>
<p>What happens to the UI libraries then? Are they only meant for those users?</p>
<p>I do plan to write a "how to" containing all the steps for the setup, and best practice suggestions.</p>

                ]]>
            </description>
        </item><item>
            <title>Strava to FOSS analysis and visualisation</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-03-19-strava-to-foss</link>
            <pubDate>Wed, 19 Mar 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Strava to FOSS analysis and visualisation</h1>
<p>https://alanionita.github.io/images/posts/strava-to-foss/lead.png</p>
<p>Having used Strava for years I've always been surprised that no one else has made a better alternative.</p>
<p>The UI has been frozen in time, and most of the <em>good</em> features are pay-walled.</p>
<p>I for one refuse to pay for Strava to analyze my data. A better way to sell the subscription would be to make a service so good, so rich, so useful, that I'm begging Strava to take my money.</p>
<p>That being said, Strava is good at activity tracking. Whilst I haven't done any thorough testing, Strava has worked well for me in terms of tracking accuracy.</p>
<p>I also enjoy the interval voice notifications and the multi-sport tracking, cycling especially.</p>
<p>The challenges also, get me outside in mechanical way that I appreciate, whilst cringing that I'm encouraged to go outside by the prospect of money of very expensive kit.</p>
<p>Anyway, where do we go from here...</p>
<p>Until I roll my sleeves and warm up my LLMs to build my own tracking app, I will continue to use Strava.</p>
<p>But I'm planning to own more of my data, and interrogate it locally. And this was the inception moment for the 'Strava to FOSS' project.</p>
<h2>The Project</h2>
<p>Core features:</p>
<ul>
<li>Download activity data from Strava</li>
<li>Convert data to geospatial data</li>
<li>Use GeoPandas to explore data</li>
<li>Use OSMnx to enrich the data with OpenStreetMap nodes</li>
<li>Use OSMnx and Leaflet to generate custom maps</li>
</ul>
<p>The project is currently just a series of notebooks on Github, but overtime will be a proper service.</p>
<p><a href="https://github.com/alanionita/strava-to-foss-notebooks-python">Link to repository</a></p>
<h2>Talking to Strava</h2>
<p>The <code>strava_data.ipynb</code> notebook show an example of dealing with the Strava OAuth flow.</p>
<p>I won't go into detail, since the notebook is easy to follow. Do note that the example just gets the first 10 activities.</p>
<p>Once you reached the end of that process you will have an <code>activities.json</code>.</p>
<h2>The data</h2>
<p>For the sake of our task we need to get the route of the activity, and one way to do this is to use the <code>.map.summary_polyline</code></p>
<pre><code class="language-json">[
    {
        "map": {
       	    "id": "a13798432720",
            "summary_polyline": "",
            "resource_state": 2
	},
    }
]
</code></pre>
<p>Unclear why, but some activities don't have this entry, whilst other valid entries contain a string.</p>
<p>Also unclear is whether this value will contain elevation data, we have <code>_high</code> and <code>_low</code> elev values within the activity. A good mystery to uncover in a future post and feature.</p>
<h2>Geospatial primer</h2>
<h3>Geometry</h3>
<blockquote>
<p>Note: the supporting code from this point forward in the <code>osm.ipynb</code> notebook.</p>
</blockquote>
<p>Quick intro to geospatial data is that most things relate to Lines, Polylines, and Polygons.</p>
<p>We normally work with these basic geometric shapes using a low level lib called Shapely.</p>
<p>Whenever we use a geospatial framework, like GeoPanda, they in turn will use Shapely under the hood.</p>
<p>Normally you just get the data in a nice format, but when you're unlocking existing data you will need to implement Shapely for data conversions.</p>
<h3>Coordinates</h3>
<p>Another important concept with geospatial data is the use of coordinates: latitude, longitude.</p>
<p>You'll see below that in order to make our lives easier we convert original PolyLine data to a geospatial list of coordinates.</p>
<p>I can't stress to you how useful this can be for future analysis.</p>
<p>Take a regular 20km run as geospatial lists: we end up with ~300 coordinate points, we can then compare distances between these, track time across each coordinate point and more.</p>
<h3>Formats and GeoInfra</h3>
<p>Geospatial data use a JSON flavour called <code>.geojson</code>. You'll see below that there's a big fuss about saving our converted data to <code>.geojson</code> so that we do the format conversion in one step and end up with a portable reusable geospatial file for later geospatial focused tasks.</p>
<p>We can go further and instead of saving to a file, we can save the data to a database like PostGIS. That's not covered here, but stay tuned.</p>
<h2>Geospatial data</h2>
<p>Back to our Strava data.</p>
<ol>
<li>Get the coordinate points</li>
</ol>
<p>This <code>summary_polyline</code> string is a Google Maps encoded Polyline, a proprietary format.</p>
<p>We will need to decode this using Shapely, because we want the coordinate points.</p>
<ol start="2">
<li>Convert them to the right coordinate reference system</li>
</ol>
<p>We need to understand what cartographic system did they originally use in their encoding. I'll spare you the head scratching, Google Maps uses Mercator system or EPSG:3857.</p>
<p>Best way to imagine the CRS is to imagine yourself in space, floating about. Now imagine that your friend is also there with you, floating around like a maniac. Great! Now I ask that both you and your friend look down at the Eifel tower, manifique!</p>
<p>You and your friend will be looking at the same coordinate (lat,lng), but you each have a different angle to that coordinate. As such you each have your or CRS.</p>
<p>We do this crs conversion via GeoPandas.</p>
<ol start="3">
<li>Working with GeoPandas</li>
</ol>
<p>When we use GeoPandas we need to also deal with how it expects coordinates. By default GeoPandas expects a crs called WGS84 or EPSG:4326.</p>
<p>When we load the data from the original Polyline conversion we need to load it as WGS84 and then change the crs to Mercator.</p>
<p>But we're not done quite yet. You see that WGS84 crs is very important for geographic operations inside GeoPandas. Mapping, distance, comparisons are all done in the WGS84 crs.</p>
<p>Before we start doing these geographic operations we need to make sure that we convert back from Mercator to WGS84.</p>
<p>What happens if we load the data as WSG84?</p>
<p>Try it and see (once you set up the mapping of the data).</p>
<p>If you want to take my word for it, the positions are inaccurate. If you have a clear start and end point they will be shifted by a few meters.</p>
<ol start="4">
<li>Correct format for lat,lng</li>
</ol>
<p>Each mapping technology will have their own system, Folium for example requires (lat, lng). The normal format.</p>
<p>Others require the 'goofy' format (lng, lat), looking at you GeoPandas and Google Maps. There may be a valid reason, but that is smaller that my ignorance atm.</p>
<p>For the record when geospatial data is inside GeoPandas it's usually (lng, lat), so remember to do a switch before you try to map the data.</p>
<p>What happens if you don't?</p>
<p>Northwest England coordinates, get mapped in the Bay of Mogadishu, Somalia. I guess you'd call it the other side of the world somehow.</p>
<ol start="5">
<li>Polygons</li>
</ol>
<p>This is where we leverage some of these coordinates to draw a shape around them.</p>
<p>Using these Polygons we can then make filters for calls to other geospatial services like OpenStreetMaps</p>
<h2>OpenStreetMaps</h2>
<p>Still very much a black hole for me, but at the core we want to map our coordinates line to an OpenStreetMap map.</p>
<p>We could be naive and just get the whole map, but that's a huge amount of data.</p>
<p>Instead we use our coordinates to create a polygon, and we filter the OSM data with that polygon. This is where depending on the situation you might have to add borders to your polygon.</p>
<p>Worth noting that this step is incredible fiddly because all OSMnx requests are made via a limited open-source api. Even if it weren't limit, hammering the api would be akin to kicking puppies. Don't do it!</p>
<p>The adjustment of the polygon thus makes sure that we only query what is necessary.</p>
<p>What we get from these queries is the <code>graph</code>.</p>
<p>In order to tell OSM what to put in the graph we can specify the network types: road, walk, cycling etc. As well as other filters.</p>
<h2>Mapping with Folium</h2>
<p>Just a remind we're aiming to map the Strava activity onto an OpenStreetMap graph of nodes.</p>
<p>Folium will:</p>
<ul>
<li>take the route DataFrame, and the OSM graph</li>
<li>use the route to define the center of the map</li>
<li>use the graph to define the edges of the map</li>
<li>then plot the route as a <code>folium.PolyLine()</code></li>
<li>then plot the route start and end as <code>folium.Marker()</code></li>
</ul>
<h2>Pulling it together</h2>
<p>In conclusion, to free our Strava data we need to:</p>
<ul>
<li>Read the Google Maps polyline data and convert to (lat,lng)</li>
<li>Remap that coordinate line to the correct CRS and regular or goofy (lat, lng) pairs</li>
<li>Create a polygon from the coordinates</li>
<li>Use the polygon to extract data from OpenStreetMap into a graph</li>
<li>Generate a map from the coordinates and the graph</li>
</ul>
<p>As a final touch we output the map to an html file, which you can see <a href="https://alanionita.github.io/strava-to-foss-notebooks-python/">here</a></p>
<p>This is just scratching the surface of what is possible, and I strongly recommend you give it a try.</p>
<p>For the next iteration, I'll be fetching all my Strava data, converting it to geospatial, and save it inside a database.</p>
<p>Final result</p>
<p>https://alanionita.github.io/images/posts/strava-to-foss/output.png</p>

                ]]>
            </description>
        </item><item>
            <title>One Big Text File (obtf)</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-03-03-obtf</link>
            <pubDate>Mon, 03 Mar 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>One Big Text File (obtf)</h1>
<p>Learning from <a href="https://ellanew.com/tagged/obtf">Ellane W</a>, I started journaling inside a "one big text file".</p>
<h2>Context</h2>
<p>My note-taking system is text based, predominantly Markdown files, structure using the PARA system:</p>
<pre><code class="language-md">
| Category      | File           |
| ------------- | -------------- |
| Projects      | project.*.md   |
| Area          | area.*.md      |
| Reference     | reference.*.md |
| Archive       | arch.*.md      |
| Journal       | journal.*.md   |

</code></pre>
<p>Project - Most large chunks of work go into a project, if it's not big enough for a project, it usually goes in the other bins. Recently I added yearly sub-scoping <code>project.2025.*.md</code>. eg project.2025.portfolio-svelte.md, project.2025.kafka-pipeline.md, etc. This helps with yearly reviews, when I can easily <code>ls project.2025*</code> to get all the in progress / complete projects in a year.</p>
<p>Archive - When I no longer need a note, it goes into archives. Over time the aim is to summarise the archives and discard the notes. Holding onto notes seems redundant. They should be distilled into "thoughts" or "timelines" for each topic, reflecting your current take on each topic.</p>
<p>Area - Meant to keep track of various parts of life. eg area.personal.md, area.work.md, area.sport.md etc. Vastly under-used at the minute within my system.</p>
<p>References - Used to store links and useful ideas on specific topics. In my case it's full of reference scoped by tech eg. reference.docker.md, reference.sql.md, reference.nextjs.md. Again vastly under-used at the minute.</p>
<p>Journal - Stores daily, quarterly, yearly notes, and this is where most of the note-taking happens. The ideal workflow is: jot down notes within a <code>journal.*.md</code> note and at the end of day, review, summarise, and update <code>area.*.md</code> or <code>resource.*.md</code>. In reality it's just a dumping ground.</p>
<p>All the notes are stored within a cloud-hosted git repo.</p>
<h2>The problem</h2>
<p>In 2025 I wrote ~500 daily note files, most of which are scoped under <code>journal.daily.*.md</code> leaves.</p>
<p>My lax approach to reviewing the notes, means that whenever I review the journal entries, the queue is full with dozens of files to peruse and summarise into a weekly / quarterly superset.</p>
<p>Moving files into <code>archive.*</code> is not easy unless you automate it with the script below.</p>
<h2>Solution</h2>
<p>Instead of ~500 leaves of <code>journal.daily.*.md</code> files have one yearly-scoped journal file <code>journal.2025.md</code>.</p>
<p>Each note entry uses a key prefix, heavily inspired by Ellane W. suggestions. A blend of bullet journalling and PARA structure.</p>
<pre><code class="language-md">T. Task
X. Task done
N. Note
P. Project
R. Reference
</code></pre>
<p>Entries all encapsulated under a day heading. New calendar entries are placed at the top of the file.</p>
<pre><code class="language-md"># 04-03-2025, Tuesday
...

# 03-03-2025, Monday

...
</code></pre>
<h2>Observations</h2>
<p>The "obtf" is already making reviews easy, highlighting past days with a simple scroll vs opening various files.</p>
<p>Using one big text file means that I use less <code>fzf</code> scripts or <code>grep -nr "term" .</code>.</p>
<p>The new journal opens in vim via terminal alias <code>,journal</code>, making for some wicked fast scribbles.</p>

                ]]>
            </description>
        </item><item>
            <title>Kafka and Elastic, too complex in 2025?</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-21-text-search-foss</link>
            <pubDate>Fri, 21 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Kafka and Elastic, too complex in 2025?</h1>
<p>Apache Kafka for example has an air of complexity and flamboyance around it.</p>
<p>A vast majority of Hacker News comments rag on Kafka, the latest comments making it sound like a "CV-padding technology, needlessly complex, overtly unnecessary".</p>
<p>Similar myths around Elastic Search: complex, hard to maintain, expensive.</p>
<p>If I were too include languages here, Haskell would be the top contender: considered too academic, impractical, obtuse, and a full stop questionable choice.</p>
<p>What is the real damage of these myths?</p>
<p>Because of the grapevine new people don't use Kafka, new people avoid Elastic Search.</p>
<p>This activates a scenario where the hard tools remain hard because new people can't shape their structure and usage. Without new users the tooling teams can't understand the new UX problems they need to solve.</p>
<p>More importantly where are these people to going for replacements?</p>
<p>For Kafka replacements, people go to the cloud. They end up using AWS Kinesis streams, or AWS EventBridge - both expensive and complex services. Arguably as complex as Kafka and closed source.</p>
<p>For Elastic Search replacements, people go to the cloud again, to database provider services like MongoDB Atlas, or to AWS OpenSearch. Both of these options are costly and just as complex.</p>
<p>Cost in itself should be a criteria for businesses, if a managed service costs but is more easy to manage then we need to asses the trade-off. Sometimes however the costs act as feature gates and the managed services are just as hard to maintain or grok.</p>
<p>For individuals, cost is a barrier, no doubt about it. Yet, despite this barrier, the industry wants developers who can build real-time systems or complex text-search.</p>
<p>Where and how do we get these new developers? Where and how do they gain this experience?</p>
<p>Cloud services, costs, and cost-gating features limits the market from gaining new developers, and limits developers from learning these tools.</p>
<h2>Kafka and the Apache stack</h2>
<p>Apache Kafka for the most part is FOSS. It has for-cost closed source components and services, but the basic streaming is there. Kafka can also run in a homelab and is not resource hungry.</p>
<p>For the next few days I plan to post further updates on this: Kafka for a time-series data ingestion pipeline.</p>
<p>In other words a "soft real-time" problem.</p>
<h2>Elastic Search</h2>
<p>In 2020 I used a cloud managed Elastic Search service through AWS. This was before their business bust up.</p>
<p>At the time, it wasn't considered a good architecture option internally, because of costs. Maintenance was also a factor.</p>
<p>Our challenge was to build real-time VOIP telecom systems, transcribe the call, and make it available within a UI. This is a "hard real-time problem", since all data goals must be met.</p>
<p>Elastic Search allowed us to achieve transcription, indexing, and retrieval, in less than 200ms. With networking done over WebSockets, in a event-driven system.</p>
<p>Despite the results, Elastic Search wasn't recommended because of costs and maintenance.</p>
<p>On management, our index required used 3 shards, and a fairly high spec set of machines. No where near as complex as it could get; a baby Elastic Search setup.</p>
<p>Fast forward to 2025 and using Elastic Search a vastly improved experience, having managed to run a node with 1G memory, bulk index in &#x3C; 30s, and make text search calls with &#x3C;200ms retrieval. Not the largest index in the world, but I can tell that even with 1G limitation it can take more documents and I don't expect the retrieval to be impacted.</p>
<p>Elastic Search is FOSS, configuration was straightforward, and the query experience is modern and straightforward - GraphQL-like in a way.</p>
<h2>Summary</h2>
<p>Without Elastic Search, how else can you build text search systems at home on a Raspberry Pi?!</p>
<p>Without Kafka how else can you ingest time-series data from MCUs or replicate AWS Kinesis and AWS EventBridge services?</p>
<p>Both are important tools for modern software development, and should be on everyone's radar who wants design data-intensive apps.</p>
<p>Most importantly, they are FOSS and local, and grant you full control.</p>
<p>Just add a pinch of energy and time investment.</p>

                ]]>
            </description>
        </item><item>
            <title>SveleteKit - reactivity quirk</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-14-sveltekit-reactivity</link>
            <pubDate>Fri, 14 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>SveleteKit, a reactivity quirk</h1>
<p>Svelte is great for heavy web scenarios: maps, visualizations, animations etc. And it's common that most of these scenarios depend on Web API.</p>
<p>Most of the times we use the <code>$app/environment</code> browser designation to allow for server process to fully complete, and then execute the Web API components, as gate by <code>browser</code> designation.</p>
<p>eg. Map example</p>
<pre><code class="language-ts">&#x3C;script lang="ts">
	import { browser } from '$app/environment';
	import Map from '$lib/components/map.svelte';
&#x3C;/script>


{#if !browser}
	&#x3C;h2>Loading map...&#x3C;/h2>
{:else}
	&#x3C;Map bind:lat bind:lng />
{/if}
</code></pre>
<p>In Svelte this behavior works correctly to bypass any server process required. Ultimately this is a premature optimization in Svelte because it is largely browser-dependent.</p>
<p>From here on you may notice that Svelte and SvelteKit are used interchangeably. Svelte mentions above are the only place where they can ONLY mean the Svelte frontend library.</p>
<h2>Sveltekit</h2>
<p>To expand on the Map example, it's common that Map libraries include <code>IntersectionObservers</code>, or <code>ResizeObservers</code>, or other Web APIs required for complex interaction.</p>
<p>The above example of browser gating will fail when implemented within a SvelteKit application, usually with the error that <code>X API cannot be found</code>, causing confusion.</p>
<p>Why is a browser gated piece of code still running on the server? Who is running? These are questions I've yet to answer.</p>
<p>However there is a way to avoid this behavior and bypass the errors.</p>
<h2>Svelte, Page options</h2>
<p>Official documentation on page options -> https://svelte.dev/docs/kit/page-options</p>
<blockquote>
<p>You can control each of these on a page-by-page basis by exporting options from +page.js or +page.server.js, or for groups of pages using a shared +layout.js or +layout.server.js.</p>
</blockquote>
<p>Within that +layout.ts file we need to focus on 2 page options specifically:</p>
<ul>
<li>ssr</li>
<li>csr</li>
</ul>
<h2>Page options, ssr</h2>
<blockquote>
<p>Normally, SvelteKit renders your page on the server first and sends that HTML to the client where it’s hydrated. If you set ssr to false, it renders an empty ‘shell’ page instead. This is useful if your page is unable to be rendered on the server (because you use browser-only globals like document for example), but in most situations it’s not recommended (see appendix).</p>
</blockquote>
<p>Turning this option off does indeed fix the problem of browser-dependencies being run outside of a browser gate.</p>
<pre><code class="language-ts">// +layout.ts
export const ssr = false;

</code></pre>
<p>But the following notice in the docs sounds concerning and mysterious.</p>
<blockquote>
<p>Even with ssr set to false, code that relies on browser APIs should be imported in your +page.svelte or +layout.svelte file instead. This is because page options can be overridden and need to be evaluated by importing your +page.js or +layout.js file on the server (if you have a runtime) or at build time (in case of prerendering).</p>
</blockquote>
<p>What does importing mean here? In the majority of cases where this becomes a problem, we experience the problem when importing from an external module, which depends on Web APIs within.</p>
<p>Map example again: lib/components/map.svelte</p>
<pre><code class="language-ts">// Map.svelte
&#x3C;script lang="ts">
	import { onMount, onDestroy } from 'svelte';
	import { browser } from '$app/environment';
	import WebMap from '@arcgis/core/WebMap';
	import MapView from '@arcgis/core/views/MapView';

	let mapView: MapView | undefined = $state();
	let mapContainer: HTMLDivElement | undefined = $state();
    	let { lng = $bindable(), lat = $bindable() } = $props();

	onMount(() => {
		// Initialize the map only in the browser
		mapView = new MapView({
			container: mapContainer
			center: [lng, lat]
		})
		
	});

	onDestroy(() => {
		if (mapView) {
			mapView.destroy();
		}
	});
&#x3C;/script>

{#if !browser}
    &#x3C;p>Loading map...&#x3C;/p>
{:else}
    &#x3C;div bind:this={mapContainer} class="arcgic-map">&#x3C;/div>
{/if}

</code></pre>
<p>This example will trigger a <code>ResizeObserver</code> error, even though we have gated the code within the browser.</p>
<p>The errors is triggered within an ARCGIS SDK library.</p>
<p>Again what does importing mean here? We have technically imported the code and gated it correctly. And since the Map component is imported by the <code>+page.svelte</code> that should pass the criteria outlined.</p>
<p>What else is needed?</p>
<p>Do we have to import the offending API itself? Surely not, since that would also require up to shadow the existing Web API (when available) with an manually imported one.</p>
<h2>Page options, csr</h2>
<blockquote>
<p>Ordinarily, SvelteKit hydrates your server-rendered HTML into an interactive client-side-rendered (CSR) page. Some pages don’t require JavaScript at all — many blog posts and ‘about’ pages fall into this category. In these cases you can disable CSR:</p>
</blockquote>
<p>This setting is not technically a requirement, but is a bit of configuration that translates what we want to expect: we want the Map page to always run Javascript because it contains an external module that has it's own features and requirements.</p>
<pre><code class="language-ts">// +layout.ts
export const ssr = false;
export const csr = true; 
</code></pre>
<h2>The quirk</h2>
<p>This is a counter intuitive naming. Although it does configure pre-rendering, the purpose for using it is nuanced.</p>
<p>In our case we can't pre-render a Map page, since they usually involved regular requests for: loading, zooming, fetching tiles etc. All managed by external libraries.</p>
<p>But we do want to pre-render the page itself.</p>
<p>Map pages are usually a code injection scenario: we inject our dynamic element into a static HTML. This is a one-way binding, in other words we inject the map and only interact with it there on.</p>
<p>Sometimes we may have a dynamic element that is external to the map and is tied to it in some way: a filter that changes as the map changes, a banner that updates as we move the map, a sector select that moves the map as we change sectors. All of which are still code injection scenarios, with 2-way bind.</p>
<p>Both scenarios are compatible with pre-rendering: since we only want to route to be pre-rendered, and the html by proxy.</p>
<p>Routes with pre-render true are excluded from manifests, so our server is tiny.</p>
<p>In my use case I want dynamic Maps within static files, no servers involved.</p>
<p>All routes are default <code>prerendered = true</code> and the Svelte <code>adapter-static</code> is used.</p>
<pre><code class="language-ts">// +layout.ts
export const ssr = false;
export const csr = true;
export const prerender = true;
</code></pre>
<p>Of note pre-render can also be 'auto', where you want popular routes to be pre-rendered and historic content to be server-rendered.</p>
<p>Unclear at what scale this is required, because a server rendering content is probably more maintenance and resource-heavy than fully static build.</p>
<p>The fully static build takes longer to compile, but once deployed it's essentially free. With CI/CD and strong hardware this long compile time is a minor negative point.</p>
<p>Either way, the "auto" option is there.</p>
<p>For larger pre-rendered builds it's important to also consider <code>entries</code>. These need to pre-build to ensure the best pre-rendering performance. I will cover them in a future post.</p>
<h2>Summary</h2>
<p>Strange behavior and even stranger docs, however the configuration above does produce the expected results.</p>
<p>In hindsight I'd expect the handling of CSR and SSR to be more consistent, but with most frameworks preferring 'server' options it's to be expected that CSR behavior is not quite there by default.</p>

                ]]>
            </description>
        </item><item>
            <title>Svelte, v5 Migration</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-13-svelte-5-migration</link>
            <pubDate>Thu, 13 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Svelte, v5 Migration</h1>
<p>Big fan of SvelteJS as framework and in particular the Svelte monolith that is SvelteKit.</p>
<p>I maintain a scaffolding repo which act as a base to most Svelte projects I create. You can see that scaffold <a href="https://github.com/alanionita/scaffold-sveltekit-tailwind-shadcn">here</a>.</p>
<p>As of October 2024 Svelte released Svelte 5, their biggest release yet as covered <a href="https://svelte.dev/blog/svelte-5-is-alive">here</a>, but it brings some significant changes to day to day Svelte writing and patterns.</p>
<p>This article aims to show:</p>
<ul>
<li>How to easily upgrade a SvelteKit 2.0 project using Svelte 4</li>
<li>What the changes are and why they've come about</li>
</ul>
<h2>Upgrading</h2>
<p>Upgrading in Svelte couldn't be easier, because of the <a href="https://svelte.dev/docs/cli/sv-migrate">sv migrate tooling</a>.</p>
<h2>Usage</h2>
<p>Within the repo run <code>npx sv migrate TARGET</code> where target here is svelte-5. Check the sv migrate docs for other available targets.</p>
<pre><code class="language-sh">npx sv migrate svelte-5
</code></pre>
<p>sv migrate will then ask:</p>
<ul>
<li>Ask about current dir, since some of the change will impact a mono-repo</li>
<li>Ask which folders to refactor</li>
<li>Ask if you want to tooling to convert components to new syntax</li>
</ul>
<p>My repo is fairly simple so I went ahead and said yes and selected all the option on the above.</p>
<h2>Changes, packages</h2>
<h3>Package versions</h3>
<p>One thing of note here about <code>sv migrate</code> vs a manual package upgrade is that you don't need to trial a bunch of module versions before you reach the right mix.</p>
<p>Having tried it before I will tell you now that: upgrading all packages to latest will break your build.</p>
<p><code>sv migrate</code> already contains the right module combinations and here are the changes.</p>
<pre><code class="language-md">Updated svelte to ^5.0.0
Updated @sveltejs/kit to ^2.5.27
Updated @sveltejs/vite-plugin-svelte to ^4.0.0
Updated prettier-plugin-svelte to ^3.2.6
Updated eslint-plugin-svelte to ^2.45.1
Updated typescript to ^5.5.0
Updated vite to ^5.4.4
</code></pre>
<p>Latest versions for reference (2025-02-13):</p>
<pre><code class="language-md">svelte                      5.20.0
@sveltejs/kit               2.17.1
@sveltejs/vite-plugin-svelte 5.0.3
prettier-plugin-svelte       3.3.3
eslint-plugin-svelte        2.46.1
typescript                   5.7.3
vite 	                     6.1.0
</code></pre>
<p>As you can see things move fast, and in the 3 months since release we've already had a fair bit of drift. This is mostly fine since Svelte is relatively stable.</p>
<p>Of most concerns is the <code>vite</code> major release of v6, and the large number of minor versions on the svelte packages.</p>
<h3>Vite v5 vs v6</h3>
<p>In my context I'm ok with a lagging codebase and will upgrade whenever crutial.</p>
<p>For anyone else who might be impacted I wanted to summarise what changed in vite v6, Read further in the migration guide, <a href="https://vite.dev/guide/migration.html">https://vite.dev/guide/migration.html</a>.</p>
<p>v6 updates:</p>
<ul>
<li>Environement API refactors; experimental api included which changes to interface; opt-in only</li>
<li>Runtime API: experimental Module Runner API, requires using the new API after updating</li>
<li>resolve.conditions: impacts those with .conditions config; from v6 conditions need to be included manually as oposed to being defaulted, allows ssr and regular configs to be different (whilst before ssr would inherit from regular config), if you have previous config in v6 you need to add the defaults per resolution</li>
<li>JSON.stringify: in v6 json.strigify: true no longer changes json.namedExports: disabled; json.namedExports is respected; strigify also has a new value of auto, which only stringifies large objects.</li>
<li>Larger support for asset references in HTML element: v5 only  and  could reference assets. v6 has an extended list <a href="https://vite.dev/guide/features#html">https://vite.dev/guide/features#html</a></li>
<li>CSS: postcss-load-config updated in v6, sass uses modern api by default, custom CSS output file in library mode</li>
</ul>
<blockquote>
<p>Svelte: because of the volume of releases there's a possibility here that we might be missing key bug fixes.</p>
</blockquote>
<h3>Installing packages</h3>
<p>Now lets install the migrated packages and see what happens.</p>
<p>1 Remove previous installed packages</p>
<pre><code class="language-sh">rm -rf node_modules/ &#x26;&#x26; rm package-lock.json
</code></pre>
<ol start="2">
<li>Install with package of choice (npm)</li>
</ol>
<pre><code class="language-sh">npm install
</code></pre>
<p>Issues:</p>
<ol>
<li><code>@sveltejs/vite-plugin-svelte@^4.0.0</code> not found (see logs below)</li>
<li><code>&#x3C;script lang="ts">&#x3C;/script></code> flagged as an issue with @sveltejs/adapter-auto</li>
<li><code>{@render children?.()}</code> throwing error about unrecognised @ symbol</li>
</ol>
<pre><code class="language-sh">npm error code ERESOLVE
npm error ERESOLVE could not resolve
npm error
npm error While resolving: streak-tracker--svelte@0.0.1
npm error Found: @sveltejs/vite-plugin-svelte@3.1.2
npm error node_modules/@sveltejs/vite-plugin-svelte
npm error   dev @sveltejs/vite-plugin-svelte@"^4.0.0" from the root project
npm error   peer @sveltejs/vite-plugin-svelte@"^3.0.0 || ^4.0.0-next.1" from @sveltejs/kit@2.6.4
npm error   node_modules/@sveltejs/kit
npm error     dev @sveltejs/kit@"^2.5.27" from the root project
npm error     peer @sveltejs/kit@"^2.0.0" from @sveltejs/adapter-auto@3.2.5
npm error     node_modules/@sveltejs/adapter-auto
npm error       dev @sveltejs/adapter-auto@"^3.0.0" from the root project
npm error   1 more (@sveltejs/vite-plugin-svelte-inspector)
npm error
npm error Could not resolve dependency:
npm error dev @sveltejs/vite-plugin-svelte@"^4.0.0" from the root project
npm error
npm error Conflicting peer dependency: svelte@5.20.0
npm error node_modules/svelte
npm error   peer svelte@"^5.0.0-next.96 || ^5.0.0" from @sveltejs/vite-plugin-svelte@4.0.4
npm error   node_modules/@sveltejs/vite-plugin-svelte
npm error     dev @sveltejs/vite-plugin-svelte@"^4.0.0" from the root project
npm error
npm error Fix the upstream dependency conflict, or retry
npm error this command with --force or --legacy-peer-deps
npm error to accept an incorrect (and potentially broken) dependency resolution.
npm error
npm error
npm error For a full report see:

</code></pre>
<h3>Fixing errors</h3>
<ol>
<li>
<p><code>@sveltejs/vite-plugin-svelte</code> is not a real error, but I suspect an issue with timeout during the npm i process; making sure I have a clean install by removing node_modules and package-lock.json allowed me to get a successful install without the error</p>
</li>
<li>
<p>Script tag issue with "@sveltejs/adapter-auto": fixed it by updating the version to ^4.0.0</p>
</li>
<li>
<p>Noticed an issue with tsconfig.json, whereby "moduleResolution" required "module" to be present and set to preserve; after adding that value the IDE refreshed and the @render issue went away</p>
</li>
</ol>
<h2>Changes, components</h2>
<h3>Runes</h3>
<p>Multiple changes stem from the new Runes API: compiler instructions that tell Svelte about reactivity.</p>
<p>Runes start with <code>$</code>.</p>
<h3>let to $props</h3>
<p>In v4 props were achieve using <code>export let xyz = "xyz"</code> declarations. These declarations made content available from <code>&#x3C;script>&#x3C;/script></code> into the main body of the component.</p>
<p>In v5 all props come from the $props rune as seen below:</p>
<pre><code class="language-ts">&#x3C;script>
	let { optional = 'unset', required } = $props();
&#x3C;/script>
</code></pre>
<p>In my case, the app is also using Typescript so there are further changes on the type definitions, which will be covered later.</p>
<p>v5</p>
<pre><code class="language-ts">&#x3C;script lang="ts">
	import type { HTMLAttributes } from "svelte/elements";
	import { cn } from "$lib/shadcn.js";

	type $$Props = HTMLAttributes&#x3C;HTMLDivElement>;

	let className: $$Props["class"] = undefined;
	export { className as class };
&#x3C;/script>

&#x3C;div
	class={cn("bg-card text-card-foreground rounded-lg border shadow-sm", className)}
	{...$$restProps}
>
	&#x3C;slot />
&#x3C;/div>

</code></pre>
<p>v6</p>
<pre><code class="language-ts">&#x3C;script lang="ts">
	import type { HTMLAttributes } from "svelte/elements";
	import { cn } from "$lib/shadcn.js";

	type $$Props = HTMLAttributes&#x3C;HTMLDivElement>;

	interface Props {
		class?: $$Props["class"];
		children?: import('svelte').Snippet;
		[key: string]: any
	}

	let { class: className = undefined, children, ...rest }: Props = $props();
	
&#x3C;/script>

&#x3C;div
	class={cn("bg-card text-card-foreground rounded-lg border shadow-sm", className)}
	{...rest}
>
	{@render children?.()}
&#x3C;/div>
</code></pre>
<blockquote>
<p>Render method no longer compiled for SSR. This is specifically related to SSR application, but worth mentioning.</p>
</blockquote>
<h3>children() and snippets</h3>
<p>In v4 the main paradigm for creating higher order components was to use <code>&#x3C;slots></code>.</p>
<p>In v5 they are deprecated in favour of  constructs which increase the flexibility. Also added in v6 a clearer construct for children.</p>
<p>In my case I've only used  for HOCs and those instances have been refactored to use children instead.</p>
<p>v4</p>
<pre><code class="language-ts">&#x3C;script>
	import '../app.css';
&#x3C;/script>

&#x3C;slot>&#x3C;/slot>
</code></pre>
<p>v5</p>
<pre><code class="language-ts">&#x3C;script lang="ts">
	import '../app.css';
	interface Props {
		children?: import('svelte').Snippet;
	}

	let { children }: Props = $props();
&#x3C;/script>

{@render children?.()}
</code></pre>
<h3>Component typing changes</h3>
<p>Most of the codebase saw change related to the new children component which is typed as Snippet from 'svelte' package.</p>
<p>No other significant changes present, but be advised that there are further changes detailed in the <a href="https://svelte.dev/docs/svelte/v5-migration-guide#Components-are-no-longer-classes-Component-typing-changes">release note</a>.</p>
<h3>let to $state</h3>
<p>Covered here last because I haven't implemented any states within this template.</p>
<p>I can see this being an important change to keep in mind in production apps since the let definition is common.</p>
<p>This essentially brings the biggest headaches, since prior to v5 developers built up this pattern of separation between prop definitions and state. This separation is still present in v5 but the change of let -> $props will confuse readers who will see more let -> $state.</p>
<p>In agreement with the pattern separation seen here, but getting used to it will take some time. The old definitions were worse for sure, but like any pattern development they get ingrained.</p>
<p>v4</p>
<pre><code class="language-ts">&#x3C;script>
	let count = 0;
&#x3C;/script>

</code></pre>
<p>v5</p>
<pre><code class="language-ts">&#x3C;script>
	let count = $state(0);
&#x3C;/script>
</code></pre>
<h2>Summary</h2>
<p>Big update, one of the biggest yet! Largely positive, but with significant gotchas and implementation details.</p>
<p>Overall a move in the right direction, but shifting into the new paradigms will cause some head scratching.</p>

                ]]>
            </description>
        </item><item>
            <title>Flutter, dev environment setup</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-12-flutter-setup</link>
            <pubDate>Wed, 12 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Flutter, dev environment setup</h1>
<p>Initial dive into Flutter for Desktop Linux development.</p>
<p>Starting from scratch, with the below guide, and although I have read about Flutter previously I've not build anything until now. I do have some experience with Android development, but I will actively try to avoid using that experience, probably forgot it anyway.</p>
<p>Going through the install guide found <a href="https://docs.flutter.dev/get-started/install/linux/desktop">here</a></p>
<h2>Install</h2>
<h3>Hardware requirements</h3>
<p>Support for Linux is there and automatically detected by the site. Nice landing page too, but frankly I just skipped to installation - don't need to read the marketing, since I already want to use it.</p>
<p>Hardware requirements are high. On Linux especially, we need to expect lesser performance since we expect folks to use older software.</p>
<p>As an ethos too, Linux enables more usage out of older software so the requirements will end us (the developers) push for hardware updates. To me, hardware updates are fine for commercial software, but for personal software it's just enough friction for someone to stop using your app.</p>
<pre><code class="language-md">| Requirement   | Minimum   | Recommended     |
| ------------- | --------- | --------------- |
| CPU cores     | 4         | 8               |
| Memory        | 8         | 16              |
| Display res   | 1366x768  | 1920x1080       |
| HDD           | 4GB       | 52GB            |
</code></pre>
<p>If you were a retro enthusiast and were working on a Core 2 Duo device you're out of luck. And to add to that, 16GB RAM recommended.</p>
<h3>Software requirements</h3>
<p>All in all 12 environment level dependencies for developing Linux apps.</p>
<p>As always I created a Flutter Ansible playbook to make it easier to quickly setup a new machine.</p>
<pre><code class="language-yaml">- name: Flutter Development Setup
  hosts: my_local
  vars:
    # Useful in case you want to uninstall the whole playbook later
    #    NB: doublecheck that you don't uninstall curl, git - stuff you need
    state: present # or 'absent'
  tasks:
    - name: curl - install
      apt:
        name: curl
        state: '{{state}}'
    - name: git - install
      apt:
        name: git
        state: '{{state}}'
    - name: unzip - install
      apt:
        name: unzip
        state: '{{state}}'
    - name: xz-utils - install
      apt:
        name: xz-utils
        state: '{{state}}'
    - name: zip - install
      apt:
        name: zip
        state: '{{state}}'
    - name: libglu1-mesa - install
      apt:
        name: libglu1-mesa
        state: '{{state}}'
    - name: clang - install
      become: true
      apt:
        name: clang
        state: '{{state}}'
    - name: cmake - install
      become: true
      apt:
        name: cmake
        state: '{{state}}'
    - name: ninja-build - install
      become: true
      apt:
        name: ninja-build
        state: '{{state}}'
    - name: pkg-config - install
      apt:
        name: pkg-config
        state: '{{state}}'
    - name: libglu1-mesa - install
      apt:
        name: libglu1-mesa
        state: '{{state}}'
    - name: libgtk-3-dev - install
      become: true
      apt:
        name: libgtk-3-dev
        state: '{{state}}'
    - name: liblzma-dev - install
      become: true
      apt:
        name: liblzma-dev
        state: '{{state}}'
    - name: libstdc++-12-dev - install
      become: true
      apt:
        name: libstdc++-12-dev
        state: '{{state}}'

</code></pre>
<h3>Configure text editor</h3>
<p>In VS Code install Flutter extension. Recommended that you install VS Code version > 1.86.</p>
<p>The correct extension is published by Dart Code.</p>
<h3>Install Flutter SDK</h3>
<p>There's a manual tar download option, but in this case I will use VS Code command palette and follow the wizard.</p>
<h3>Create the project</h3>
<p>From command palette create a new Application and wait until it starts.</p>
<p>Very slow on the Thinkpad T480 laptop, but ultimately created a functional app.</p>
<p>Test the installation of Flutter using the <code>flutter doctor</code> command within VS Code terminal.</p>
<h2>Start developing apps</h2>
<p>Use the following guides to continue learning about development with Flutter.</p>
<ol>
<li>
<p><a href="https://docs.flutter.dev/get-started/codelab/">Learn how to write first Flutter app</a></p>
</li>
<li>
<p><a href="https://docs.flutter.dev/get-started/fundamentals/">Flutter fundamentals docs</a></p>
</li>
<li>
<p><a href="https://docs.flutter.dev/platform-integration/linux/building">Build Linux apps with Flutter</a></p>
</li>
<li>
<p>Seems to be a lengthy video tutorial that is cross platform on Desktop and Web</p>
</li>
<li>
<p>Seems like a better outline, but still not clear</p>
</li>
</ol>
<p>One surprising thing to notice is that the Linux development side of Flutter is using C. A positive surprise, but unexpected. I thought I'd still be dealing with Dart.</p>
<h2>Summary</h2>
<p>Relatively straightforward install, but the development guide itself for Linux is lacking. Lots of piecing together, but seems interesting to learn about app development using C.</p>
<p>Will push on with this further and aim to build an RSS app for Linux.</p>

                ]]>
            </description>
        </item><item>
            <title>W3C, easy checks for accessibility</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-10-checks-for-accessibility</link>
            <pubDate>Mon, 10 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>W3C, easy checks for accessibility</h1>
<p>Accessibility is important and has been since working at <a href="https://www.em-code.com/">Code Computer Love</a>, a design and development agency from Manchester known for top notch Design and UX research.</p>
<p>Here is a rundown of the <a href="https://www.w3.org/WAI/test-evaluate/preliminary/">W3C - preliminary check</a> as applied to my new portfolio redesign.</p>
<h2>Title page</h2>
<p>Test:</p>
<pre><code class="language-md">[ ] each page contains a title that is visible in the browser
</code></pre>
<p>Test notes:</p>
<pre><code>- Confirmed
</code></pre>
<p>Implement:</p>
<ul>
<li>Create a dynamic title component and load it into each page.</li>
<li>This will allow for a dynamic title that always includes "Alan Ionita - " for search ranking purposes</li>
</ul>
<pre><code class="language-js">// PageTitle component

&#x3C;script>
    const siteName = "Alan Ionita"
    let props = $props();
&#x3C;/script>

&#x3C;svelte:head>
	&#x3C;title>{siteName + " - " + props.text}&#x3C;/title>
&#x3C;/svelte:head>

</code></pre>
<pre><code class="language-js">// *Page component
import PageTitle from '...'

&#x3C;PageTitle text="Each page title" />

</code></pre>
<h2>Page headings</h2>
<p>Test:</p>
<pre><code class="language-md">[ ] The page has a heading. In almost all pages there should be at least one heading.
[ ] All text that looks like a heading is marked up as a heading.
[ ] All text that is marked up as a heading is really a conceptual section heading.
[ ] The heading hierarchy is meaningful. 
    - Ideally the page starts with an "h1", which is usually similar to the page title
</code></pre>
<p>Test notes:</p>
<pre><code class="language-md">HomePage: 
- hierarchy is a bit odd with only "Alan Ionita," being marked up as h1
    - the whole section should be an h1 really
- rest of the hierarchy makes sense
- html structure for each section seems a bit overkill; too many nested elements

BlogPage: 
- headings are good

BlogPostPage: 
- markdown to html headings converted correctly
- needed to correct the source markdown to make sure that each post had an h1
</code></pre>
<h2>Contrast ratio</h2>
<p>Test:</p>
<pre><code class="language-md">[ ] minimum contrast: a contrast ratio of at least 4.5:1 for normal-size text.
[ ] provide support for high contrast
</code></pre>
<p>Test notes:</p>
<pre><code class="language-md">BlogPostPage: 
- starting here, because it's the page with most content
- core font is rated as AAA compliant with a contrast of 6.77

BlogPage: 
- h1 is compliant to AAA (6.77)
- blog list main text is AA (3.09) 
- sub-text AA (3.84)

HomePage
- orange section is AA
- rest is AAA
- same scores as above

Nav: 
- not-visited links are not compliant (2.9)
</code></pre>
<p>Implement:</p>
<ul>
<li>Find colour variant for orange to get to AAA adherence</li>
<li>Missing support for "prefers-contrast" option, high contrast solution for users that need it. Will require some research on best colours to use to achieve a high contrast whilst still preserving the theme, as a fallback will just default to black,yellow combination</li>
<li><code>prefers-contrast</code> further reading <a href="https://hacks.mozilla.org/2020/07/adding-prefers-contrast-to-firefox/">here</a></li>
</ul>
<h3>Check contrast</h3>
<p>My preference is to use Firefox for personal development and will not cover Chrome.</p>
<ol>
<li>
<p>Inspect text element</p>
</li>
<li>
<p>Inspector: verify the correct html element is selected</p>
</li>
<li>
<p>Inspector: in the Styles side panel find the class that provides the color css</p>
</li>
<li>
<p>Inspector / Styles: click on the color round icon</p>
</li>
<li>
<p>Inspector / Styles: check the contrast in the pop-up</p>
</li>
</ol>
<h2>Text resize</h2>
<p>Test:</p>
<pre><code class="language-md">- [ ] make sure that resizing text doesn't cause page overflows, or stop interactive elements from being accessible
</code></pre>
<p>Test notes:</p>
<pre><code class="language-md">- Overflowing was causing the Nav to no longer be accessible, so made a change to fix that.
- All other elements and pages are passing.
</code></pre>
<h2>Keyboard, and visual focus</h2>
<p>Test:</p>
<pre><code class="language-md">- [ ] test keyboard nav and visual focus
</code></pre>
<p>Test notes:</p>
<pre><code class="language-md">
HomePage: 
- keyboard navigation works correctly across links
- ideally the navigation should cover all the sections too
- will require a refactor to improve the page

BlogPage: 
- works as expected since the list is correctly styled as a list

BlogPostPage: 
- correctly navigates through links, but this is correct behavior here

- Tab all, Tab away, Tab order is fine
- All functionality works by keyboard
- Admittedly it's a very simple site with a low number of features
- Dropdown list navigation and image links considerations are not applicable here
</code></pre>
<p>Implement:</p>
<ul>
<li>Visual focus styling is a visually weak, somewhat clashing with the theme; will require a refactor</li>
</ul>
<h2>Forms, labels, and errors</h2>
<p>Not applicable here</p>
<h2>Moving, or Flashing Content</h2>
<p>Test notes:</p>
<ul>
<li>Since the site is a static site, there's no perceivable load or layout shift.</li>
<li>The only thing of note here is the transition from <code>/</code> to <code>/blog</code>, where <code>/</code> is a dark colour and <code>/blog</code> is a lighter colour. Here the design is such that on <code>/blog</code> there's colour degradation from top to bottom of page so the user is gradually moving from dark colours to lighter colours.</li>
</ul>
<h2>Multimedia alternatives</h2>
<p>Not applicable</p>
<h2>Basic structure check</h2>
<p>Test:</p>
<pre><code class="language-md">[ ] Turn off images and show the text alternatives.
[ ] Turn off style sheets (CSS), which specifies how the page is displayed with layout, colors, etc.
[ ] Linearize the page or the tables (depending on the toolbar).
</code></pre>
<p>Test notes:</p>
<pre><code class="language-md">HomePage: 
- images are not applicable, CSS off looks ok
- already a fairly linear layout so working as expected 
- nav is off to right, should fix

BlogPage: 
- same nav issue, main text ok
- post list is completely white and invisible, should fix

BlogPostPage: 
- passing, same nav issue 

+ Both issues were down to poor testing, see the below script to correctly run the test
</code></pre>
<h3>Disabling CSS stylesheets</h3>
<p>Run the following in the Developer Tools Console</p>
<pre><code class="language-js">document.querySelectorAll('style, link[rel="stylesheet"]').forEach(e => e.remove());
</code></pre>
<h2>Summary</h2>
<p>With the exception of the orange colour and the visual focus, we have a AAA compliant website.</p>

                ]]>
            </description>
        </item><item>
            <title>Timeline apps, a timeline</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-09-timeline-apps</link>
            <pubDate>Sun, 09 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Timeline apps, a timeline</h1>
<p>Social Media is a disaster. Under the guise of connecting with friends and belonging to a community there's just one driver: profit.</p>
<p>And in that chase for profit we have lost out on core hygiene elements that platforms should consider:</p>
<ul>
<li>moderation</li>
<li>stability</li>
<li>access</li>
<li>user control</li>
</ul>
<p>The future however might not be so bleak because of the increasing popularity of "timeline apps". To understand them I'd like to set the scene for the trend and them explain the apps themselves.</p>
<h2>Advertising</h2>
<p>Social Media advertising spend in 2024 was $164bil [*1] globally, but after digging into the data and predictions it seems to be designed by hockey stick hopefuls not real analysts.</p>
<p>In the UK social media paid ads are classified under "Display" ads, and UK digital advertising industry body (IAB.UK) shows that in 2013, Display ads were worth 1.89 billion pounds [*2]. In 2024 the revenue [*3] from Display ads went up to 6.5 billion pounds.</p>
<p>Nice growth you'd say? Sure, "+22% YoY" the IAB.UK quotes. The interesting thing however is how that money is split: video and non-video. Non-video is social media and other ads, which in 2024 amounted to 2.5 billion pounds.</p>
<p>Anecdotally I remember social being up to 11% in 2013, so 0.207 billion pounds vs 2.5 billion pounds. 12x increase in 11 years. Again not bad!</p>
<p>The driver for the ad industry is the not necessarily consistent return, they're not pension funds. The ad industry "rides the wave" and so they need to follow and the hottest trends and deploy capital accordingly. The driver is maximum return on investment, and max ROI is gained from deploying capital with a weight towards top performing segments.</p>
<p>In 2024 that segment was not social, it was digital video. And by a long margin 2x. As an ad agent you cannot pass on that opportunity.</p>
<p>Which leaves social advertising with a massive deficit in capital deployment: they will get at minimum 1/3 of ad spends.</p>
<p>Social advertising is not making money where "making" is the exponential scale that most business crave for.</p>
<h2>The past, RSS</h2>
<p>Social media advertising is generally in decline today, mostly because of the financial view explained above and because of other smaller platform related issues.</p>
<p>In 2013 social media ads were growing and performing well. In 2011 Google launched Google Plus+ and by 2013 the performance of Google Plus+, social advertising meant that Google decided to kill one of their most loved products, Google Reader.</p>
<p>Google Reader was built on top of RSS, a protocol that stands for Really Simple Syndication. RSS allows a website to define what updates it has in the form of a feed. RSS Readers, like Google Reader would use these feeds to produce a custom feed for a user in a nice web interface.</p>
<p>Google Reader was nice and was the first "timeline app".</p>
<h2>Today, siloed apps</h2>
<p>Today the big social media platforms are in trouble. They can't make revenue and most are adopting dark patterns to force us to share more details with a platform, whilst implementing harder and harder journey for exporting data and deleting accounts. A Facebook was notoriously hard to delete in 2013-2014, which is why people moved to Twitter.</p>
<p>Twitter, which started with a historical timeline, eventually moved to and "algorithm timeline" and pushed a lot of users and businesses to create solutions for this. <a href="https://en.wikipedia.org/wiki/TweetDeck">TweetDeck</a> was one such solutions and Twitter was relentless in making API changes that affected consumers like TweetDeck. Eventually Twitter bought TweetDeck and is not part of X Pro.</p>
<p>There are endless of other stories regarding social media companies acting like cartels and protecting the data within their platform and making it as painful as possible for anyone to development improved UIs that accessed that data. The reason is advertising.</p>
<p>RSS however bypasses that model and most websites on the web have one. RSS feeds are particularly popular on the IndieWeb, but also podcasts have RSS feeds, Youtube channels have RSS, even the new generation of federate social media has them Mastodon and it's famous cousin Bluesky.</p>
<p>Timeline apps use RSS to pull all this information into one UI.</p>
<p><a href="https://apps.apple.com/gb/app/feeeed-rss-reader-and-more/id1600187490">feeeed</a> for example is a fantastic app for a custom timeline. In addition to news, you can add any website or newsletter and you can even connect to Health  track your walking goals in the app. Happy to report that no ads are present yet.</p>
<p><a href="https://apps.apple.com/us/app/tapestry-by-iconfactory/id6448078074">Tapestry</a> is another good timeline app, but they've launched with ads baked in.</p>
<p>Whilst there are more examples out there, these above are the "latest" incarnation of the timeline app. Sure we still have tradition RSS readers which function similarly, but the slick UI and extra focus on the timeline is not there.</p>
<p>Timeline apps will more of a focus in the future, with more people choosing them other a locked in platform. Facebook for example is already moving towards an open platform with Threads and Instagram, but it remains to be seen if they will continue or lock everything down.</p>
<p>Should we have ads in timeline apps? In my honest opinion, no. The user space is more private and more self-aware, so ads should fade to subscriptions or donations.</p>
<p>We must learn from the lessons of the 2013-2024 social web and keep our data. Media empires should not be built on top our data, but rather small apps should be supported because they give us value with being absolutely extractive.</p>
<p>Give timeline apps a try and better still, go and make one!</p>
<p>References:</p>
<ul>
<li>
<ol>
<li>Social media advertising spend in 2024, <a href="https://www.statista.com/statistics/271406/advertising-revenue-of-social-networks-worldwide/">link</a></li>
</ol>
</li>
<li>
<ol start="2">
<li>IAB.UK 2013 advertising budgets, <a href="https://www.iabuk.com/sites/default/files/research-docs/IAB_PWC_AdspendOnePager_2013.pdf">link</a></li>
</ol>
</li>
<li>
<ol start="3">
<li>IAB.UK 2024 advertising budgets, <a href="https://www.iabuk.com/sites/default/files/public_files/UK%20Digital%20Adspend%20H1%202024%20Update.pdf">link</a></li>
</ol>
</li>
</ul>

                ]]>
            </description>
        </item><item>
            <title>Thinkpad T480, screen flicker fix</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-07-t480-screen-flicker</link>
            <pubDate>Fri, 07 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Thinkpad T480, screen flicker fix</h1>
<p>Linux Mint 22.1 on the T480 causes screen flickering.</p>
<p>To replicate:</p>
<ul>
<li>check the login screen once the laptop has come out of suspend</li>
<li>or open a dock applet</li>
<li>hold the mouse on on a dropdown for a long time.</li>
</ul>
<p>Issue seems to be related to transparency and screen sleep, in X11.</p>
<h2>Fix</h2>
<h3>mesa-utils</h3>
<p>Check that <code>mesa-utils</code> package is installed</p>
<pre><code class="language-sh">apt list | grep "mesa-utils"
</code></pre>
<p>Install the package if not present</p>
<h3>X11 config</h3>
<p>Open the X11 config directory</p>
<pre><code class="language-sh">cd /etc/X11/xorg.conf.d
</code></pre>
<p>Create the <code>20-intel.conf</code> within the above directory, with the following contents</p>
<pre><code class="language-sh">Section "Device"
 Identifier "Intel Graphics"
 Driver "Intel"
 Option "AccelMethod" "sna"
 Option "TearFree" "true"
EndSection
</code></pre>
<h3>Reboot</h3>
<hr></hr>
<blockquote>
<p>You can also fix it by installing KDE Plasma :P /s</p>
</blockquote>

                ]]>
            </description>
        </item><item>
            <title>A computer for life</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2025-02-07-computer-for-life</link>
            <pubDate>Fri, 07 Feb 2025 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>A computer for life</h1>
<p>It's easy to think about computers today as always present on our person, the smartphone. They're useful for many things, but addictive in equal measures.</p>
<p>Strangely alonside this new addiction side-effect computers today have another side-effect we don't notice: price.</p>
<p>Price is a proxy for value and if we get more value I'm all for increased price. Sadly the trend today is to get less value, less use out of a computer that somehow asks for a higher price.</p>
<p>AI has now made this worse. As an industry AI is vastly a service business, rent what compute you need vs a DIY gig, and so it follows that computers with AI cost more.</p>
<p>But they don't just have a higher outlay, they also have a higher recurring expense via subscriptions - a topic I might write on later.</p>
<p>For the sake of the argument I wanted to outline the marketing reasons for computers and why they are unnecesary.</p>
<h2>Design</h2>
<p>Design is valuable and design is important. I'm a huge fan of nice looking and functioning objects, but neither I not Deiter Rams would disagree that good design is a synonim to expensive design.</p>
<p>Soviet design for example is somewhat brutal, stark, bold, yet cheap and easy to mass produce.</p>
<p>Yet today in 2025, computer designs warrant a high price tag.</p>
<p>Why? Because... we buy it and buying gives businesses the signal that this is what we want and that they can charge more.</p>
<h2>Repairability</h2>
<p>We've all had computers fail and require repairs. Repairs are not chear, the computer itself was not cheap and we're constantly worried about what happens with our device once it's at a service. What happens to our data?</p>
<p>Sadly, the newer the computer and the "better" the less repairable it is.</p>
<p>The cause for this is tied to design:</p>
<p>Business wants nicer design -> They posit that "nice" = "small" -> Smaller devices means less space inside for accessible mounts, more use of tape and glue, less room for device saving features like (liquid draining)</p>
<p>But therein lies the problem, a company design is subjective. Over time it can become a universal looks for the object, but initially it's a choice we're given.
So I ask myself why would I want a device that is harder to repair?</p>
<h2>UX</h2>
<p>We all remember the Apple butterfly keyboard incident from a few years back? The summary is that Apple in their chase for smaller, redesign the keyboard switches, the new design felt weird and was prone to dust damage. Yes you read that right, the if tiniest bit of dust would get under the key you're done for.</p>
<p>Yet millions of these buterfly keyboard computers sold at a minimum price of ~800 GBP.</p>
<p>The public will bought them even though you were getting a laptop that was not as nice to use day to day.</p>
<p>The removal of headphone jacks and ports is another UX faux pas, leaving the public stuck with dongles and somehow prices are going up.</p>
<h2>Software</h2>
<p>Windows is the classic culprit for the idea of bad software that sells well. But lately we see more and more companies "skin" the os or loading up new computer with pre-packaged software and in some cases pre-packaged malware.</p>
<p>Apple for example has a trained skill in producing new versions of Mac OS that are locked in step with the newest hardware, leaving a customer with an older device with a noticeably slower device.</p>
<p>They incentives with software is to make more money, whether from forcing more future purchases or from data collection.</p>
<p>Android is no exception to this with Google services being know to send user data to Google in support of their ad delivery platform.</p>
<h2>Today's computer</h2>
<p>In other words today's computer is not a computer for life. We've grown acustomed to believing and buying into the idea that a computer is both a high-price item and an item that you should replace at regular intervals.</p>
<p>A computer is not a stable platform for work, but a source of change because big manufacturers want to experiment, cut manufacturing costs, increase ad revenue.
But to all this negativity there's a potential solution</p>
<h2>Alternatives</h2>
<p>Before I describe the alternatives I need to start with the software. Software is what allows us that stable platform, that universal common denominator across hardware. Although not perfect by any means Linux is the best option we have right now for a stable software base.</p>
<p>Core features like browing the web, word processing, playing games is available across all flavours of Linux and the experience of using 1 flavour vs the other is relatively unchanged.</p>
<p>On the mobile software side iOS is locked and Android is not, so the option is clear. But stock Android comes with Google services so we only have 2 options: either a De-Googled Android, or mobile Linux.</p>
<p>Mobile Linux is sadly still being developed on and is only stable on a handful of phones. You also loose out on any Android apps and have to depend on the browser for banking etc.</p>
<p>Of the De-Googled Android flavours, <a href="https://grapheneos.org/">GrapheneOS</a> is the best example of a simple os that mimicks Android whilst kicking out Google services and still maintaining security.</p>
<hr></hr>
<p>With the software in place we can now consider some hardware.</p>
<p>To a degree the software will have an impact on the hardware. Linux is largely supported, but newer hardware is more likely to cause issues. Equally really old hardware can become unsupported by the kernels.</p>
<p>Thinkpads are by far the best computer for the money.</p>
<p>Thinkpads are cheap on the used market. They feature good hardware that is usually upgradeable. They feature easy to repair designs and detailed manuals on how to replace and troubleshoot any issue.</p>
<p>Thinkpads also offer the best keyboards on the market. Keyboards that have largely remained unchanged and constant.</p>
<p>You're computer will always be a stable unchanging tool with a Thinkpad.</p>
<p>As for phones, the Pixel phones are the best option because most of security updates are made in the open and GrapheOS can use these to maintain the security of their OS.</p>

                ]]>
            </description>
        </item><item>
            <title>Testing - Code syntax highlighting</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2024-07-27-code-highlight</link>
            <pubDate>Sat, 27 Jul 2024 00:00:00 +0100</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Testing - Code syntax highlighting</h1>
<p>Currently only supports: Javascript, Yaml, Shell</p>
<p>Needs expanding for every new language.</p>
<h2>Javascript</h2>
<pre><code class="language-js">const x = 10
const y = 5

function add(a, b) {
    return a + b
}

const result = add(x, y);
</code></pre>
<h2>Yaml</h2>
<blockquote>
<p>Only registered for yaml, not longer supporting yml</p>
</blockquote>
<pre><code class="language-yaml">name: E2E Cypress testing
on:
  push:
    branches:
      - main
jobs:
  cypress-run:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          browser: chrome
          headless: true
          env: host=https://alanionita.github.io
          
      - uses: actions/upload-artifact@v1
        if: failure()
        with:
          name: fails
          path: cypress/    
</code></pre>
<h2>Shell</h2>
<blockquote>
<p>Doesn't seem to be displaying correctly but works</p>
</blockquote>
<pre><code class="language-sh">
cat ./test/test-file.txt

wc -l en_US.twitter.txt

foo@bar:~$ whoami

foo

</code></pre>
<h2>VTL</h2>
<blockquote>
<p>No highlightjs support, but using PHP language works</p>
</blockquote>
<pre><code class="language-vtl">#set( $expNames  = {} )
#set( $expValues = {} )
#set( $expSet = {} )
#set( $expRemove = [] )

#foreach( $entry in $context.arguments.newProfile.entrySet() )
    #if( $entry.key != "name" )
        #if( (!$entry.value) &#x26;&#x26; ("$!{entry.value}" == "") )
            ## If the argument is set to null, then remove that attribute from the item in DynamoDB **

            #set( $discard = ${expRemove.add("#${entry.key}")} )
            $!{expNames.put("#${entry.key}", "$entry.key")}
        #else
            ## Otherwise set (or update) the attribute on the item in DynamoDB **

            $!{expSet.put("#${entry.key}", ":${entry.key}")}
            $!{expNames.put("#${entry.key}", "$entry.key")}
            $!{expValues.put(":${entry.key}", { 
                "S": "${entry.value}"
            })}
            
        #end
    #else
        $!{expSet.put("#${entry.key}", ":${entry.key}")}
        $!{expNames.put("#${entry.key}", "$entry.key")}
        $!{expValues.put(":${entry.key}", { 
            "S": "${entry.value}"
        })}
    #end
#end

## Start building the update expression, starting with attributes we are going to SET **

#set( $expression = "" )
#if( !${expSet.isEmpty()} )
    #set( $expression = "SET" )
    #foreach( $entry in $expSet.entrySet() )
        #set( $expression = "${expression} ${entry.key} = ${entry.value}" )
        #if ( $foreach.hasNext )
            #set( $expression = "${expression}," )
        #end
    #end
#end

## Continue building the update expression, adding attributes we are going to REMOVE **
#if( !${expRemove.isEmpty()} )
    #set( $expression = "${expression} REMOVE" )

    #foreach( $entry in $expRemove )
        #set( $expression = "${expression} ${entry}" )
        #if ( $foreach.hasNext )
            #set( $expression = "${expression}," )
        #end
    #end
#end

{
    "version": "2018-05-29",
    "operation": "UpdateItem",
    "key": {
        "id" : $util.dynamodb.toDynamoDBJson($context.identity.username)
    },
    "update": {
        "expression" : "${expression}"
        #if( !${expNames.isEmpty()} )
            ,"expressionNames" : $utils.toJson($expNames)
        #end
        #if( !${expValues.isEmpty()} )
            ,"expressionValues" : $utils.toJson($expValues)
        #end
    },
    "condition" : {
        "expression" : "attribute_exists(id)"
    }
}

</code></pre>
<h2>JSON</h2>
<pre><code class="language-json">{
    "version" : "2018-05-29",
    "operation" : "UpdateItem",
    "key": {
        "id" : $util.dynamodb.toDynamoDBJson($context.identity.username)
    },

    "update" : {
        "expression" : "set #name = :name, imgUrl = :imgUrl, bgImgUrl = :bgImgUrl, bio = :bio, #location = :location, website = :website, birthdate = :birthdate",
        "expressionNames" : {
           "#name" : "name",
           "#location": "location",
       },

       "expressionValues" : {
            ":name" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.name),
            ":imgUrl" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.imgUrl),
            ":bgImgUrl" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.bgImgUrl),
            ":bio" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.bio),
            ":location" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.location),
            ":website" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.website),
            ":birthdate" : $util.dynamodb.toDynamoDBJson($context.arguments.newProfile.birthdate),
        }

    },

    "condition" : {
        "expression" : "attribute_exists(id)"
    },
}

</code></pre>
<h2>GraphQL</h2>
<blockquote>
<p>only registered for gql</p>
</blockquote>
<pre><code class="language-gql">input ProfileInput {
    name: String!
    imgUrl: AWSURL
    bgImgUrl: AWSURL
    bio: String
    location: String
    website: String
    birthdate: AWSDate
}

</code></pre>
<h2>Markdown</h2>
<pre><code class="language-md">
| Requirement   | Minimum   | Recommended     |
| ------------- | --------- | --------------- |
| CPU cores     | 4         | 8               |
| Memory        | 8         | 16              |
| Display res   | 1366x768  | 1920x1080       |
| HDD           | 4GB       | 52GB            |

</code></pre>
<h2>Svelte</h2>
<pre><code class="language-ts">&#x3C;script lang="ts">
	import type { PageProps } from './$types';

	let { data }: PageProps = $props();
&#x3C;/script>

&#x3C;h1>{data.title}&#x3C;/h1>
&#x3C;div>{@html data.content}&#x3C;/div>

</code></pre>

                ]]>
            </description>
        </item><item>
            <title>Life OS</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-03-28-life-os</link>
            <pubDate>Sun, 28 Mar 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Life OS</h1>
<p>After recently listening to <a href="https://www.calnewport.com/podcast/">Cal Newport's - Deep Questions podcast</a> I decided to think more about my system for managing life goals.</p>
<p>The system suggests the following docs:</p>
<ul>
<li>core plan or root plan</li>
<li>personal values plan</li>
<li>professional values plan</li>
<li>maintenance plan</li>
<li>progress plan</li>
</ul>
<p>This docs is my personal interpretation of that system.</p>
<h2>Why plan?</h2>
<p>The reality is that life is measured in years, yet lived by the day, hour, minute, second.</p>
<p>Life expectancy is up so in my situation, if I'm lucky, I can expect a good 50 years of ... life!</p>
<p>50 years is a log time without a map of what you care about, what you want to do, where you want to go, and who you want to be.</p>
<p>To some planning might remove some of the fun from the next few years. I've recently reread the <a href="https://waitbutwhy.com/2015/12/the-tail-end.html">The tail end</a> article from WaitButWhy to make me realise that I plan not to decide what I want to happen, but to make sure that everything I want does happen.</p>
<p>Professionally, software development is and ever evolving field and as an engineer I need to chart the best course into the future, whilst making sure I'm still relevant now.</p>
<h2>The ever evolving plan</h2>
<p>I've used a few methods to carve out this plan in the past. I wanted to outline the resources and current plan I've used so far.</p>
<p>The one method that I've abused a lot over the years was 'do more of what you like', which explain why I spent more time playing video games, that studying in high school.</p>
<p>Should we do more of what we like or should we find areas where that selfish goal also has a an altruistic side-effect.</p>
<p>This is where another great resource came in for me, <a href="https://80000hours.org">80000hours.org</a>. The site highlights the greatest issues facing humanity and informs the reader how to pursue the biggest impact jobs working on those issues.</p>
<p>I use the guidance from 80000h as a moral compass that allows me to gauge if I'm making an impact.</p>
<p>For planning I tend to run yearly goals, determined annually in June. I use the <a href="https://yearcompass.com">YearCompass </a> framework to help me reflect on the past year and chart the next.</p>
<p>For day to day operation I use a Bullet Journal system. Every month I'll use my yearly goals to set monthly aims, and then I daily / weekly aim to spend time towards those goals.</p>
<p>I usually timebox things in the calendar in order to make sure I do them or spend the time.</p>
<p>Another important concept to mention are distractions. My aim is to constantly limit distraction to allow more time for working on the plan.</p>
<h2>Root plan</h2>
<p>A document where core systems and strategies to organise life are stored. Instead of storing everything in your head - write it down, flesh it out.</p>
<p>This plan must be generic and should not capture the specific rules.</p>
<p>The <code>root</code> will point to other more specific documents:</p>
<h2>Value plan</h2>
<p>An outline of roles responsibilities and the core values I need to display in those.</p>
<h2>Personal plan</h2>
<p>A document on all personal goals and processes.</p>
<h2>Professional plan</h2>
<p>A document on all professional goals and processes.</p>
<h2>Maintenance procedure</h2>
<p>How do I maintain these systems - when, where, what.</p>
<p>A good idea here is to think about how data enters the system and how it leaves it.</p>
<h2>Operational plan</h2>
<p>How do things get done and what are some of the habits and process you use to achieve the things on the plan.</p>
<h2>Metrics</h2>
<p>What are you going to track in order to measure the system.</p>

                ]]>
            </description>
        </item><item>
            <title>Raspberry Pi OS Study</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-03-21-pi-os-study</link>
            <pubDate>Sun, 21 Mar 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Raspberry Pi OS Study</h1>
<p>I use the Raspberry Pi a lot, they are great little devices for prototyping ideas and building projects.</p>
<p>Choosing an OS has been tricky because there are so many options out there.</p>
<p>Requirements:</p>
<pre><code class="language-md">[ ] easy headless setup (no monitor, keyboard, or mouse needed)
[ ] reliable OS
[ ] flexible OS
[ ] transferable skills
[ ] small footprint
[ ] solid support
[ ] ROS support*
</code></pre>
<p>Grades:</p>
<pre><code>❌ fail
✅ pass
🟡 potential
🏁 in progress
</code></pre>
<h2>What is my process</h2>
<p>Using <a href="https://www.ansible.com">Ansible</a> to run a set of playbooks on the Pi: OS hardening, and improved the security etc.</p>
<p>In general I just care about an OS that can take minutes to flash and is easy to SSH into via Ansible once connected to my network.</p>
<p>I'm planning to write a post on Ansible soon.</p>
<h2>What is the hardware?</h2>
<p>I prefer the standard RPi unit usually the 4GB or 8GB version.</p>
<p>Although I've tried the Pi Zero, I've not used it for projects recently. I would use it if I need the form factor, but using it will probably make me change the OS.</p>
<h2>What was I using?</h2>
<p><a href="https://blog.hypriot.com">HypriotOS</a>, which is an OS that comes with Docker pre-installed.</p>
<p>Hypriot was also really easy to configure before mounting - it came with a clear documentation and the config files were easy to get to.</p>
<p>Unfortunately the HypriotOS community and support is small at the minute and I've encountered a few issues with the OS.</p>
<p>I wanted to move away from it before support became a problem.</p>
<pre><code class="language-md">✅ easy headless setup (no monitor, keyboard, or mouse needed)
🟡 reliable OS
✅ flexible OS
🟡 transferable skills
❌ ROS support*
✅ small footprint
❌ solid support
</code></pre>
<h2>Why, RPi OS?</h2>
<p>Raspberry Pi OS (Raspian) was harder to set up in headless mode a few months ago. In my case I remember there being a lot of steps in order to set everything up.</p>
<p>I should test this again, but since my main requirement is not met (ROS support) I might not use it anyway.</p>
<pre><code class="language-md">🟡 easy headless setup (no monitor, keyboard, or mouse needed)
✅ reliable OS
✅ flexible OS
🟡 transferable skills
❌ ROS support*
✅ small footprint
✅ solid support
</code></pre>
<h2>OS Study</h2>
<h3>Ubuntu Server LTS 20.04</h3>
<p>Download from <a href="https://ubuntu.com/download">here</a></p>
<p>Best option with a small footprint (until you update/upgrade). Support with ROS and Ubuntu itself is a popular OS in general which makes troubleshooting easy.</p>
<p>LTS stands for Long Term Support so I can reliably reuse this image for projects for the next few years.</p>
<p>This also means that it will be a popular option for enterprise clients too so all the skills learned are transferable, which I can't say the same about Raspberry Pi OS or HypriotOS.</p>
<p>The setup is simple because Ubuntu Server is included in the <a href="https://www.raspberrypi.org/software/">Raspberry Pi Imager</a>.</p>
<pre><code class="language-md">✅ easy headless setup (no monitor, keyboard, or mouse needed)
✅ reliable OS
✅ flexible OS
✅ transferable skills
✅ ROS support*
🟡 small footprint
✅ solid support
</code></pre>
<p>Not the smallest OS on the list, especially after updating and upgrading.</p>
<p>Result: best pick for now</p>
<h3>Alpine Linux</h3>
<p>Download from <a href="https://alpinelinux.org/about/">here</a></p>
<p>Huge fan of Alpine from Docker and I really wanted to make this work as my server driver.</p>
<p>Again the ROS requirement is a 'no-go', but for other project this could be an ideal OS.</p>
<p>The size and performance is awesome!</p>
<p>Skills learned through using the OS are not as transferable as Ubuntu, but you can reuse those skill if you ever deal with Docker instance - which is still a plus.</p>
<p>The setup on the site is a lot more complex that what I came up with</p>
<pre><code class="language-sh">// 1. Download the tar from the Alpine site

// 2. Create a bootable SD card using PI imager

// 3. Extract the tarball contents to the SD card.

tar -xvf alpine-rpi-3.13.2-aarch64.tar.gz -C {SD-card}

// 4. Pop the SD card into the Pi 
</code></pre>
<p>Annoyingly I need a desktop and peripherals in order to complete the setup.</p>
<pre><code class="language-md">❌ easy headless setup (no monitor, keyboard, or mouse needed)
✅ reliable OS
✅ flexible OS
✅ transferable skills
❌ ROS support*
✅ small footprint
✅ solid support
</code></pre>
<p>Result: cannot complete the setup in headless mode</p>
<h2>Ubuntu Core</h2>
<p>Download from  <a href="https://ubuntu.com/core">here</a></p>
<p>An OS specifically designed for embedded solutions.</p>
<p>Core has a lot of interesting features for embedded: OTA updates, secure boot, full disk encryption, recovery mode, and Snaps.</p>
<p>Everything apart from Snaps is awesome and as an ex-embedded developer I was drooling when I read about it.</p>
<p><a href="https://ubuntu.com/core/docs/snaps-in-ubuntu-core">Snaps</a> are 🟡 for me. A lot of people are strongly against it, but I'm not one of them.</p>
<p>I can live with the format, but it does introduce a new step in the workflow: compiling your app to a Snap.</p>
<p>After a bit of reading I discovered the official Canonical advice re: Ubuntu Core:</p>
<blockquote>
<p>To be used as part of a set and forget system. For deployment please use Ubuntu Server</p>
</blockquote>
<p>That makes sense now and it's a serious contender for finished projects.</p>
<p>The setup is simple because Ubuntu Core is included in the <a href="https://www.raspberrypi.org/software/">Raspberry Pi Imager</a>.</p>
<p>Annoyingly though Ubuntu Core requires a desktop and peripherals in order to complete the setup.</p>
<pre><code class="language-md">❌ easy headless setup (no monitor, keyboard, or mouse needed)
✅ reliable OS
❌ flexible OS
❌ transferable skills
❌ ROS support*
✅ small footprint
✅ solid support
</code></pre>
<p>I marked it down for flexibility because Ubuntu Core is not meant for development. Also marked it down for transferable skills since it seems like there's a lot of specific Ubuntu Core knowledge needed to make this work and those skills are not all transferable.</p>
<p>Result: cannot complete the setup in headless mode</p>
<h2>SkiffOS</h2>
<p>Download from <a href="https://github.com/skiffos/skiffos">here</a></p>
<p>Skiff is a lightweight OS and is very similar to HypriotOS.</p>
<p>It's built around Docker using Buildroot to produce a minimal 'single-file' host OS. 🤯</p>
<p>Another benefit here was the fact that I can configure the build before compilation so I can probably reduce the amount of work done on the machine to complete the setup.</p>
<p>The config files use <code>yaml</code> which is a super transferable skill in Docker and Cloud environments.</p>
<pre><code class="language-md">🏁 easy headless setup (no monitor, keyboard, or mouse needed)
🏁 reliable OS
✅ flexible OS
✅ transferable skills
❌ ROS support*
✅ small footprint
🏁 solid support
</code></pre>
<p>The setup seems easy enough but I had issues with the gcc and make so I couldn't actually try this out properly.</p>
<p>The fact that it comes with Docker pre-installed makes this a really 🌶 option for flexibility. It's also got a really small footprint depending on which layers you need and how many.</p>
<p>🏁 means that although I started testing, I couldn't make my mind just yet. SkiffOS is a new project so I won't be able to tell how they are with future support just yet.</p>
<p>Result: Needs more testing in order to get a clear view.</p>
<h2>Results summary</h2>
<p>Final rankings</p>
<pre><code class="language-md">| Criteria              | RPi OS | Ubuntu    | Alpine  | Ubuntu   | SkiffOS |
|                       |        | 20.04 LTS | Linux   | Core     |         |
| --------------------- | ------ | ----------| ------- | -------- | ------- |
| easy headless init    | 🟡     | ✅        | ❌      | ❌      | 🏁      |
| reliable OS           | ✅     | ✅        | ✅      | ✅      | 🏁      |
| flexible OS           | ✅     | ✅        | ✅      | ❌      | ✅      |
| transferable skills   | 🟡     | ✅        | ✅      | ❌      | ✅      |
| small footprint       | ✅     | 🟡        | ✅      | ✅      | ✅      |
| solid support         | ✅     | ✅        | ✅      | ✅      | 🏁      |
| ROS support*          | ❌     | ✅        | ❌      | ❌      | ❌      |
</code></pre>

                ]]>
            </description>
        </item><item>
            <title>What Next</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-03-19-what-next</link>
            <pubDate>Fri, 19 Mar 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>What next?</h1>
<p>In my <a href="https://www.amazon.co.uk/Pragmatic-Programmer-Andrew-Hunt/dp/020161622X">Pragmatic Programmer</a> the authors talk about your 'learning portfolio'. The assumption being that you should treat your learning like an investment portfolio and invest your time in topics depending on risk.</p>
<p>2021 is my 4th year as a developer and I've been processing my learning portfolio a lot lately.</p>
<h2>Summary</h2>
<p>My current learning portfolio is made up from high risk areas (embedded, machine learning, robotics), medium risk (web security), and low risk (web frameworks, web native, AWS cloud).</p>
<h2>What is my risk?</h2>
<p>Risk tolerance is unique to everyone. For me I'm in a high risk stage at the minute.</p>
<p>This means that I can afford to explore a lot and not really care about consequences to my career.</p>
<p>I actually don't see programming as a career, it more of a profession - it something I myself doing for the rest of my life. The fact that I get paid for it is a really nice bonus.</p>
<pre><code class="language-sh">risk = time_investment / personal_gain;
</code></pre>
<p>Risk has nothing to do with fun and it's mainly related to return - can I make a living or make money from it.</p>
<p>Choosing a high risk learning area is unlikely to payoff now, but it might return a bigger reward in the future as technology progresses.</p>
<p>High risk areas are more likely to be harder topics or use knowledge that you don't necessarily have right now.</p>
<h2>My Portfolio</h2>
<h3>Modern JS frameworks (low risk)</h3>
<p>React is a preference for me and I've made a salary from React since the beginning.</p>
<p>I know enough of React now that it doesn't take a lot of time for me to keep up with releases, new patterns, and features.</p>
<p>The time I've spent in the ecosystem has allowed to encounter problems with React performance, and tooling.</p>
<p>Right now my favourite way to write React is within NextJS, library that still uses React for the design whilst adding some nice features for Server-Side Rendered apps and static file generation. NextJS is flexible enough to allow a lot of config, but they provide conventions around common sticking points with React.</p>
<p>Following that pattern I'm also really excited by NestJS, which looks to do the same for Node / Express apis.</p>
<h4>Notable mentions</h4>
<p>I care about the JAMstack because I like the performance benefits. I keep an eye on new releases and I read tutorials for Eleventy and RedwoodJS. Same goes for JAM platforms: Zeit, Netlify.</p>
<p>I also like VueJS as a framework and I admire their performance focus and the fact that it's a community-driven project. I'm not interested in moving to VueJS just yet, so my interest is shallow - reading and tutorials.</p>
<p>I found React to be 'good enough' from a performance perspective (only relative to European customer bases), but as I'm looking into embedded websites I'm starting to research lighter weight options. Preact is a clear option here since there's little rework required. Also considering and testing Svelte, but it comes with a bit of a refactor from React.</p>
<h3>Web Native and DIY (low risk)</h3>
<p>This is a preference and an area of learning.</p>
<p>My preference is to DIY something over using a module - when I use a module it really has to be a flywheel.</p>
<p>When it comes to frontends, I tend to lean towards building everything from scratch. My experience with accessibility has force me down this path and I find it more flexible than reusing components from a library and trying to edit them.</p>
<p>This also means that I need to keep tabs on browser API releases, Javascript language releases, CSS releases and so on.</p>
<h3>AWS Cloud (low risk)</h3>
<p>Fullstack development requires knowledge on how to build architectures. My preference here is AWS and I'm currently working through certifications on AWS.</p>
<p>This actually covers a lot of other topics: databases, AI services, hosting services, server management security and so much more. I categorise all of that together because it's easier to track.</p>
<h3>Embedded Development (high risk)</h3>
<p>Learning more about FPGA and micro-controller development. Currently learning about building drivers for sensors, algorithms for measuring uncertainty, and building multi sensor systems.</p>
<h3>Machine Learning (high risk)</h3>
<p>Having already spent a few years working with machine learning algorithms, it's still something I'm fascinated about.</p>
<p>It took a while to realise what I enjoy within the machines learning area and right now I'm excited about planetary observation using AI.</p>
<p>A recent example of this was using satellite imagery to detect economic well-being in rural India.</p>
<p>Object detection is my main passion here, but also interested in autonomous vehicles.</p>
<h3>Robotics (high risk)</h3>
<p>Embedded development and machine learning folds into robotics.</p>
<p>The main reason why I'm interested in robotics is to do with robotics aids or using robots to speed up a traditionally human based workflow.</p>
<p>One of the earliest robotics projects I planned was a farming drone that can detect plant growth issues.</p>
<h3>Web Security (medium risk)</h3>
<p>During the pandemic I start getting interested in cyber-security.</p>
<p>For my learning I'm currently learning everything about web security vulnerabilities, exploits, and tooling.</p>
<p>The main reason for this learning areas is to apply it to bug bounties and vulnerability disclosures.</p>

                ]]>
            </description>
        </item><item>
            <title>Why Teach</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-03-18-why-teach</link>
            <pubDate>Thu, 18 Mar 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Why Teach</h1>
<p>I've been asked this question a lot over the years, but never really dug into why.</p>
<p>Whenever I've been asked about it I've just shrugged and confirmed that I just like it. And that answer works for most conversations, but really - why do I like it?</p>
<h2>Lessons learned from teaching</h2>
<ul>
<li>teaching/mentoring others is hard because everyone is different - your role as a mentor is to quickly figure out the person and their learning style, and adjust your delivery</li>
<li>through teaching you learn more - the more people you teach the more you have access to different angles of looking at a problem and if you pay attention you'll learn something from that</li>
<li>teaching is humbling - you might not know the answer so it makes you realise how much more there is to learn</li>
<li>mentoring / teaching is rewarding - you get to see aspiring developers grow to junior developers in real time</li>
<li>teaching improves your memory of core basics, core basics improve your overall systems knowledge, and improved systems knowledge helps you with architecting systems and troubleshooting</li>
<li>your communication improves through teaching because of repetition and explaining the same concepts in different ways</li>
</ul>
<h2>My Teaching Timeline</h2>
<p>How long have I mentored or taught or advised on various topics?</p>
<p>During university I had chance to do a work placement in corporate sales. Up to then, my big dream was to be a CEO and move to Japan. My role was to sell HR and legal insurance so a big part of the pitch was fact finding about the business and seeing if there could areas where my coverage packages could help.</p>
<p>I very rarely oversold the package and was still the best in my hiring group. I cared about businesses and really wanted to learn about every single one and see where the product could actually help. A great job in hindsight for a business management student with a love of strategy.</p>
<p>That placement changed me for the better. I realised soon after that I wanted to make a difference so I continued advising entrepreneurs on marketing strategy during my last year of uni. I did it pro-bono, because I cared about exposure and the experience.</p>
<p>Some of these relationships turned out great and some didn't, but it was a great use of time.</p>
<p>I stopped doing all this pro-bono consulting once I graduated from uni and got into my first job, as mixed product/project manager at a small digital agency.</p>
<p>I managed the agencies retainer client so maintaining the client relationships was key. I achieved this by learning about their business, learning about their problem, and then figuring out what data I can find to plan out the solutions. It's unusual for a project manager to do this, which is why I talk about it as a mixed product/project role.</p>
<p>I was mentoring the clients on tech, strategy, and marketing.</p>
<p>I got back to my own strategy consulting a few years later whilst I was in my last season as a product manager. I started working with an incredible group of engineers developing aquaponics tech and sensors.</p>
<p>I couldn't code then, but knew enough about tech systems to know where to start. I was advising them on marketing, funding, strategy, whilst wanting to implement AI into the system. I wish I knew then how difficult a task that would be, but it got me to work with the Raspberry Pi for the first time.</p>
<p>My consulting was doing great: the group got selected as a contender for the Pitch@Palace event ran by a member of the Royal Family and I got to pitch the project to a room of investors and journalists.</p>
<p>But whilst I was mentoring the engineers on marketing they would teach me how to solder, how to code, how sensors worked and soon after that project reached it's completion I started thinking that I could actually program for a living.</p>
<p>I joined a coding bootcamp shortly after. I enjoyed the bootcamp environment so much that I jumped at the opportunity to teach a beginners class. I also applied for the instructor role, but wasn't as successful.</p>
<p>After graduating the bootcamp I was determined to continue with the tech mentoring so when the opportunity to start a coding bootcamp for refugees came up I was the first to sign up. I was a co-organiser of that bootcamp for a year and oversaw recruitment, teaching, and wellbeing for the students in collaboration with a team of like-minded mentors.</p>
<p>Some of the social problems facing refugees were getting to me and at that time I started to attend other mentoring groups that were casual and less frequent.</p>
<p>I kept doing this free tech mentoring for two years and in the process I must've worked with 100+ people looking to learn how to code.</p>
<p>Towards the end of that period I also got approached by a local university to help as a teaching instructor on their upcoming bootcamp.</p>
<p>This lead to me becoming and instructor on a future cohort.</p>
<h2>Conclusion</h2>
<p>I like teaching / mentoring for selfish reasons: it makes me a better developer and it also allows me to help aspiring developers join the industry.</p>

                ]]>
            </description>
        </item><item>
            <title>AWS - Serverless Advice</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-01-27-serverless-advice</link>
            <pubDate>Wed, 27 Jan 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>AWS - Serverless Advice</h1>
<p>I work for an AWS Advanced Partner. All of my work is inside AWS and most of the code and services we build and use are serverless.</p>
<p>My experience with serverless is a lengthy one, with the first time dipping my toe in the ecosystem in 2017 just as I graduated from a coding bootcamp.</p>
<p>Since then serverless has matured a lot, but I still see and hear of developers and businesses who haven't tried it yet or don't see the point.</p>
<p>With this post I'd hope to highlight why you should learn it, what you gain from it, and how to learn it (resources).</p>
<h2>Why you should learn it</h2>
<p>Serverless is popular because of the developer experience. It is so much easier to use a serverless service than a server-based deployment.</p>
<p>All the cloud vendors have incredible support for serverless: AWS, GCP, Azure.</p>
<p>New vendors are doing some really interesting work on serverless: Cloudflare, Netlify, Vercel.</p>
<p>These companies are at that stage in their growth where most of the services are free or close to free.</p>
<p>Tooling is incredible on all platforms. Even though no platform is awesome overall, each have their own gems - Azure is spending a lot of time on developer experience.</p>
<p>Serverless is simple to write. Everything is a function so by nature developers start to write small flexible micro-services. Part of the complexity of serverless comes from the architecture part of how we orchestrate all of the services to work together, but a quick refresher in distributed systems will help you navigate that.</p>
<p>Serverless is 'usually' cheaper that servers or virtual machines because of the on-demand nature and price per request.</p>
<p>Serverless is heavily documented. I'll takes AWS as an example and there are guides upon guides available. Not to mention official video, code snippets, and tutorials. That's just the official stuff too!</p>
<p>More and more companies require some knowledge. Due to the reasons above companies have adopted serverless and have either all their infrastructure as serverless solutions or run a hybrid mix of server and serverless solutions.</p>
<h2>How to learn it</h2>
<p>Serverless can be done through the platform consoles sure, but as soon as you start creating 2 or more resources the console becomes cumbersome, not to mention how hard it is to trace your steps.</p>
<p>It’s worth picking an infrastructure as code framework before you start and the best templating in my opinion is Serverless Framework or Terraform (courses below). Serverless Framework is easy to get started with, but there are some services that are not covered by it.</p>
<p>If you’re going serverless, go all in. If you have an existing architecture then port everything to serverless including DBs. It might need some refactoring, but moving slowly might not be as beneficial as you think. Event a mock fullstack sample will teach you more than gradually migrating the stack over time.</p>
<p>DynamoDB is the most common serverless database, but there are new ones coming into the fold. DynamoDB follows a concept of ‘single-table’ design which can be kinda hard to understand at first if you're used to SQL. You can sinply use different tables for entities event thought it means you loose some benefits and performance - single table designs are optimised for performance and cost.</p>
<p>Spend some time really digging into Lambda or Cloud Functions and how they function. Recent new features allow you to generate custom lambda containers using Docker - this really changes Lambda from a swiss-army knife to a chainsaw.</p>
<p>Look into Lambda or Cloud Function power tuning to counteract cold start problems - <a href="https://github.com/alexcasalboni/aws-lambda-power-tuning">AWS Lambda Power Tuning Tools Github repo</a>. Some Cloud Functions will not have any cold start problems, like the Cloudflare Workers.</p>
<p>Stick to single responsibility principle with serverless. Breaking architecture up into microservices should be always on your mind. You’ll reach a point where you need to use multiple lambda, streams (data streams) and data queues are useful for integrating micro-services and allowing for a share data flow.</p>
<p>Consider data logging and tracing early. Use AWS CloudWatch, AWS X-Ray or a 3rd party tool. It might cost a bit more, but it will save time when debugging.</p>
<p>Invest in a monitoring setup. You want to know when something gets hammered or gets throttled - a dashboard will do wonders to highlight this.</p>
<p>Take a certification course even if you don’t want to officially take the exam. The courses usually cover a lot of ground. The AWS certifications have free videos on FreeCodeCamp.</p>
<h2>Resources:</h2>
<ul>
<li>FooBar Serverless - loads of great tutorials <a href="https://www.youtube.com/channel/UCSLIvjWJwLRQze9Pn4cectQ">https://www.youtube.com/channel/UCSLIvjWJwLRQze9Pn4cectQ</a></li>
<li>Complete Coding - Sam and I worked in the same company last year and he's a great instructor, he covers a lot of serverless topics on Youtube <a href="https://www.youtube.com/channel/UC8uBP0Un18DJAnWjm1CPqBg">https://www.youtube.com/channel/UC8uBP0Un18DJAnWjm1CPqBg</a></li>
<li>Serverless Stack - the best tutorial for learning serverless, really elaborate, covers a lot of yml concepts and follows project structure best practices <a href="https://serverless-stack.com/">https://serverless-stack.com/</a></li>
<li>AWS Lambda Terraform cookbook - using Terraform to orchestrate any example of Lambda services <a href="https://github.com/nsriram/lambda-the-terraform-way">https://github.com/nsriram/lambda-the-terraform-way</a></li>
<li>FreeCodeCamp - AWS Associate Developer certification, covers a lot of serverless tools <a href="https://www.youtube.com/watch?v=RrKRN9zRBWs">https://www.youtube.com/watch?v=RrKRN9zRBWs</a></li>
<li>FreeCodeCamp - AWS Solutions Architect certification - this covers less serverless specifics, more networking, servers, business practices, can teach you a lot if you are a beginner <a href="https://www.youtube.com/watch?v=Ia-UEYYR44s&#x26;t=2616s">https://www.youtube.com/watch?v=Ia-UEYYR44s&#x26;t=2616s</a></li>
</ul>

                ]]>
            </description>
        </item><item>
            <title>Choose a Learning Path</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-01-23-learning-path</link>
            <pubDate>Sat, 23 Jan 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Choose a Learning Path</h1>
<p>My interests are wide-spanning and although they reside within the broad area of technology, they usually require different skills.</p>
<p>Each interest also requires time, planning, and organisation.</p>
<p>I've been considering recently how to best optimise my learning so that I can build up momentum. After watching a video from <a href="https://www.youtube.com/watch?v=sO1ctUNQ1k8">Devslopes</a> I realised that I'm having challenges with learning because my learning is not done towards a path, a singular goal.</p>
<h2>Background</h2>
<p>Like most people I hated school. It prescribed too much of what I had to learn and didn't do a great job of relating that with why I'm learning it and how it can benefit me.</p>
<p>Most of the topics I loved were topics that had decent teachers. Those teachers were in equal parts educators and actors, they knew exactly how to spark curiosity and wonder whilst also allowing me the freedom to make my own mind on it.</p>
<p>I was glad to finish school because finally, at university, I was pursuing something I liked. In hindsight university is where you first encounter the idea of learning for a path: you enrol in a course, you set your target on an industry and you slowly find a sub-category of work that you enjoy. This freedom meant that whilst following a business degree I could also pursue learning Japanese, sociology, service design, and advertising.</p>
<p>In university I started joining different clubs and branching out my interests. This helped me avoid boredom, fight off the loneliness of being a foreign student, introduced me to new people, and helped learn some valuable life skills.</p>
<p>It did have a negative impact: I started to identify myself as a polymath.</p>
<h2>The challenge</h2>
<p>A polymath or a person with multiple interests believe that they can be good at a few things. They are 'Jack of all trades'.</p>
<p>I read once that a huge chunk of learners never become experts in a field because they become too addicted to being a beginner. As soon as they come close to mastery, they either start misjudge their experience or find new topics to learn.</p>
<p>The main issue I have is that having multiple interests, although exciting, leaves me with too much planning. If I were to focus on electronics, I could focus on this until I reach mastery.</p>
<p>Ultimately it comes down to choices and the more choices you have the higher the cost of making decision.</p>
<h2>Inspiration</h2>
<p><a href="https://www.youtube.com/watch?v=sO1ctUNQ1k8">Devslopes</a> was tackling the question 'Should I learn Python in 2021' and the conclusion was 'No' because learning Python isn't part of a path that can get a beginner into the industry.</p>
<h2>Tools and process</h2>
<p>Because of the multiple interests I've already developed some systems to manage my interests.</p>
<p>The most useful tool for validating new skills has been the 'learning radar' inspired by the <a href="https://www.thoughtworks.com/radar">Thoughworks Tech Radar</a>.</p>
<p>Every year I decide what skills, tech, tools, practices, languages &#x26; frameworks I want to hold, assess, trial, adopt.</p>
<p>For each entry in the radar I plan quarterly goals, and from there I set a mastery target. Eg. By Q1 2021 I want to get an AWS certification.</p>
<p>The quarterly goals then get slip to weekly time slots. I time-box a lot and I find it really useful when learning.</p>
<h2>Assessing the process</h2>
<p>The biggest issue with the process is that my 'learning radar' is huge, which has a knock-on effect on the amount of time I can allocate to each entry.</p>
<p>This mean that it takes me longer to reach mastery for each entry.</p>
<p>Some entries, like calculus require vastly more energy than something like web development because of the ratio of skill/complexity for that entry. I'm a lot more experienced at web development so it's easier to complete goals for that entry.</p>
<h2>Refactoring the process</h2>
<p>Started by zooming out from the 'learning radar' and considering the 'paths' of each entry.</p>
<ul>
<li>
<p>'Learning Python' aligns with 'Machine Learning Engineer' and 'Web Security Engineer' and 'Automation Engineer (Network or DevOps)' paths.</p>
</li>
<li>
<p>'Learning Rust' aligns with 'Embedded Engineer' and 'Robotics Engineer' paths.</p>
</li>
<li>
<p>'Learning Calculus' aligns with 'Machine Learning Engineer' and 'Robotics Engineer' paths.</p>
</li>
</ul>
<p>After mapping each entry to a path patterns start to develop.</p>
<p>I realise that some entries are shared by multiple paths, so I settle on the rule of only adding something new to the 'learning radar' if it aligns to 2 or more paths.</p>
<p>As hard as it is I decide to drop all other entries that don't match the above rule.</p>
<p>A small sample of learning entries I dropped:</p>
<ul>
<li>WebGL</li>
<li>Vue</li>
<li>Electron</li>
<li>Flask</li>
<li>Swift</li>
<li>Kotlin</li>
<li>Flutter</li>
</ul>
<h2>What paths am I following</h2>
<ul>
<li>Software Engineer with focus on Web and Cloud</li>
<li>Machine Learning Engineer with focus on economic models and satellite imagery</li>
<li>SysOps Engineer with a focus DevOps and Networking</li>
<li>Security Engineer with a focus on Web Security</li>
<li>Embedded Engineer with a focus on human machine interfaces and sensors</li>
<li>Robotics Engineer with a focus on autonomous systems for land / air vehicles and soft robots</li>
</ul>
<h2>Remaining work</h2>
<p>The paths themselves also need some prioritisation based on success rates. For example there's not as many 'Machine Learning Engineer' roles in my area as 'Software Engineer' roles.</p>
<p>I also need to factor in the difficulty of a particular field and how far do I want to get down that path.</p>
<p>There seem to be a lot of paths so there's more work to do here to whittle these down.</p>
<h2>Summary</h2>
<p>Don't learn anything that doesn't align to a path. I might feel nice to play around with an obscure knowledge but your time is limited.</p>
<p>Settling on a path / paths makes it easier to choose what you should learn next.</p>

                ]]>
            </description>
        </item><item>
            <title>Stack Overflow Contributions</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-01-20-so-contributions</link>
            <pubDate>Wed, 20 Jan 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Stack Overflow Contributions</h1>
<p>Stack Overflow is a developers friend, but too many developers consume its content without adding to it.</p>
<p>Here is the story of what drove me to change that habit, what I'm doing now, and what results I've seen from this effort.</p>
<h2>Background</h2>
<p>I started contributing to Stack Overflow in 2018. At the time I was in my first proper development job I was the only official web developer and I had to build a lot complicated apps.</p>
<p>Stack Overflow was my oasis when I would get stuck. I also never posted questions, I was scared of looking silly, I was worried of the responses I might get, so I just consumed.</p>
<p>A few years later I started doing a small amount of open source maintaining and it massively expanded both my knowledge and self-awareness.</p>
<p>From open source I also learned how maintainers get paid nothing or poorly, even when their software is used by big businesses.</p>
<p>Open source made reassess my approach with Stack Overflow and I wanted to start contributing again.</p>
<p>To my surprise when I returned to Stack Overflow and signed in with an old account I realised that some of the questions from the start of my development journey were receiving thousands of views.</p>
<p>My early Electron questions were collectively getting 10,000 views!</p>
<h2>Epiphany</h2>
<p>I didn't want to share my questions with the Stack Overflow because I thought they were basic. My Electron posts were proof that actually I was an early adopter and as others were coming over to Electron they were encountering similar problems.</p>
<p>I was validating my ideas instead of allowing the community to choose.</p>
<h2>Analysis</h2>
<p>After the penny dropped I renamed by Stack Overflow account to match my name. I wanted to act as another element of my digital persona.</p>
<p>Now I started analysing my Stack Overflow posts and uncovered some trends:</p>
<ul>
<li>most of my posts were targeted at beginners</li>
<li>most of the posts were on new tech with growing market shares</li>
<li>some of my posts covered obscure behavior from common tasks</li>
<li>my best answers were long, simple to follow, and shared example code snippets</li>
<li>most of my questions had a lot of comments</li>
</ul>
<h2>Process</h2>
<p>From the analysis there's a clear process developing.</p>
<h3>Niche</h3>
<p>Having a niche on Stack Overflow is really important. The community mimics the tech landscape so there is a lot of topics to contribute to.</p>
<p>Each topic comes with it's own caveats too. For example, the <code>javascript</code> topic is difficult to support since there are many followers and questions get answers instantly. This makes it unlikely to provide an answer that gets voted or accepted as an answer.</p>
<p>On the other hand new technologies are barren, partly because the Venn diagram of new adopters and Stack Overflow posters is really small.</p>
<p>It's in new technologies that you have a better chance at receiving votes and getting answers accepted.</p>
<h3>Posts content and quality</h3>
<p>Stack Overflow allows you to answer your own question and I've learned early on that this system can help with my problem solving: I would find a problem, post it on Stack Overflow, and keep working on the problem. Some times I would get an answer and other times I would uncover the solution myself.</p>
<p>I found this pattern to be similar to rubber-ducking aka talking to yourself expect instead of talking to myself I'd be talking to the internet.</p>
<p>Questions on Stack Overflow have to be specific. This in itself helped my problem solving because I would usually find a solution during the question submition screen from Stack Overflow.</p>
<p>Poorly written questions get negative feedback from Stack Overflow. This can be to do with the question framing, lack of context, or a lack of difficulty. If you ask a stupid question you will know about it!</p>
<p>I realised that 'performant' questions usually are procedural: if I do X, I get Y, but I expect Z. Code samples, short copy, and useful headings make it more likely for a question to be answered or interacted with in a positive way.</p>
<p>Answers follow similar patterns: good formatting, clear headlines, and sample code. In addition to this, answers benefit from multiple scenario handling: if you want Z do this, if you use Y do this.</p>
<p>Bigger answers are RARE on Stack Overflow because the community values brevity and chases the accepted answer metric. When you write long-form answers you essentially give more context and cover more scenarios, so overall your performance is better.</p>
<p>Do notice however that this only works with niche threads, since that is where knowledge is missing. To someone learning a new technology a lengthy, in-depth explanation is just what they require.</p>
<h3>Writing style</h3>
<p>Stack Overflow has a lot of resources on <a href="https://stackoverflow.com/help/how-to-answer">how to write a good answer</a> and it is crucial guidance to follow.</p>
<p>It helped me learn how to write with a neutral tone where no one is offended by the answer and the logic of the answers is enough to avoid personal preferences or fanboy behaviour.</p>
<h3>Post frequency and lifecycle</h3>
<p>Finding a fresh pool of new questions without answers and crafting thoughtful answers takes some time so I set myself the goal to post up to 5 answers per week.</p>
<p>Both questions and answers are actually evergreen and I decided early on that there are no benefits to answering early. I wanted to produce quality content that over time would be more valuable.</p>
<h2>Results</h2>
<p>I ran the above process in August 2020 and kept up with it for two weeks.</p>
<p>At the time I was reaching 22,000 people through my questions and answers.</p>
<p>I focused mainly on the NextJS topic since it was a technology I knew and liked. The community on Stack Overflow was also lackluster even though every day there would be 10 new questions.</p>
<p>I decided to check into my account again this month - January 2021.</p>
<p>I have now reached 106,000 people!</p>
<p>For January 2021 I ranked in the top 14% percentile, with a rank of #28778.</p>
<p>I also gained +67800 change points - which outlines growth and an increase in ranking. This puts me in a group on only 5 other users.</p>
<h2>Closing remarks</h2>
<p>I'm astonished of what can be achieved with such a small amount of effort.</p>
<p>I can't believe that my content has reached 100k people and the stream of positive comments and upvotes is heart warming.</p>
<p>I wanted to expand my contribution to the platform I feel like I have achieved that goal. Little did I know that I would also improve how I write technical instructions, how to deal with online negativity, and how to problem solve.</p>
<p>I also received a book offer request through Stack Overflow.</p>

                ]]>
            </description>
        </item><item>
            <title>The MVP fallacy</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-01-17-mvp-fallacy</link>
            <pubDate>Sun, 17 Jan 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>The MVP fallacy</h1>
<p>Inspired by this post <a href="https://danluu.com/sounds-easy/">Sounds Easy by Dan Luu</a></p>
<blockquote>
<p>Businesses that actually care about turning a profit will spend a lot of time (hence, a lot of engineers) working on optimizing systems, even if an MVP for the system could have been built in a weekend.</p>
</blockquote>
<p>Far too often companies and especially startups prioritise speed over careful engineering. 'Proof of concepts' (POCs) and 'Minimum Viable Products' (MVPs) often become the products without the necessary work needed to make them client-ready.</p>
<p>As an R&#x26;D developer, I've seen it too often and there is no real solution to it apart from a frank discussion with those in charge.</p>
<p>MVPs allow us to prove a point, solve a problem, and then get the funding for it. That feature should not be considered 'done' until we've received the tender and redesigned the solution.</p>
<p>When it comes to R&#x26;D project I like to think of the build through the lens of <a href="https://martin.kleppmann.com/">Martin Kleppmann's </a> maintainbility principles: Evolvability, Simplicity, and Operability.</p>
<h2>Can evolve</h2>
<p>The ability of a project to be easily updated or features can easily be added. The most important build goal for R&#x26;D projects and often the one that gets neglected.</p>
<p>But should we note prioritise speed at all cost?</p>
<p>We can, but the cost of that is exponential - usually a 'act first, think later' MVP end up being rewritten. Yes it takes time to plan a solid foundation for an MVP, but the cost of that is too often discounted as wasteful. In reality a bit of planning negates a rewrite.</p>
<h2>Simplicity</h2>
<p>Keeping a project lean is actually different depending on who you ask.</p>
<p>Firstly it's important to simplify the ask from a product perspective. As developers we must realise what a user will value and incite dialogue with the product teams. A simple product is not always a simple build, but it reduces some of the 'purpose' questions that we get during a difficult build.</p>
<p>Secondly the source-code must be simple and to simplify the source-code we must abstract, organise, and make use of modules where possible.</p>
<p>MVPs are vehicles for change, they spark change or progress. Usually they trigger new features and improvements, and most of time these changes and improvements are critical. If we can already predict this then it makes sense to prioritise abstraction at build time.</p>
<h2>Can operate</h2>
<p>Making sure that the MVP can operate under different conditions.</p>
<p>It all starts with documentation and moves on to logging, monitoring, and error handling.</p>
<p>An MVP that cannot be operated is a waste of resources.</p>
<h2>Culture</h2>
<p>Culture also has a huge part to play in this MVP fallacy. When we product an MVP developers prove that something is possible and to management that also translates as 'feature X is done'.</p>
<p>The language we use contributes to this when in reality developers skirt around what we 'didn't do' to reach a deadline for the MVP.</p>
<p>This is a simple problem to solve. Developers must have the tools to explain if required for the feature to be complete and managers should never equate an MVP to a completed feature.</p>
<p>Consistently delivering MVPs that get mistaken for full features by management is a bad product scenario since the product over time starts to resemble a 'house of cards'.</p>
<h2>Summary</h2>
<p>A completed MVP should never equate a completed feature. It's important that both developers and managers agree on this.</p>
<p>Equally important is that MVP maintainability is prioritised in parallel with speed.</p>
<p>After all 'haste makes waste'.</p>

                ]]>
            </description>
        </item><item>
            <title>Lead chats or 1-2-1s</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-01-17-lead-chats</link>
            <pubDate>Sun, 17 Jan 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Lead chats or 1-2-1s</h1>
<p>Having recently took on the role of tech lead for one of the squads, I wanted to document a bit about the process and system for running.</p>
<h2>People vs. Performance</h2>
<p>Manager-to-team-member chats are typically used in business for performance tracking and realignment. Most people who enter the industry have either worked in similar environments or never had the chance to experience such meetings.</p>
<p>Let me say this, 1-2-1s are not for performance! Sure performance does come up, but it should never be the goal.</p>
<p>The goals are related to personal growth and since everyone is different, every 1-2-1 is different.</p>
<p>It's also a 'process meeting', not chit-chat. As a facilitator we need to have a range of tool to overcome road block and direct the meeting towards a positive outcome.</p>
<h2>Intention and outcome</h2>
<p>Before starting a 1-2-1 I set a goal for it. I walk into a meeting or click that Meet url knowing what I want to achieve from the meeting.</p>
<p>Think hard about how the individual goals can help the team and vice versa.</p>
<p>After you've run a few meetings you'll have a list of outcomes and because we deal with human beings these outcomes end up being seasonal.</p>
<p>The ultimate goal is to help the individual reach clarity, set some short-term goals, and boots their motivation towards their new goals.</p>
<h2>Prep</h2>
<p>Review the persons past performance and make a note of feedback you want to share. This is an editing process and depends on the individual and their current 'season' - if someone is already unmotivated then withhold the difficult feedback.</p>
<p>Review the individuals previous goals. Have they made progress? Have they completed the goal? Did they receive any feedback about the most recent goal?</p>
<p>I also have a few canned questions to help drive the conversation:</p>
<blockquote>
<p>What have you enjoyed the most about the past sprint? Have you been having fun lately?</p>
</blockquote>
<p>Fun is crucial to fulfilling work and the question helps us focus on the positive parts of the sprint.</p>
<blockquote>
<p>What feedback do you have for me?</p>
</blockquote>
<p>This is a tricky question since everyone struggles to give feedback on the spot, but I repeat this each week. Usually I don't receive feedback, but when there's a bad week I always know about it.</p>
<p>This question is also trust building. I ask it every week because I care about feedback and I want my team to know that. I also want them to know that we all make mistakes and that I really care about knowing when I've made mistakes.</p>
<blockquote>
<p>How are you feeling after last sprint?</p>
</blockquote>
<p>Simple question to put everyone at ease and have a bit of a laugh. This is a great question to warm people up to talking about home life or in general discuss current emotions.</p>
<h2>During</h2>
<p>Go where the conversation goes until you reach clarity. Once you have clarity state it, filter it together, and reach an agreement. Once you've agreed, choose another questions from above or continue.</p>
<blockquote>
<p>Here is some feedback... What do you think of it?</p>
</blockquote>
<p>Provide the feedback to the team member and ask for their opinion. I think of feedback as a tool for growth, so usually framed as such and always delivered positively.</p>
<p>I tend to think of feedback of 'High five' and 'Watch out'.</p>
<p>Link 'High five' feedback to the receivers skills or ways of working.</p>
<p>When giving out 'Watch out' feedback make sure you also offer a clear path for adjustment and focus on habits and systems that can improve the situation.</p>
<p>Adjust you delivery to the receiver: some people are open to chat whilst other requires some coaxing.</p>
<p>Remember to have fun. There's a lot to think about, but were not robots - enjoy the conversation and company more than aiming to tick all the boxes.</p>
<h2>Habits</h2>
<p>Plan your meetings in advance and book the time in your calendar.</p>
<p>Dedicate a specific time for the meetings, I find that 25 minutes is enough to get through most of the above. If a wider discussion is needed I'll book a follow-up.</p>
<p>Try to batch your meetings together, it stops you from switching context. These meetings use up a lot of energy so its better to focus on them before you jump on the next tech task.</p>
<h2>Other tools</h2>
<p>Since we're talking about individual growth, it's important to try and build other tools that can help you maintain individual growth.</p>
<p>One easy tool is a share scoring system. The system is only used for self-reflection is particularly useful for developers who can benchmark their technical and soft skills.</p>
<p>Have the company policy on hand for those times when you get company policy questions.</p>
<p>Build up a collection of useful resources and work through that as a team. This helps with clarity and lets everyone self-serve when they want to level up.</p>

                ]]>
            </description>
        </item><item>
            <title>Why write a blog?</title>
            <link>https://alanionita.github.io/portfolio--svelte/blog/2021-01-13-why-write</link>
            <pubDate>Wed, 13 Jan 2021 00:00:00 Z</pubDate>
            <author>Alan Ionita</author>
            <description>
                <![CDATA[
                    <h1>Why write a blog?</h1>
<p>Selfish reasons - I often use writing for work and often repeat why and how I approach things.</p>
<p>This blog post is for me and other who want to understand the same things I care about: tech, social justice, learning, and more.</p>
<h2>Writing and I</h2>
<p>In school, some 12 years ago, I had to pick between maths and literature. It was a mandatory choice and considering my poor maths performance I picked literature.</p>
<p>I was fortunate to be taught by an amazing teacher and what started as trepidation ended up developing into a passion.</p>
<p>Literature and software development is not that different - they both involve a lot of writing, editing, and conceptualising ideas. The both require analysis and creativity and the both feel like introspective pursuits even though each profession has become more and more social since I first encountered them.</p>
<p>Why didn't I do programming instead of literature? I never had the option, nor the equipment. Before my first PC I used computers in school for learning Pascal and Word or in internet-caffes to shoot my friends in Counter Strike or race against them in Need For Speed.</p>
<h2>Purpose and content</h2>
<p>This is intended to be a raw collection of thoughts and ideas. Like my interests, this blog with snake and weave through a lot of topics.</p>

                ]]>
            </description>
        </item>
            </channel>
        </rss>